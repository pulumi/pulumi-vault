# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['SysConfigCorsArgs', 'SysConfigCors']

@pulumi.input_type
class SysConfigCorsArgs:
    def __init__(__self__, *,
                 allowed_origins: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]],
                 allowed_headers: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None):
        """
        The set of arguments for constructing a SysConfigCors resource.

        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_origins: Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_headers: Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
               - `Content-Type`
               - `X-Requested-With`
               - `X-Vault-AWS-IAM-Server-ID`
               - `X-Vault-MFA`
               - `X-Vault-No-Request-Forwarding`
               - `X-Vault-Wrap-Format`
               - `X-Vault-Wrap-TTL`
               - `X-Vault-Policy-Override`
               - `Authorization`
               - `X-Vault-Token`
        """
        pulumi.set(__self__, "allowed_origins", allowed_origins)
        if allowed_headers is not None:
            pulumi.set(__self__, "allowed_headers", allowed_headers)

    @_builtins.property
    @pulumi.getter(name="allowedOrigins")
    def allowed_origins(self) -> pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]:
        """
        Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
        """
        return pulumi.get(self, "allowed_origins")

    @allowed_origins.setter
    def allowed_origins(self, value: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]):
        pulumi.set(self, "allowed_origins", value)

    @_builtins.property
    @pulumi.getter(name="allowedHeaders")
    def allowed_headers(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
        - `Content-Type`
        - `X-Requested-With`
        - `X-Vault-AWS-IAM-Server-ID`
        - `X-Vault-MFA`
        - `X-Vault-No-Request-Forwarding`
        - `X-Vault-Wrap-Format`
        - `X-Vault-Wrap-TTL`
        - `X-Vault-Policy-Override`
        - `Authorization`
        - `X-Vault-Token`
        """
        return pulumi.get(self, "allowed_headers")

    @allowed_headers.setter
    def allowed_headers(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "allowed_headers", value)


@pulumi.input_type
class _SysConfigCorsState:
    def __init__(__self__, *,
                 allowed_headers: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 allowed_origins: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 enabled: pulumi.Input[Optional[_builtins.bool]] = None):
        """
        Input properties used for looking up and filtering SysConfigCors resources.

        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_headers: Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
               - `Content-Type`
               - `X-Requested-With`
               - `X-Vault-AWS-IAM-Server-ID`
               - `X-Vault-MFA`
               - `X-Vault-No-Request-Forwarding`
               - `X-Vault-Wrap-Format`
               - `X-Vault-Wrap-TTL`
               - `X-Vault-Policy-Override`
               - `Authorization`
               - `X-Vault-Token`
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_origins: Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
        :param pulumi.Input[_builtins.bool] enabled: (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowed_origins` is configured, and `false` when CORS is deleted.
        """
        if allowed_headers is not None:
            pulumi.set(__self__, "allowed_headers", allowed_headers)
        if allowed_origins is not None:
            pulumi.set(__self__, "allowed_origins", allowed_origins)
        if enabled is not None:
            pulumi.set(__self__, "enabled", enabled)

    @_builtins.property
    @pulumi.getter(name="allowedHeaders")
    def allowed_headers(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
        - `Content-Type`
        - `X-Requested-With`
        - `X-Vault-AWS-IAM-Server-ID`
        - `X-Vault-MFA`
        - `X-Vault-No-Request-Forwarding`
        - `X-Vault-Wrap-Format`
        - `X-Vault-Wrap-TTL`
        - `X-Vault-Policy-Override`
        - `Authorization`
        - `X-Vault-Token`
        """
        return pulumi.get(self, "allowed_headers")

    @allowed_headers.setter
    def allowed_headers(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "allowed_headers", value)

    @_builtins.property
    @pulumi.getter(name="allowedOrigins")
    def allowed_origins(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
        """
        return pulumi.get(self, "allowed_origins")

    @allowed_origins.setter
    def allowed_origins(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "allowed_origins", value)

    @_builtins.property
    @pulumi.getter
    def enabled(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowed_origins` is configured, and `false` when CORS is deleted.
        """
        return pulumi.get(self, "enabled")

    @enabled.setter
    def enabled(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "enabled", value)


@pulumi.type_token("vault:index/sysConfigCors:SysConfigCors")
class SysConfigCors(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 allowed_headers: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 allowed_origins: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        """
        Manages the CORS (Cross-Origin Resource Sharing) configuration for Vault, controlling which origins can make cross-origin requests and which headers are allowed.

        > **Important** This resource requires `sudo` capability and must be called from the root namespace. CORS configuration does not replicate across Performance Replication clusters in Vault Enterprise.

        **Note** This feature is available in Vault 1.14+

        ## Example Usage

        ### Enable CORS for specific origins

        ```python
        import pulumi
        import pulumi_vault as vault

        example = vault.SysConfigCors("example",
            allowed_origins=[
                "http://www.example.com",
                "https://app.example.com",
            ],
            allowed_headers=[
                "X-Custom-Header",
                "X-Application-ID",
            ])
        ```

        ### Allow all origins (wildcard)

        ```python
        import pulumi
        import pulumi_vault as vault

        wildcard = vault.SysConfigCors("wildcard", allowed_origins=["*"])
        ```

        ### Production environment configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        production = vault.SysConfigCors("production",
            allowed_origins=[
                "https://app.example.com",
                "https://admin.example.com",
                "https://api.example.com",
            ],
            allowed_headers=[
                "X-Custom-Header",
                "X-Request-ID",
                "X-Application-Version",
            ])
        ```

        ## Security Considerations

        * The wildcard `"*"` origin should be used cautiously and typically only in development environments
        * Allowing all origins in production can expose Vault to cross-site request forgery attacks
        * Custom headers should be carefully reviewed to ensure they don't expose sensitive information
        * CORS settings must be configured from the root namespace
        * In Vault Enterprise with Performance Replication, CORS configuration does not replicate across clusters - each secondary cluster must have its CORS configuration set independently
        * To disable CORS, delete the resource using `terraform destroy` or remove it from your configuration. There is no `enabled = false` option

        ## API Documentation

        For more information on the Vault CORS configuration API, see the [Vault API documentation](https://developer.hashicorp.com/vault/api-docs/system/config-cors).

        ## Import

        CORS configuration can be imported using the fixed ID `sys/config/cors`:

        ```sh
        $ pulumi import vault:index/sysConfigCors:SysConfigCors example sys/config/cors
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_headers: Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
               - `Content-Type`
               - `X-Requested-With`
               - `X-Vault-AWS-IAM-Server-ID`
               - `X-Vault-MFA`
               - `X-Vault-No-Request-Forwarding`
               - `X-Vault-Wrap-Format`
               - `X-Vault-Wrap-TTL`
               - `X-Vault-Policy-Override`
               - `Authorization`
               - `X-Vault-Token`
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_origins: Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: SysConfigCorsArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manages the CORS (Cross-Origin Resource Sharing) configuration for Vault, controlling which origins can make cross-origin requests and which headers are allowed.

        > **Important** This resource requires `sudo` capability and must be called from the root namespace. CORS configuration does not replicate across Performance Replication clusters in Vault Enterprise.

        **Note** This feature is available in Vault 1.14+

        ## Example Usage

        ### Enable CORS for specific origins

        ```python
        import pulumi
        import pulumi_vault as vault

        example = vault.SysConfigCors("example",
            allowed_origins=[
                "http://www.example.com",
                "https://app.example.com",
            ],
            allowed_headers=[
                "X-Custom-Header",
                "X-Application-ID",
            ])
        ```

        ### Allow all origins (wildcard)

        ```python
        import pulumi
        import pulumi_vault as vault

        wildcard = vault.SysConfigCors("wildcard", allowed_origins=["*"])
        ```

        ### Production environment configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        production = vault.SysConfigCors("production",
            allowed_origins=[
                "https://app.example.com",
                "https://admin.example.com",
                "https://api.example.com",
            ],
            allowed_headers=[
                "X-Custom-Header",
                "X-Request-ID",
                "X-Application-Version",
            ])
        ```

        ## Security Considerations

        * The wildcard `"*"` origin should be used cautiously and typically only in development environments
        * Allowing all origins in production can expose Vault to cross-site request forgery attacks
        * Custom headers should be carefully reviewed to ensure they don't expose sensitive information
        * CORS settings must be configured from the root namespace
        * In Vault Enterprise with Performance Replication, CORS configuration does not replicate across clusters - each secondary cluster must have its CORS configuration set independently
        * To disable CORS, delete the resource using `terraform destroy` or remove it from your configuration. There is no `enabled = false` option

        ## API Documentation

        For more information on the Vault CORS configuration API, see the [Vault API documentation](https://developer.hashicorp.com/vault/api-docs/system/config-cors).

        ## Import

        CORS configuration can be imported using the fixed ID `sys/config/cors`:

        ```sh
        $ pulumi import vault:index/sysConfigCors:SysConfigCors example sys/config/cors
        ```


        :param str resource_name: The name of the resource.
        :param SysConfigCorsArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(SysConfigCorsArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 allowed_headers: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 allowed_origins: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = SysConfigCorsArgs.__new__(SysConfigCorsArgs)

            __props__.__dict__["allowed_headers"] = allowed_headers
            if allowed_origins is None and not opts.urn:
                raise TypeError("Missing required property 'allowed_origins'")
            __props__.__dict__["allowed_origins"] = allowed_origins
            __props__.__dict__["enabled"] = None
        super(SysConfigCors, __self__).__init__(
            'vault:index/sysConfigCors:SysConfigCors',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            allowed_headers: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            allowed_origins: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
            enabled: pulumi.Input[Optional[_builtins.bool]] = None) -> 'SysConfigCors':
        """
        Get an existing SysConfigCors resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_headers: Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
               - `Content-Type`
               - `X-Requested-With`
               - `X-Vault-AWS-IAM-Server-ID`
               - `X-Vault-MFA`
               - `X-Vault-No-Request-Forwarding`
               - `X-Vault-Wrap-Format`
               - `X-Vault-Wrap-TTL`
               - `X-Vault-Policy-Override`
               - `Authorization`
               - `X-Vault-Token`
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] allowed_origins: Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
        :param pulumi.Input[_builtins.bool] enabled: (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowed_origins` is configured, and `false` when CORS is deleted.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _SysConfigCorsState.__new__(_SysConfigCorsState)

        __props__.__dict__["allowed_headers"] = allowed_headers
        __props__.__dict__["allowed_origins"] = allowed_origins
        __props__.__dict__["enabled"] = enabled
        return SysConfigCors(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="allowedHeaders")
    def allowed_headers(self) -> pulumi.Output[Optional[Sequence[_builtins.str]]]:
        """
        Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
        - `Content-Type`
        - `X-Requested-With`
        - `X-Vault-AWS-IAM-Server-ID`
        - `X-Vault-MFA`
        - `X-Vault-No-Request-Forwarding`
        - `X-Vault-Wrap-Format`
        - `X-Vault-Wrap-TTL`
        - `X-Vault-Policy-Override`
        - `Authorization`
        - `X-Vault-Token`
        """
        return pulumi.get(self, "allowed_headers")

    @_builtins.property
    @pulumi.getter(name="allowedOrigins")
    def allowed_origins(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
        """
        return pulumi.get(self, "allowed_origins")

    @_builtins.property
    @pulumi.getter
    def enabled(self) -> pulumi.Output[_builtins.bool]:
        """
        (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowed_origins` is configured, and `false` when CORS is deleted.
        """
        return pulumi.get(self, "enabled")


# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['KerberosAuthBackendConfigArgs', 'KerberosAuthBackendConfig']

@pulumi.input_type
class KerberosAuthBackendConfigArgs:
    def __init__(__self__, *,
                 keytab_wo: pulumi.Input[_builtins.str],
                 keytab_wo_version: pulumi.Input[_builtins.int],
                 mount: pulumi.Input[_builtins.str],
                 service_account: pulumi.Input[_builtins.str],
                 add_group_aliases: pulumi.Input[Optional[_builtins.bool]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 remove_instance_name: pulumi.Input[Optional[_builtins.bool]] = None):
        """
        The set of arguments for constructing a KerberosAuthBackendConfig resource.

        :param pulumi.Input[_builtins.str] keytab_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               Base64-encoded keytab file content. This is a write-only
               field and is not stored in Terraform state. The keytab must contain an entry
               matching the `service_account`.
        :param pulumi.Input[_builtins.int] keytab_wo_version: Version identifier for keytab updates. Increment this
               value to trigger a keytab update in Vault. This allows you to rotate the keytab without
               forcing resource replacement.
        :param pulumi.Input[_builtins.str] mount: Path where the Kerberos auth method is mounted.
               Changing this will force a new resource to be created.
        :param pulumi.Input[_builtins.str] service_account: The Kerberos service account associated with the
               keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vault_svc`).
        :param pulumi.Input[_builtins.bool] add_group_aliases: Adds group aliases during authentication. When
               enabled, Vault will create entity aliases for each group the user belongs to.
               Defaults to `false`.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] remove_instance_name: Removes instance names from Kerberos service
               principal names during authentication. This can be useful when the instance name
               is not relevant for authentication. Defaults to `false`.
        """
        pulumi.set(__self__, "keytab_wo", keytab_wo)
        pulumi.set(__self__, "keytab_wo_version", keytab_wo_version)
        pulumi.set(__self__, "mount", mount)
        pulumi.set(__self__, "service_account", service_account)
        if add_group_aliases is not None:
            pulumi.set(__self__, "add_group_aliases", add_group_aliases)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)
        if remove_instance_name is not None:
            pulumi.set(__self__, "remove_instance_name", remove_instance_name)

    @_builtins.property
    @pulumi.getter(name="keytabWo")
    def keytab_wo(self) -> pulumi.Input[_builtins.str]:
        """
        **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
        Base64-encoded keytab file content. This is a write-only
        field and is not stored in Terraform state. The keytab must contain an entry
        matching the `service_account`.
        """
        return pulumi.get(self, "keytab_wo")

    @keytab_wo.setter
    def keytab_wo(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "keytab_wo", value)

    @_builtins.property
    @pulumi.getter(name="keytabWoVersion")
    def keytab_wo_version(self) -> pulumi.Input[_builtins.int]:
        """
        Version identifier for keytab updates. Increment this
        value to trigger a keytab update in Vault. This allows you to rotate the keytab without
        forcing resource replacement.
        """
        return pulumi.get(self, "keytab_wo_version")

    @keytab_wo_version.setter
    def keytab_wo_version(self, value: pulumi.Input[_builtins.int]):
        pulumi.set(self, "keytab_wo_version", value)

    @_builtins.property
    @pulumi.getter
    def mount(self) -> pulumi.Input[_builtins.str]:
        """
        Path where the Kerberos auth method is mounted.
        Changing this will force a new resource to be created.
        """
        return pulumi.get(self, "mount")

    @mount.setter
    def mount(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "mount", value)

    @_builtins.property
    @pulumi.getter(name="serviceAccount")
    def service_account(self) -> pulumi.Input[_builtins.str]:
        """
        The Kerberos service account associated with the
        keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vault_svc`).
        """
        return pulumi.get(self, "service_account")

    @service_account.setter
    def service_account(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "service_account", value)

    @_builtins.property
    @pulumi.getter(name="addGroupAliases")
    def add_group_aliases(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Adds group aliases during authentication. When
        enabled, Vault will create entity aliases for each group the user belongs to.
        Defaults to `false`.
        """
        return pulumi.get(self, "add_group_aliases")

    @add_group_aliases.setter
    def add_group_aliases(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "add_group_aliases", value)

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The namespace to provision the resource in.
        The value should not contain leading or trailing forward slashes.
        The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @namespace.setter
    def namespace(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "namespace", value)

    @_builtins.property
    @pulumi.getter(name="removeInstanceName")
    def remove_instance_name(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Removes instance names from Kerberos service
        principal names during authentication. This can be useful when the instance name
        is not relevant for authentication. Defaults to `false`.
        """
        return pulumi.get(self, "remove_instance_name")

    @remove_instance_name.setter
    def remove_instance_name(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "remove_instance_name", value)


@pulumi.input_type
class _KerberosAuthBackendConfigState:
    def __init__(__self__, *,
                 add_group_aliases: pulumi.Input[Optional[_builtins.bool]] = None,
                 keytab_wo: pulumi.Input[Optional[_builtins.str]] = None,
                 keytab_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
                 mount: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 remove_instance_name: pulumi.Input[Optional[_builtins.bool]] = None,
                 service_account: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering KerberosAuthBackendConfig resources.

        :param pulumi.Input[_builtins.bool] add_group_aliases: Adds group aliases during authentication. When
               enabled, Vault will create entity aliases for each group the user belongs to.
               Defaults to `false`.
        :param pulumi.Input[_builtins.str] keytab_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               Base64-encoded keytab file content. This is a write-only
               field and is not stored in Terraform state. The keytab must contain an entry
               matching the `service_account`.
        :param pulumi.Input[_builtins.int] keytab_wo_version: Version identifier for keytab updates. Increment this
               value to trigger a keytab update in Vault. This allows you to rotate the keytab without
               forcing resource replacement.
        :param pulumi.Input[_builtins.str] mount: Path where the Kerberos auth method is mounted.
               Changing this will force a new resource to be created.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] remove_instance_name: Removes instance names from Kerberos service
               principal names during authentication. This can be useful when the instance name
               is not relevant for authentication. Defaults to `false`.
        :param pulumi.Input[_builtins.str] service_account: The Kerberos service account associated with the
               keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vault_svc`).
        """
        if add_group_aliases is not None:
            pulumi.set(__self__, "add_group_aliases", add_group_aliases)
        if keytab_wo is not None:
            pulumi.set(__self__, "keytab_wo", keytab_wo)
        if keytab_wo_version is not None:
            pulumi.set(__self__, "keytab_wo_version", keytab_wo_version)
        if mount is not None:
            pulumi.set(__self__, "mount", mount)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)
        if remove_instance_name is not None:
            pulumi.set(__self__, "remove_instance_name", remove_instance_name)
        if service_account is not None:
            pulumi.set(__self__, "service_account", service_account)

    @_builtins.property
    @pulumi.getter(name="addGroupAliases")
    def add_group_aliases(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Adds group aliases during authentication. When
        enabled, Vault will create entity aliases for each group the user belongs to.
        Defaults to `false`.
        """
        return pulumi.get(self, "add_group_aliases")

    @add_group_aliases.setter
    def add_group_aliases(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "add_group_aliases", value)

    @_builtins.property
    @pulumi.getter(name="keytabWo")
    def keytab_wo(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
        Base64-encoded keytab file content. This is a write-only
        field and is not stored in Terraform state. The keytab must contain an entry
        matching the `service_account`.
        """
        return pulumi.get(self, "keytab_wo")

    @keytab_wo.setter
    def keytab_wo(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "keytab_wo", value)

    @_builtins.property
    @pulumi.getter(name="keytabWoVersion")
    def keytab_wo_version(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        Version identifier for keytab updates. Increment this
        value to trigger a keytab update in Vault. This allows you to rotate the keytab without
        forcing resource replacement.
        """
        return pulumi.get(self, "keytab_wo_version")

    @keytab_wo_version.setter
    def keytab_wo_version(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "keytab_wo_version", value)

    @_builtins.property
    @pulumi.getter
    def mount(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Path where the Kerberos auth method is mounted.
        Changing this will force a new resource to be created.
        """
        return pulumi.get(self, "mount")

    @mount.setter
    def mount(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "mount", value)

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The namespace to provision the resource in.
        The value should not contain leading or trailing forward slashes.
        The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @namespace.setter
    def namespace(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "namespace", value)

    @_builtins.property
    @pulumi.getter(name="removeInstanceName")
    def remove_instance_name(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        Removes instance names from Kerberos service
        principal names during authentication. This can be useful when the instance name
        is not relevant for authentication. Defaults to `false`.
        """
        return pulumi.get(self, "remove_instance_name")

    @remove_instance_name.setter
    def remove_instance_name(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "remove_instance_name", value)

    @_builtins.property
    @pulumi.getter(name="serviceAccount")
    def service_account(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The Kerberos service account associated with the
        keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vault_svc`).
        """
        return pulumi.get(self, "service_account")

    @service_account.setter
    def service_account(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "service_account", value)


@pulumi.type_token("vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig")
class KerberosAuthBackendConfig(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 add_group_aliases: pulumi.Input[Optional[_builtins.bool]] = None,
                 keytab_wo: pulumi.Input[Optional[_builtins.str]] = None,
                 keytab_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
                 mount: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 remove_instance_name: pulumi.Input[Optional[_builtins.bool]] = None,
                 service_account: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        """
        Manages the configuration of a Kerberos Auth Backend in Vault.

        This resource configures the Kerberos authentication method by providing the keytab
        and service account information required for Vault to authenticate users via Kerberos.

        For more information, see the
        [Vault docs](https://www.vaultproject.io/docs/auth/kerberos).

        > **Important** The `keytab_wo` field is write-only and is not stored in Terraform state.
        It is only sent to Vault when the `keytab_wo_version` changes or during initial creation.
        See the main provider documentation for more details.

        > **Note** Vault does not support deleting auth backend configurations via the API.
        When this resource is destroyed or replaced (e.g., when changing the `mount`), it is
        only removed from Terraform state. The configuration remains in Vault until the auth
        mount itself is deleted.

        ## Example Usage

        ### Basic Configuration

        ```python
        import pulumi
        import pulumi_std as std
        import pulumi_vault as vault

        kerberos = vault.AuthBackend("kerberos",
            type="kerberos",
            path="kerberos")
        config = vault.KerberosAuthBackendConfig("config",
            mount=kerberos.path,
            keytab_wo=std.filebase64(input="/path/to/vault.keytab").result,
            keytab_wo_version=1,
            service_account="vault/localhost@EXAMPLE.COM")
        ```

        ### Full Configuration with All Options

        ```python
        import pulumi
        import pulumi_std as std
        import pulumi_vault as vault

        kerberos = vault.AuthBackend("kerberos",
            type="kerberos",
            path="kerberos")
        config = vault.KerberosAuthBackendConfig("config",
            mount=kerberos.path,
            keytab_wo=std.filebase64(input="/path/to/vault.keytab").result,
            keytab_wo_version=1,
            service_account="vault/localhost@EXAMPLE.COM",
            remove_instance_name=True,
            add_group_aliases=True)
        ```

        ### Using Namespace (Vault Enterprise)

        ```python
        import pulumi
        import pulumi_std as std
        import pulumi_vault as vault

        example = vault.Namespace("example", path="example-namespace")
        kerberos = vault.AuthBackend("kerberos",
            namespace=example.path,
            type="kerberos",
            path="kerberos")
        config = vault.KerberosAuthBackendConfig("config",
            namespace=example.path,
            mount=kerberos.path,
            keytab_wo=std.filebase64(input="/path/to/vault.keytab").result,
            keytab_wo_version=1,
            service_account="vault/localhost@EXAMPLE.COM")
        ```

        ## Import

        Kerberos auth backend configurations can be imported using the `path`, e.g.

        ```sh
        $ pulumi import vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig config auth/kerberos/config
        ```

        > **Note** The `keytab_wo` field cannot be imported as it is write-only and not stored
        in state. You will need to provide it in your configuration after import.

        ### Importing with Namespace (Vault Enterprise)

        For Vault Enterprise with namespaces, set the `TERRAFORM_VAULT_NAMESPACE_IMPORT` environment variable
        before importing:

        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.bool] add_group_aliases: Adds group aliases during authentication. When
               enabled, Vault will create entity aliases for each group the user belongs to.
               Defaults to `false`.
        :param pulumi.Input[_builtins.str] keytab_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               Base64-encoded keytab file content. This is a write-only
               field and is not stored in Terraform state. The keytab must contain an entry
               matching the `service_account`.
        :param pulumi.Input[_builtins.int] keytab_wo_version: Version identifier for keytab updates. Increment this
               value to trigger a keytab update in Vault. This allows you to rotate the keytab without
               forcing resource replacement.
        :param pulumi.Input[_builtins.str] mount: Path where the Kerberos auth method is mounted.
               Changing this will force a new resource to be created.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] remove_instance_name: Removes instance names from Kerberos service
               principal names during authentication. This can be useful when the instance name
               is not relevant for authentication. Defaults to `false`.
        :param pulumi.Input[_builtins.str] service_account: The Kerberos service account associated with the
               keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vault_svc`).
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: KerberosAuthBackendConfigArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manages the configuration of a Kerberos Auth Backend in Vault.

        This resource configures the Kerberos authentication method by providing the keytab
        and service account information required for Vault to authenticate users via Kerberos.

        For more information, see the
        [Vault docs](https://www.vaultproject.io/docs/auth/kerberos).

        > **Important** The `keytab_wo` field is write-only and is not stored in Terraform state.
        It is only sent to Vault when the `keytab_wo_version` changes or during initial creation.
        See the main provider documentation for more details.

        > **Note** Vault does not support deleting auth backend configurations via the API.
        When this resource is destroyed or replaced (e.g., when changing the `mount`), it is
        only removed from Terraform state. The configuration remains in Vault until the auth
        mount itself is deleted.

        ## Example Usage

        ### Basic Configuration

        ```python
        import pulumi
        import pulumi_std as std
        import pulumi_vault as vault

        kerberos = vault.AuthBackend("kerberos",
            type="kerberos",
            path="kerberos")
        config = vault.KerberosAuthBackendConfig("config",
            mount=kerberos.path,
            keytab_wo=std.filebase64(input="/path/to/vault.keytab").result,
            keytab_wo_version=1,
            service_account="vault/localhost@EXAMPLE.COM")
        ```

        ### Full Configuration with All Options

        ```python
        import pulumi
        import pulumi_std as std
        import pulumi_vault as vault

        kerberos = vault.AuthBackend("kerberos",
            type="kerberos",
            path="kerberos")
        config = vault.KerberosAuthBackendConfig("config",
            mount=kerberos.path,
            keytab_wo=std.filebase64(input="/path/to/vault.keytab").result,
            keytab_wo_version=1,
            service_account="vault/localhost@EXAMPLE.COM",
            remove_instance_name=True,
            add_group_aliases=True)
        ```

        ### Using Namespace (Vault Enterprise)

        ```python
        import pulumi
        import pulumi_std as std
        import pulumi_vault as vault

        example = vault.Namespace("example", path="example-namespace")
        kerberos = vault.AuthBackend("kerberos",
            namespace=example.path,
            type="kerberos",
            path="kerberos")
        config = vault.KerberosAuthBackendConfig("config",
            namespace=example.path,
            mount=kerberos.path,
            keytab_wo=std.filebase64(input="/path/to/vault.keytab").result,
            keytab_wo_version=1,
            service_account="vault/localhost@EXAMPLE.COM")
        ```

        ## Import

        Kerberos auth backend configurations can be imported using the `path`, e.g.

        ```sh
        $ pulumi import vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig config auth/kerberos/config
        ```

        > **Note** The `keytab_wo` field cannot be imported as it is write-only and not stored
        in state. You will need to provide it in your configuration after import.

        ### Importing with Namespace (Vault Enterprise)

        For Vault Enterprise with namespaces, set the `TERRAFORM_VAULT_NAMESPACE_IMPORT` environment variable
        before importing:

        :param str resource_name: The name of the resource.
        :param KerberosAuthBackendConfigArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(KerberosAuthBackendConfigArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 add_group_aliases: pulumi.Input[Optional[_builtins.bool]] = None,
                 keytab_wo: pulumi.Input[Optional[_builtins.str]] = None,
                 keytab_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
                 mount: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 remove_instance_name: pulumi.Input[Optional[_builtins.bool]] = None,
                 service_account: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = KerberosAuthBackendConfigArgs.__new__(KerberosAuthBackendConfigArgs)

            __props__.__dict__["add_group_aliases"] = add_group_aliases
            if keytab_wo is None and not opts.urn:
                raise TypeError("Missing required property 'keytab_wo'")
            __props__.__dict__["keytab_wo"] = None if keytab_wo is None else pulumi.Output.secret(keytab_wo)
            if keytab_wo_version is None and not opts.urn:
                raise TypeError("Missing required property 'keytab_wo_version'")
            __props__.__dict__["keytab_wo_version"] = keytab_wo_version
            if mount is None and not opts.urn:
                raise TypeError("Missing required property 'mount'")
            __props__.__dict__["mount"] = mount
            __props__.__dict__["namespace"] = namespace
            __props__.__dict__["remove_instance_name"] = remove_instance_name
            if service_account is None and not opts.urn:
                raise TypeError("Missing required property 'service_account'")
            __props__.__dict__["service_account"] = service_account
        secret_opts = pulumi.ResourceOptions(additional_secret_outputs=["keytabWo"])
        opts = pulumi.ResourceOptions.merge(opts, secret_opts)
        super(KerberosAuthBackendConfig, __self__).__init__(
            'vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            add_group_aliases: pulumi.Input[Optional[_builtins.bool]] = None,
            keytab_wo: pulumi.Input[Optional[_builtins.str]] = None,
            keytab_wo_version: pulumi.Input[Optional[_builtins.int]] = None,
            mount: pulumi.Input[Optional[_builtins.str]] = None,
            namespace: pulumi.Input[Optional[_builtins.str]] = None,
            remove_instance_name: pulumi.Input[Optional[_builtins.bool]] = None,
            service_account: pulumi.Input[Optional[_builtins.str]] = None) -> 'KerberosAuthBackendConfig':
        """
        Get an existing KerberosAuthBackendConfig resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.bool] add_group_aliases: Adds group aliases during authentication. When
               enabled, Vault will create entity aliases for each group the user belongs to.
               Defaults to `false`.
        :param pulumi.Input[_builtins.str] keytab_wo: **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
               Base64-encoded keytab file content. This is a write-only
               field and is not stored in Terraform state. The keytab must contain an entry
               matching the `service_account`.
        :param pulumi.Input[_builtins.int] keytab_wo_version: Version identifier for keytab updates. Increment this
               value to trigger a keytab update in Vault. This allows you to rotate the keytab without
               forcing resource replacement.
        :param pulumi.Input[_builtins.str] mount: Path where the Kerberos auth method is mounted.
               Changing this will force a new resource to be created.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] remove_instance_name: Removes instance names from Kerberos service
               principal names during authentication. This can be useful when the instance name
               is not relevant for authentication. Defaults to `false`.
        :param pulumi.Input[_builtins.str] service_account: The Kerberos service account associated with the
               keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vault_svc`).
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _KerberosAuthBackendConfigState.__new__(_KerberosAuthBackendConfigState)

        __props__.__dict__["add_group_aliases"] = add_group_aliases
        __props__.__dict__["keytab_wo"] = keytab_wo
        __props__.__dict__["keytab_wo_version"] = keytab_wo_version
        __props__.__dict__["mount"] = mount
        __props__.__dict__["namespace"] = namespace
        __props__.__dict__["remove_instance_name"] = remove_instance_name
        __props__.__dict__["service_account"] = service_account
        return KerberosAuthBackendConfig(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="addGroupAliases")
    def add_group_aliases(self) -> pulumi.Output[Optional[_builtins.bool]]:
        """
        Adds group aliases during authentication. When
        enabled, Vault will create entity aliases for each group the user belongs to.
        Defaults to `false`.
        """
        return pulumi.get(self, "add_group_aliases")

    @_builtins.property
    @pulumi.getter(name="keytabWo")
    def keytab_wo(self) -> pulumi.Output[_builtins.str]:
        """
        **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
        Base64-encoded keytab file content. This is a write-only
        field and is not stored in Terraform state. The keytab must contain an entry
        matching the `service_account`.
        """
        return pulumi.get(self, "keytab_wo")

    @_builtins.property
    @pulumi.getter(name="keytabWoVersion")
    def keytab_wo_version(self) -> pulumi.Output[_builtins.int]:
        """
        Version identifier for keytab updates. Increment this
        value to trigger a keytab update in Vault. This allows you to rotate the keytab without
        forcing resource replacement.
        """
        return pulumi.get(self, "keytab_wo_version")

    @_builtins.property
    @pulumi.getter
    def mount(self) -> pulumi.Output[_builtins.str]:
        """
        Path where the Kerberos auth method is mounted.
        Changing this will force a new resource to be created.
        """
        return pulumi.get(self, "mount")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        The namespace to provision the resource in.
        The value should not contain leading or trailing forward slashes.
        The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @_builtins.property
    @pulumi.getter(name="removeInstanceName")
    def remove_instance_name(self) -> pulumi.Output[Optional[_builtins.bool]]:
        """
        Removes instance names from Kerberos service
        principal names during authentication. This can be useful when the instance name
        is not relevant for authentication. Defaults to `false`.
        """
        return pulumi.get(self, "remove_instance_name")

    @_builtins.property
    @pulumi.getter(name="serviceAccount")
    def service_account(self) -> pulumi.Output[_builtins.str]:
        """
        The Kerberos service account associated with the
        keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vault_svc`).
        """
        return pulumi.get(self, "service_account")


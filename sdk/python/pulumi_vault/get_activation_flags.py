# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = [
    'GetActivationFlagsResult',
    'AwaitableGetActivationFlagsResult',
    'get_activation_flags',
    'get_activation_flags_output',
]

@pulumi.output_type
class GetActivationFlagsResult:
    """
    A collection of values returned by getActivationFlags.
    """
    def __init__(__self__, activated_flags=None, id=None, unactivated_flags=None):
        if activated_flags and not isinstance(activated_flags, list):
            raise TypeError("Expected argument 'activated_flags' to be a list")
        pulumi.set(__self__, "activated_flags", activated_flags)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if unactivated_flags and not isinstance(unactivated_flags, list):
            raise TypeError("Expected argument 'unactivated_flags' to be a list")
        pulumi.set(__self__, "unactivated_flags", unactivated_flags)

    @_builtins.property
    @pulumi.getter(name="activatedFlags")
    def activated_flags(self) -> Sequence[_builtins.str]:
        """
        Set of currently activated feature flag keys.
        """
        return pulumi.get(self, "activated_flags")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Unique identifier for this data source. This value is always `sys/activation-flags`.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter(name="unactivatedFlags")
    def unactivated_flags(self) -> Sequence[_builtins.str]:
        """
        Set of currently unactivated feature flag keys.
        """
        return pulumi.get(self, "unactivated_flags")


class AwaitableGetActivationFlagsResult(GetActivationFlagsResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetActivationFlagsResult(
            activated_flags=self.activated_flags,
            id=self.id,
            unactivated_flags=self.unactivated_flags)


def get_activation_flags(opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetActivationFlagsResult:
    """
    Reads activation flags from Vault.

    > **Important** Activation flags require Vault 1.16 or later.

    > **Important** Activation flags are available only in Vault Enterprise and are exposed through a singleton system endpoint.

    > **Important** The activation flags endpoint is root-namespace-only. This data source does not accept a `namespace` argument.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_vault as vault

    current = vault.get_activation_flags()
    ```

    ## Notes

    * Activation flag names are returned exactly as reported by Vault.
    * This data source reads from `GET /sys/activation-flags`.
    * The provider returns both `activated_flags` and `unactivated_flags` as Terraform sets.
    """
    __args__ = dict()
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('vault:index/getActivationFlags:getActivationFlags', __args__, opts=opts, typ=GetActivationFlagsResult).value

    return AwaitableGetActivationFlagsResult(
        activated_flags=pulumi.get(__ret__, 'activated_flags'),
        id=pulumi.get(__ret__, 'id'),
        unactivated_flags=pulumi.get(__ret__, 'unactivated_flags'))
def get_activation_flags_output(opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetActivationFlagsResult]:
    """
    Reads activation flags from Vault.

    > **Important** Activation flags require Vault 1.16 or later.

    > **Important** Activation flags are available only in Vault Enterprise and are exposed through a singleton system endpoint.

    > **Important** The activation flags endpoint is root-namespace-only. This data source does not accept a `namespace` argument.

    ## Example Usage

    ```python
    import pulumi
    import pulumi_vault as vault

    current = vault.get_activation_flags()
    ```

    ## Notes

    * Activation flag names are returned exactly as reported by Vault.
    * This data source reads from `GET /sys/activation-flags`.
    * The provider returns both `activated_flags` and `unactivated_flags` as Terraform sets.
    """
    __args__ = dict()
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('vault:index/getActivationFlags:getActivationFlags', __args__, opts=opts, typ=GetActivationFlagsResult)
    return __ret__.apply(lambda __response__: GetActivationFlagsResult(
        activated_flags=pulumi.get(__response__, 'activated_flags'),
        id=pulumi.get(__response__, 'id'),
        unactivated_flags=pulumi.get(__response__, 'unactivated_flags')))

# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

__all__ = ['GroupPolicyApplicationArgs', 'GroupPolicyApplication']

@pulumi.input_type
class GroupPolicyApplicationArgs:
    def __init__(__self__, *,
                 group_policy_application_mode: pulumi.Input[_builtins.str],
                 namespace: pulumi.Input[Optional[_builtins.str]] = None):
        """
        The set of arguments for constructing a GroupPolicyApplication resource.

        :param pulumi.Input[_builtins.str] group_policy_application_mode: Mode for group policy application. Must be either `within_namespace_hierarchy` or `any`. Defaults to `within_namespace_hierarchy`.
        :param pulumi.Input[_builtins.str] namespace: Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
               Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
               *Available only for Vault Enterprise*.
        """
        pulumi.set(__self__, "group_policy_application_mode", group_policy_application_mode)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter(name="groupPolicyApplicationMode")
    def group_policy_application_mode(self) -> pulumi.Input[_builtins.str]:
        """
        Mode for group policy application. Must be either `within_namespace_hierarchy` or `any`. Defaults to `within_namespace_hierarchy`.
        """
        return pulumi.get(self, "group_policy_application_mode")

    @group_policy_application_mode.setter
    def group_policy_application_mode(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "group_policy_application_mode", value)

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
        Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @namespace.setter
    def namespace(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "namespace", value)


@pulumi.input_type
class _GroupPolicyApplicationState:
    def __init__(__self__, *,
                 group_policy_application_mode: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering GroupPolicyApplication resources.

        :param pulumi.Input[_builtins.str] group_policy_application_mode: Mode for group policy application. Must be either `within_namespace_hierarchy` or `any`. Defaults to `within_namespace_hierarchy`.
        :param pulumi.Input[_builtins.str] namespace: Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
               Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
               *Available only for Vault Enterprise*.
        """
        if group_policy_application_mode is not None:
            pulumi.set(__self__, "group_policy_application_mode", group_policy_application_mode)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)

    @_builtins.property
    @pulumi.getter(name="groupPolicyApplicationMode")
    def group_policy_application_mode(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Mode for group policy application. Must be either `within_namespace_hierarchy` or `any`. Defaults to `within_namespace_hierarchy`.
        """
        return pulumi.get(self, "group_policy_application_mode")

    @group_policy_application_mode.setter
    def group_policy_application_mode(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "group_policy_application_mode", value)

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
        Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @namespace.setter
    def namespace(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "namespace", value)


@pulumi.type_token("vault:config/groupPolicyApplication:GroupPolicyApplication")
class GroupPolicyApplication(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 group_policy_application_mode: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        """
        Manages the global group policy application mode for Vault Enterprise. This resource controls how policies attached to identity groups are applied across namespace boundaries.

        **Important:** This is a singleton resource - only one instance can exist per Vault cluster. The resource must be managed from the root or administrative namespace.

        **Note** this feature is available only with Vault Enterprise 1.13.8+.

        ## Example Usage

        ### Basic Usage (Root Namespace)

        ```python
        import pulumi
        import pulumi_vault as vault

        # Configure group policy application mode in root namespace
        # Using the default mode
        test = vault.config.GroupPolicyApplication("test", group_policy_application_mode="within_namespace_hierarchy")
        ```

        ### Using "any" Mode

        ```python
        import pulumi
        import pulumi_vault as vault

        config = vault.config.GroupPolicyApplication("config", group_policy_application_mode="any")
        ```

        ### Explicit Root Namespace

        ```python
        import pulumi
        import pulumi_vault as vault

        config = vault.config.GroupPolicyApplication("config", group_policy_application_mode="within_namespace_hierarchy")
        ```

        ### Administrative Namespace

        When working with an administrative namespace, you can specify it directly in the resource:

        ```python
        import pulumi
        import pulumi_vault as vault

        config = vault.config.GroupPolicyApplication("config",
            group_policy_application_mode="within_namespace_hierarchy",
            namespace="admin")
        ```

        ## Behavior Notes

        ### Singleton Resource

        This resource is a singleton, meaning only one instance can exist per Vault cluster. Multiple `config.GroupPolicyApplication` resources in your Terraform configuration will conflict.

        ### Deletion Behavior

        When this resource is destroyed via `terraform destroy`, it does not delete the configuration from Vault. Instead, it resets the mode to the default value `within_namespace_hierarchy`. This ensures that Vault always has a valid group policy application mode configured.

        ### Namespace Requirements

        This resource can only be managed from:
        - The root namespace
        - The administrative namespace (`"admin"`)

        Attempting to manage this resource from any other namespace will result in an error.

        ### Policy Application Scope

        **Important:** The group policy application mode only applies to ACL policies and no longer affects Sentinel RGPs for Vault ≥ 1.13.8, 1.14.4, 1.15.0.

        ### Replication Behavior

        This configuration will be replicated between primary and secondary clusters. Primaries cannot have a different mode than secondaries.

        ## Version Requirements

        - Requires Vault Enterprise 1.15.0 or later (TFVP support)

        ## Import

        The group policy application configuration can be imported using the path `sys/config/group-policy-application`:

        ```sh
        $ pulumi import vault:config/groupPolicyApplication:GroupPolicyApplication config sys/config/group-policy-application
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] group_policy_application_mode: Mode for group policy application. Must be either `within_namespace_hierarchy` or `any`. Defaults to `within_namespace_hierarchy`.
        :param pulumi.Input[_builtins.str] namespace: Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
               Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
               *Available only for Vault Enterprise*.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: GroupPolicyApplicationArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manages the global group policy application mode for Vault Enterprise. This resource controls how policies attached to identity groups are applied across namespace boundaries.

        **Important:** This is a singleton resource - only one instance can exist per Vault cluster. The resource must be managed from the root or administrative namespace.

        **Note** this feature is available only with Vault Enterprise 1.13.8+.

        ## Example Usage

        ### Basic Usage (Root Namespace)

        ```python
        import pulumi
        import pulumi_vault as vault

        # Configure group policy application mode in root namespace
        # Using the default mode
        test = vault.config.GroupPolicyApplication("test", group_policy_application_mode="within_namespace_hierarchy")
        ```

        ### Using "any" Mode

        ```python
        import pulumi
        import pulumi_vault as vault

        config = vault.config.GroupPolicyApplication("config", group_policy_application_mode="any")
        ```

        ### Explicit Root Namespace

        ```python
        import pulumi
        import pulumi_vault as vault

        config = vault.config.GroupPolicyApplication("config", group_policy_application_mode="within_namespace_hierarchy")
        ```

        ### Administrative Namespace

        When working with an administrative namespace, you can specify it directly in the resource:

        ```python
        import pulumi
        import pulumi_vault as vault

        config = vault.config.GroupPolicyApplication("config",
            group_policy_application_mode="within_namespace_hierarchy",
            namespace="admin")
        ```

        ## Behavior Notes

        ### Singleton Resource

        This resource is a singleton, meaning only one instance can exist per Vault cluster. Multiple `config.GroupPolicyApplication` resources in your Terraform configuration will conflict.

        ### Deletion Behavior

        When this resource is destroyed via `terraform destroy`, it does not delete the configuration from Vault. Instead, it resets the mode to the default value `within_namespace_hierarchy`. This ensures that Vault always has a valid group policy application mode configured.

        ### Namespace Requirements

        This resource can only be managed from:
        - The root namespace
        - The administrative namespace (`"admin"`)

        Attempting to manage this resource from any other namespace will result in an error.

        ### Policy Application Scope

        **Important:** The group policy application mode only applies to ACL policies and no longer affects Sentinel RGPs for Vault ≥ 1.13.8, 1.14.4, 1.15.0.

        ### Replication Behavior

        This configuration will be replicated between primary and secondary clusters. Primaries cannot have a different mode than secondaries.

        ## Version Requirements

        - Requires Vault Enterprise 1.15.0 or later (TFVP support)

        ## Import

        The group policy application configuration can be imported using the path `sys/config/group-policy-application`:

        ```sh
        $ pulumi import vault:config/groupPolicyApplication:GroupPolicyApplication config sys/config/group-policy-application
        ```


        :param str resource_name: The name of the resource.
        :param GroupPolicyApplicationArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(GroupPolicyApplicationArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 group_policy_application_mode: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = GroupPolicyApplicationArgs.__new__(GroupPolicyApplicationArgs)

            if group_policy_application_mode is None and not opts.urn:
                raise TypeError("Missing required property 'group_policy_application_mode'")
            __props__.__dict__["group_policy_application_mode"] = group_policy_application_mode
            __props__.__dict__["namespace"] = namespace
        super(GroupPolicyApplication, __self__).__init__(
            'vault:config/groupPolicyApplication:GroupPolicyApplication',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            group_policy_application_mode: pulumi.Input[Optional[_builtins.str]] = None,
            namespace: pulumi.Input[Optional[_builtins.str]] = None) -> 'GroupPolicyApplication':
        """
        Get an existing GroupPolicyApplication resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] group_policy_application_mode: Mode for group policy application. Must be either `within_namespace_hierarchy` or `any`. Defaults to `within_namespace_hierarchy`.
        :param pulumi.Input[_builtins.str] namespace: Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
               Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
               *Available only for Vault Enterprise*.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _GroupPolicyApplicationState.__new__(_GroupPolicyApplicationState)

        __props__.__dict__["group_policy_application_mode"] = group_policy_application_mode
        __props__.__dict__["namespace"] = namespace
        return GroupPolicyApplication(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="groupPolicyApplicationMode")
    def group_policy_application_mode(self) -> pulumi.Output[_builtins.str]:
        """
        Mode for group policy application. Must be either `within_namespace_hierarchy` or `any`. Defaults to `within_namespace_hierarchy`.
        """
        return pulumi.get(self, "group_policy_application_mode")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
        Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")


# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

__all__ = ['UiHeaderArgs', 'UiHeader']

@pulumi.input_type
class UiHeaderArgs:
    def __init__(__self__, *,
                 values: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]],
                 name: pulumi.Input[Optional[_builtins.str]] = None):
        """
        The set of arguments for constructing a UiHeader resource.

        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] values: A set of values for the header. At least one value is required.
               Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
               
               > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
        :param pulumi.Input[_builtins.str] name: The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
               Changing this will recreate the resource.
        """
        pulumi.set(__self__, "values", values)
        if name is not None:
            pulumi.set(__self__, "name", name)

    @_builtins.property
    @pulumi.getter
    def values(self) -> pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]:
        """
        A set of values for the header. At least one value is required.
        Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.

        > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
        """
        return pulumi.get(self, "values")

    @values.setter
    def values(self, value: pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]):
        pulumi.set(self, "values", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
        Changing this will recreate the resource.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)


@pulumi.input_type
class _UiHeaderState:
    def __init__(__self__, *,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 values: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None):
        """
        Input properties used for looking up and filtering UiHeader resources.

        :param pulumi.Input[_builtins.str] name: The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
               Changing this will recreate the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] values: A set of values for the header. At least one value is required.
               Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
               
               > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
        """
        if name is not None:
            pulumi.set(__self__, "name", name)
        if values is not None:
            pulumi.set(__self__, "values", values)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
        Changing this will recreate the resource.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def values(self) -> pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        A set of values for the header. At least one value is required.
        Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.

        > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
        """
        return pulumi.get(self, "values")

    @values.setter
    def values(self, value: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "values", value)


@pulumi.type_token("vault:config/uiHeader:UiHeader")
class UiHeader(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 values: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        """
        Manages custom HTTP headers for the Vault UI. This resource allows you to configure
        custom HTTP response headers that will be sent by the Vault UI, enabling security
        policies, CORS configuration, and custom organizational headers.

        > **Important** This resource requires **Vault 1.16.0 or later**. The `sys/config/ui/headers` API endpoint was introduced in Vault 1.16.0.

        > **Important** All operations on this resource require the `sudo` capability on the
        `sys/config/ui/headers/*` path.

        ## Example Usage

        ### Basic Header Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        custom = vault.config.UiHeader("custom",
            name="X-Custom-Header",
            values=["custom-value"])
        ```

        ### Security Headers

        ```python
        import pulumi
        import pulumi_vault as vault

        # Content Security Policy
        csp = vault.config.UiHeader("csp",
            name="Content-Security-Policy",
            values=["default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'"])
        # X-Frame-Options
        frame_options = vault.config.UiHeader("frame_options",
            name="X-Frame-Options",
            values=["DENY"])
        # Strict Transport Security
        hsts = vault.config.UiHeader("hsts",
            name="Strict-Transport-Security",
            values=["max-age=31536000; includeSubDomains; preload"])
        # X-Content-Type-Options
        content_type_options = vault.config.UiHeader("content_type_options",
            name="X-Content-Type-Options",
            values=["nosniff"])
        ```

        ### CORS Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        cors_origin = vault.config.UiHeader("cors_origin",
            name="Access-Control-Allow-Origin",
            values=[
                "https://example.com",
                "https://app.example.com",
            ])
        cors_methods = vault.config.UiHeader("cors_methods",
            name="Access-Control-Allow-Methods",
            values=[
                "GET",
                "POST",
                "OPTIONS",
            ])
        cors_headers = vault.config.UiHeader("cors_headers",
            name="Access-Control-Allow-Headers",
            values=[
                "Content-Type",
                "Authorization",
            ])
        ```

        ### Multiple Values

        ```python
        import pulumi
        import pulumi_vault as vault

        multi_value = vault.config.UiHeader("multi_value",
            name="X-Multi-Value-Header",
            values=[
                "value1",
                "value2",
                "value3",
            ])
        ```

        ## Migration from generic.Endpoint

        If you're currently managing UI headers using `generic.Endpoint`, you can migrate
        to this dedicated resource:

        ### Before (using generic_endpoint)

        ```python
        import pulumi
        import json
        import pulumi_vault as vault

        csp_header = vault.generic.Endpoint("csp_header",
            path="sys/config/ui/headers/Content-Security-Policy",
            data_json=json.dumps({
                "values": ["default-src 'self'"],
            }))
        ```

        ### After (using dedicated resource)

        ```python
        import pulumi
        import pulumi_vault as vault

        csp = vault.config.UiHeader("csp",
            name="Content-Security-Policy",
            values=["default-src 'self'"])
        ```

        ### Migration Steps

        1. Add the new `config.UiHeader` resource to your configuration
        2. Import the existing header: `terraform import vault_config_ui_header.csp Content-Security-Policy`
        3. Remove the old `generic.Endpoint` resource from your configuration
        4. Run `pulumi preview` to verify no changes are required

        ## Important Notes

        ### State Management

        This resource follows Terraform best practices by reading the actual configuration from Vault
        after create and update operations. This ensures that the Terraform state always reflects the
        actual state in Vault, including any server-side processing or normalization of values.

        ## Import

        UI header configurations can be imported using the header `name`, e.g.

        ```sh
        $ pulumi import vault:config/uiHeader:UiHeader csp Content-Security-Policy
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] name: The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
               Changing this will recreate the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] values: A set of values for the header. At least one value is required.
               Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
               
               > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: UiHeaderArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manages custom HTTP headers for the Vault UI. This resource allows you to configure
        custom HTTP response headers that will be sent by the Vault UI, enabling security
        policies, CORS configuration, and custom organizational headers.

        > **Important** This resource requires **Vault 1.16.0 or later**. The `sys/config/ui/headers` API endpoint was introduced in Vault 1.16.0.

        > **Important** All operations on this resource require the `sudo` capability on the
        `sys/config/ui/headers/*` path.

        ## Example Usage

        ### Basic Header Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        custom = vault.config.UiHeader("custom",
            name="X-Custom-Header",
            values=["custom-value"])
        ```

        ### Security Headers

        ```python
        import pulumi
        import pulumi_vault as vault

        # Content Security Policy
        csp = vault.config.UiHeader("csp",
            name="Content-Security-Policy",
            values=["default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'"])
        # X-Frame-Options
        frame_options = vault.config.UiHeader("frame_options",
            name="X-Frame-Options",
            values=["DENY"])
        # Strict Transport Security
        hsts = vault.config.UiHeader("hsts",
            name="Strict-Transport-Security",
            values=["max-age=31536000; includeSubDomains; preload"])
        # X-Content-Type-Options
        content_type_options = vault.config.UiHeader("content_type_options",
            name="X-Content-Type-Options",
            values=["nosniff"])
        ```

        ### CORS Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        cors_origin = vault.config.UiHeader("cors_origin",
            name="Access-Control-Allow-Origin",
            values=[
                "https://example.com",
                "https://app.example.com",
            ])
        cors_methods = vault.config.UiHeader("cors_methods",
            name="Access-Control-Allow-Methods",
            values=[
                "GET",
                "POST",
                "OPTIONS",
            ])
        cors_headers = vault.config.UiHeader("cors_headers",
            name="Access-Control-Allow-Headers",
            values=[
                "Content-Type",
                "Authorization",
            ])
        ```

        ### Multiple Values

        ```python
        import pulumi
        import pulumi_vault as vault

        multi_value = vault.config.UiHeader("multi_value",
            name="X-Multi-Value-Header",
            values=[
                "value1",
                "value2",
                "value3",
            ])
        ```

        ## Migration from generic.Endpoint

        If you're currently managing UI headers using `generic.Endpoint`, you can migrate
        to this dedicated resource:

        ### Before (using generic_endpoint)

        ```python
        import pulumi
        import json
        import pulumi_vault as vault

        csp_header = vault.generic.Endpoint("csp_header",
            path="sys/config/ui/headers/Content-Security-Policy",
            data_json=json.dumps({
                "values": ["default-src 'self'"],
            }))
        ```

        ### After (using dedicated resource)

        ```python
        import pulumi
        import pulumi_vault as vault

        csp = vault.config.UiHeader("csp",
            name="Content-Security-Policy",
            values=["default-src 'self'"])
        ```

        ### Migration Steps

        1. Add the new `config.UiHeader` resource to your configuration
        2. Import the existing header: `terraform import vault_config_ui_header.csp Content-Security-Policy`
        3. Remove the old `generic.Endpoint` resource from your configuration
        4. Run `pulumi preview` to verify no changes are required

        ## Important Notes

        ### State Management

        This resource follows Terraform best practices by reading the actual configuration from Vault
        after create and update operations. This ensures that the Terraform state always reflects the
        actual state in Vault, including any server-side processing or normalization of values.

        ## Import

        UI header configurations can be imported using the header `name`, e.g.

        ```sh
        $ pulumi import vault:config/uiHeader:UiHeader csp Content-Security-Policy
        ```


        :param str resource_name: The name of the resource.
        :param UiHeaderArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(UiHeaderArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 name: pulumi.Input[Optional[_builtins.str]] = None,
                 values: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = UiHeaderArgs.__new__(UiHeaderArgs)

            __props__.__dict__["name"] = name
            if values is None and not opts.urn:
                raise TypeError("Missing required property 'values'")
            __props__.__dict__["values"] = values
        super(UiHeader, __self__).__init__(
            'vault:config/uiHeader:UiHeader',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            name: pulumi.Input[Optional[_builtins.str]] = None,
            values: pulumi.Input[Optional[Sequence[pulumi.Input[_builtins.str]]]] = None) -> 'UiHeader':
        """
        Get an existing UiHeader resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] name: The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
               Changing this will recreate the resource.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] values: A set of values for the header. At least one value is required.
               Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
               
               > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _UiHeaderState.__new__(_UiHeaderState)

        __props__.__dict__["name"] = name
        __props__.__dict__["values"] = values
        return UiHeader(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
        Changing this will recreate the resource.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def values(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        A set of values for the header. At least one value is required.
        Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.

        > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
        """
        return pulumi.get(self, "values")


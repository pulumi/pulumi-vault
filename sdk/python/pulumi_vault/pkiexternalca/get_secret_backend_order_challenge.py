# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

__all__ = [
    'GetSecretBackendOrderChallengeResult',
    'AwaitableGetSecretBackendOrderChallengeResult',
    'get_secret_backend_order_challenge',
    'get_secret_backend_order_challenge_output',
]

@pulumi.output_type
class GetSecretBackendOrderChallengeResult:
    """
    A collection of values returned by getSecretBackendOrderChallenge.
    """
    def __init__(__self__, challenge_type=None, expires=None, id=None, identifier=None, key_authorization=None, mount=None, namespace=None, order_id=None, role_name=None, status=None, token=None):
        if challenge_type and not isinstance(challenge_type, str):
            raise TypeError("Expected argument 'challenge_type' to be a str")
        pulumi.set(__self__, "challenge_type", challenge_type)
        if expires and not isinstance(expires, str):
            raise TypeError("Expected argument 'expires' to be a str")
        pulumi.set(__self__, "expires", expires)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if identifier and not isinstance(identifier, str):
            raise TypeError("Expected argument 'identifier' to be a str")
        pulumi.set(__self__, "identifier", identifier)
        if key_authorization and not isinstance(key_authorization, str):
            raise TypeError("Expected argument 'key_authorization' to be a str")
        pulumi.set(__self__, "key_authorization", key_authorization)
        if mount and not isinstance(mount, str):
            raise TypeError("Expected argument 'mount' to be a str")
        pulumi.set(__self__, "mount", mount)
        if namespace and not isinstance(namespace, str):
            raise TypeError("Expected argument 'namespace' to be a str")
        pulumi.set(__self__, "namespace", namespace)
        if order_id and not isinstance(order_id, str):
            raise TypeError("Expected argument 'order_id' to be a str")
        pulumi.set(__self__, "order_id", order_id)
        if role_name and not isinstance(role_name, str):
            raise TypeError("Expected argument 'role_name' to be a str")
        pulumi.set(__self__, "role_name", role_name)
        if status and not isinstance(status, str):
            raise TypeError("Expected argument 'status' to be a str")
        pulumi.set(__self__, "status", status)
        if token and not isinstance(token, str):
            raise TypeError("Expected argument 'token' to be a str")
        pulumi.set(__self__, "token", token)

    @_builtins.property
    @pulumi.getter(name="challengeType")
    def challenge_type(self) -> _builtins.str:
        return pulumi.get(self, "challenge_type")

    @_builtins.property
    @pulumi.getter
    def expires(self) -> _builtins.str:
        """
        Expiry time for the challenge in RFC3339 format.
        """
        return pulumi.get(self, "expires")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        Unique identifier for this data source read.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter
    def identifier(self) -> _builtins.str:
        return pulumi.get(self, "identifier")

    @_builtins.property
    @pulumi.getter(name="keyAuthorization")
    def key_authorization(self) -> _builtins.str:
        """
        The key authorization string for the challenge. This is the value that must be:
        - Served at the challenge URL for HTTP-01
        - Set as the TXT record value for DNS-01
        - Used in the TLS certificate for TLS-ALPN-01
        """
        return pulumi.get(self, "key_authorization")

    @_builtins.property
    @pulumi.getter
    def mount(self) -> _builtins.str:
        return pulumi.get(self, "mount")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> Optional[_builtins.str]:
        return pulumi.get(self, "namespace")

    @_builtins.property
    @pulumi.getter(name="orderId")
    def order_id(self) -> _builtins.str:
        return pulumi.get(self, "order_id")

    @_builtins.property
    @pulumi.getter(name="roleName")
    def role_name(self) -> _builtins.str:
        return pulumi.get(self, "role_name")

    @_builtins.property
    @pulumi.getter
    def status(self) -> _builtins.str:
        """
        The current status of the challenge (e.g., `pending`, `valid`, `invalid`).
        """
        return pulumi.get(self, "status")

    @_builtins.property
    @pulumi.getter
    def token(self) -> _builtins.str:
        """
        The challenge token provided by the ACME server. For HTTP-01 challenges, this is used in the URL path.
        """
        return pulumi.get(self, "token")


class AwaitableGetSecretBackendOrderChallengeResult(GetSecretBackendOrderChallengeResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetSecretBackendOrderChallengeResult(
            challenge_type=self.challenge_type,
            expires=self.expires,
            id=self.id,
            identifier=self.identifier,
            key_authorization=self.key_authorization,
            mount=self.mount,
            namespace=self.namespace,
            order_id=self.order_id,
            role_name=self.role_name,
            status=self.status,
            token=self.token)


def get_secret_backend_order_challenge(challenge_type: Optional[_builtins.str] = None,
                                       identifier: Optional[_builtins.str] = None,
                                       mount: Optional[_builtins.str] = None,
                                       namespace: Optional[_builtins.str] = None,
                                       order_id: Optional[_builtins.str] = None,
                                       role_name: Optional[_builtins.str] = None,
                                       opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetSecretBackendOrderChallengeResult:
    """
    Retrieves ACME challenge details for a specific identifier in an order. This data source provides the information needed to fulfill ACME challenges (HTTP-01, DNS-01, or TLS-ALPN-01) for domain validation.

    > **Note** This data source will poll the order status until it reaches the `awaiting-challenge-fulfillment` or `completed` state. Use this data source after creating an order with `pkiexternalca.SecretBackendOrder`.

    ## Example Usage

    ### With HTTP-01 Challenge

    ```python
    import pulumi
    import pulumi_local as local
    import pulumi_vault as vault

    pki_external_ca = vault.Mount("pki-external-ca",
        path="pki-external-ca",
        type="pki-external-ca")
    example = vault.pkiexternalca.SecretBackendAcmeAccount("example",
        mount=pki_external_ca.path,
        name="my-acme-account",
        directory_url="https://acme-v02.api.letsencrypt.org/directory",
        email_contacts=["admin@example.com"])
    example_secret_backend_role = vault.pkiexternalca.SecretBackendRole("example",
        mount=pki_external_ca.path,
        name="example-role",
        acme_account_name=example.name,
        allowed_domains=["example.com"],
        allowed_domain_options=[
            "bare_domains",
            "subdomains",
        ],
        allowed_challenge_types=["http-01"])
    example_secret_backend_order = vault.pkiexternalca.SecretBackendOrder("example",
        mount=pki_external_ca.path,
        role_name=example_secret_backend_role.name,
        identifiers=["www.example.com"])
    # Retrieve HTTP-01 challenge details
    http = vault.pkiexternalca.get_secret_backend_order_challenge_output(mount=pki_external_ca.path,
        role_name=example_secret_backend_role.name,
        order_id=example_secret_backend_order.order_id,
        challenge_type="http-01",
        identifier="www.example.com")
    # Deploy the challenge file
    acme_challenge = local.File("acme_challenge",
        filename=f/var/www/html/.well-known/acme-challenge/{http.token},
        content=http.key_authorization)
    pulumi.export("challengeToken", http.token)
    pulumi.export("challengeUrl", http.apply(lambda http: f"http://www.example.com/.well-known/acme-challenge/{http.token}"))
    ```

    ### With DNS-01 Challenge

    ```python
    import pulumi
    import pulumi_aws as aws
    import pulumi_vault as vault

    # Retrieve DNS-01 challenge details
    dns = vault.pkiexternalca.get_secret_backend_order_challenge(mount=pki_external_ca["path"],
        role_name=example_vault_pki_external_ca_secret_backend_role["name"],
        order_id=example["orderId"],
        challenge_type="dns-01",
        identifier="www.example.com")
    # Create DNS TXT record using AWS Route53
    acme_challenge = aws.Route53Record("acme_challenge",
        zone_id=example_aws_route53_zone.zone_id,
        name=_acme-challenge.www.example.com,
        type=TXT,
        ttl=60,
        records=[dns.key_authorization])
    pulumi.export("dnsRecordName", "_acme-challenge.www.example.com")
    pulumi.export("dnsRecordValue", dns.key_authorization)
    ```

    ### With TLS-ALPN-01 Challenge

    ```python
    import pulumi
    import pulumi_vault as vault

    # Retrieve TLS-ALPN-01 challenge details
    tls_alpn = vault.pkiexternalca.get_secret_backend_order_challenge(mount=pki_external_ca["path"],
        role_name=example_vault_pki_external_ca_secret_backend_role["name"],
        order_id=example["orderId"],
        challenge_type="tls-alpn-01",
        identifier="www.example.com")
    pulumi.export("tlsAlpnKeyAuth", tls_alpn.key_authorization)
    ```

    ## Challenge Type Details

    ### HTTP-01 Challenge

    For HTTP-01 challenges, you must serve the `key_authorization` value at:

    The response must:
    - Return HTTP 200 status
    - Have Content-Type: `text/plain` or no Content-Type header
    - Contain only the `key_authorization` value

    ### DNS-01 Challenge

    For DNS-01 challenges, you must create a TXT record at:

    The TXT record value must be the `key_authorization` value. DNS propagation may take time, so ensure the record is resolvable before marking the challenge as fulfilled.

    ### TLS-ALPN-01 Challenge

    For TLS-ALPN-01 challenges, you must configure your TLS server to:
    - Present a self-signed certificate for the identifier
    - Include the `acme-tls/1` ALPN protocol
    - Include a specific extension with the `key_authorization` value

    This challenge type is more complex and typically requires custom TLS server configuration.

    > **Note** This data source polls the order status with a maximum of 5 attempts at 2-second intervals. If the order does not reach the appropriate state within this time, the data source read will fail.


    :param _builtins.str challenge_type: The type of ACME challenge to retrieve. Valid values are:
           - `http-01` - HTTP challenge (requires placing a file at a specific URL)
           - `dns-01` - DNS challenge (requires creating a TXT record)
           - `tls-alpn-01` - TLS-ALPN challenge (requires configuring TLS with specific ALPN extension)
    :param _builtins.str identifier: The identifier (domain name) for which to retrieve the challenge.
    :param _builtins.str mount: The path where the PKI External CA secret backend is mounted.
    :param _builtins.str namespace: The namespace to provision the resource in.
           The value should not contain leading or trailing forward slashes.
           The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
           *Available only for Vault Enterprise*.
    :param _builtins.str order_id: The unique identifier for the ACME order.
    :param _builtins.str role_name: Name of the role associated with the order.
    """
    __args__ = dict()
    __args__['challengeType'] = challenge_type
    __args__['identifier'] = identifier
    __args__['mount'] = mount
    __args__['namespace'] = namespace
    __args__['orderId'] = order_id
    __args__['roleName'] = role_name
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('vault:pkiexternalca/getSecretBackendOrderChallenge:getSecretBackendOrderChallenge', __args__, opts=opts, typ=GetSecretBackendOrderChallengeResult).value

    return AwaitableGetSecretBackendOrderChallengeResult(
        challenge_type=pulumi.get(__ret__, 'challenge_type'),
        expires=pulumi.get(__ret__, 'expires'),
        id=pulumi.get(__ret__, 'id'),
        identifier=pulumi.get(__ret__, 'identifier'),
        key_authorization=pulumi.get(__ret__, 'key_authorization'),
        mount=pulumi.get(__ret__, 'mount'),
        namespace=pulumi.get(__ret__, 'namespace'),
        order_id=pulumi.get(__ret__, 'order_id'),
        role_name=pulumi.get(__ret__, 'role_name'),
        status=pulumi.get(__ret__, 'status'),
        token=pulumi.get(__ret__, 'token'))
def get_secret_backend_order_challenge_output(challenge_type: pulumi.Input[Optional[_builtins.str]] = None,
                                              identifier: pulumi.Input[Optional[_builtins.str]] = None,
                                              mount: pulumi.Input[Optional[_builtins.str]] = None,
                                              namespace: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                                              order_id: pulumi.Input[Optional[_builtins.str]] = None,
                                              role_name: pulumi.Input[Optional[_builtins.str]] = None,
                                              opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetSecretBackendOrderChallengeResult]:
    """
    Retrieves ACME challenge details for a specific identifier in an order. This data source provides the information needed to fulfill ACME challenges (HTTP-01, DNS-01, or TLS-ALPN-01) for domain validation.

    > **Note** This data source will poll the order status until it reaches the `awaiting-challenge-fulfillment` or `completed` state. Use this data source after creating an order with `pkiexternalca.SecretBackendOrder`.

    ## Example Usage

    ### With HTTP-01 Challenge

    ```python
    import pulumi
    import pulumi_local as local
    import pulumi_vault as vault

    pki_external_ca = vault.Mount("pki-external-ca",
        path="pki-external-ca",
        type="pki-external-ca")
    example = vault.pkiexternalca.SecretBackendAcmeAccount("example",
        mount=pki_external_ca.path,
        name="my-acme-account",
        directory_url="https://acme-v02.api.letsencrypt.org/directory",
        email_contacts=["admin@example.com"])
    example_secret_backend_role = vault.pkiexternalca.SecretBackendRole("example",
        mount=pki_external_ca.path,
        name="example-role",
        acme_account_name=example.name,
        allowed_domains=["example.com"],
        allowed_domain_options=[
            "bare_domains",
            "subdomains",
        ],
        allowed_challenge_types=["http-01"])
    example_secret_backend_order = vault.pkiexternalca.SecretBackendOrder("example",
        mount=pki_external_ca.path,
        role_name=example_secret_backend_role.name,
        identifiers=["www.example.com"])
    # Retrieve HTTP-01 challenge details
    http = vault.pkiexternalca.get_secret_backend_order_challenge_output(mount=pki_external_ca.path,
        role_name=example_secret_backend_role.name,
        order_id=example_secret_backend_order.order_id,
        challenge_type="http-01",
        identifier="www.example.com")
    # Deploy the challenge file
    acme_challenge = local.File("acme_challenge",
        filename=f/var/www/html/.well-known/acme-challenge/{http.token},
        content=http.key_authorization)
    pulumi.export("challengeToken", http.token)
    pulumi.export("challengeUrl", http.apply(lambda http: f"http://www.example.com/.well-known/acme-challenge/{http.token}"))
    ```

    ### With DNS-01 Challenge

    ```python
    import pulumi
    import pulumi_aws as aws
    import pulumi_vault as vault

    # Retrieve DNS-01 challenge details
    dns = vault.pkiexternalca.get_secret_backend_order_challenge(mount=pki_external_ca["path"],
        role_name=example_vault_pki_external_ca_secret_backend_role["name"],
        order_id=example["orderId"],
        challenge_type="dns-01",
        identifier="www.example.com")
    # Create DNS TXT record using AWS Route53
    acme_challenge = aws.Route53Record("acme_challenge",
        zone_id=example_aws_route53_zone.zone_id,
        name=_acme-challenge.www.example.com,
        type=TXT,
        ttl=60,
        records=[dns.key_authorization])
    pulumi.export("dnsRecordName", "_acme-challenge.www.example.com")
    pulumi.export("dnsRecordValue", dns.key_authorization)
    ```

    ### With TLS-ALPN-01 Challenge

    ```python
    import pulumi
    import pulumi_vault as vault

    # Retrieve TLS-ALPN-01 challenge details
    tls_alpn = vault.pkiexternalca.get_secret_backend_order_challenge(mount=pki_external_ca["path"],
        role_name=example_vault_pki_external_ca_secret_backend_role["name"],
        order_id=example["orderId"],
        challenge_type="tls-alpn-01",
        identifier="www.example.com")
    pulumi.export("tlsAlpnKeyAuth", tls_alpn.key_authorization)
    ```

    ## Challenge Type Details

    ### HTTP-01 Challenge

    For HTTP-01 challenges, you must serve the `key_authorization` value at:

    The response must:
    - Return HTTP 200 status
    - Have Content-Type: `text/plain` or no Content-Type header
    - Contain only the `key_authorization` value

    ### DNS-01 Challenge

    For DNS-01 challenges, you must create a TXT record at:

    The TXT record value must be the `key_authorization` value. DNS propagation may take time, so ensure the record is resolvable before marking the challenge as fulfilled.

    ### TLS-ALPN-01 Challenge

    For TLS-ALPN-01 challenges, you must configure your TLS server to:
    - Present a self-signed certificate for the identifier
    - Include the `acme-tls/1` ALPN protocol
    - Include a specific extension with the `key_authorization` value

    This challenge type is more complex and typically requires custom TLS server configuration.

    > **Note** This data source polls the order status with a maximum of 5 attempts at 2-second intervals. If the order does not reach the appropriate state within this time, the data source read will fail.


    :param _builtins.str challenge_type: The type of ACME challenge to retrieve. Valid values are:
           - `http-01` - HTTP challenge (requires placing a file at a specific URL)
           - `dns-01` - DNS challenge (requires creating a TXT record)
           - `tls-alpn-01` - TLS-ALPN challenge (requires configuring TLS with specific ALPN extension)
    :param _builtins.str identifier: The identifier (domain name) for which to retrieve the challenge.
    :param _builtins.str mount: The path where the PKI External CA secret backend is mounted.
    :param _builtins.str namespace: The namespace to provision the resource in.
           The value should not contain leading or trailing forward slashes.
           The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
           *Available only for Vault Enterprise*.
    :param _builtins.str order_id: The unique identifier for the ACME order.
    :param _builtins.str role_name: Name of the role associated with the order.
    """
    __args__ = dict()
    __args__['challengeType'] = challenge_type
    __args__['identifier'] = identifier
    __args__['mount'] = mount
    __args__['namespace'] = namespace
    __args__['orderId'] = order_id
    __args__['roleName'] = role_name
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('vault:pkiexternalca/getSecretBackendOrderChallenge:getSecretBackendOrderChallenge', __args__, opts=opts, typ=GetSecretBackendOrderChallengeResult)
    return __ret__.apply(lambda __response__: GetSecretBackendOrderChallengeResult(
        challenge_type=pulumi.get(__response__, 'challenge_type'),
        expires=pulumi.get(__response__, 'expires'),
        id=pulumi.get(__response__, 'id'),
        identifier=pulumi.get(__response__, 'identifier'),
        key_authorization=pulumi.get(__response__, 'key_authorization'),
        mount=pulumi.get(__response__, 'mount'),
        namespace=pulumi.get(__response__, 'namespace'),
        order_id=pulumi.get(__response__, 'order_id'),
        role_name=pulumi.get(__response__, 'role_name'),
        status=pulumi.get(__response__, 'status'),
        token=pulumi.get(__response__, 'token')))

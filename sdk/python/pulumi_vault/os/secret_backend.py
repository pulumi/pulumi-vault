# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from .. import _utilities

__all__ = ['SecretBackendArgs', 'SecretBackend']

@pulumi.input_type
class SecretBackendArgs:
    def __init__(__self__, *,
                 mount: pulumi.Input[_builtins.str],
                 max_versions: pulumi.Input[Optional[_builtins.int]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 ssh_host_key_trust_on_first_use: pulumi.Input[Optional[_builtins.bool]] = None):
        """
        The set of arguments for constructing a SecretBackend resource.

        :param pulumi.Input[_builtins.str] mount: The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
        :param pulumi.Input[_builtins.int] max_versions: The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] ssh_host_key_trust_on_first_use: If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
        """
        pulumi.set(__self__, "mount", mount)
        if max_versions is not None:
            pulumi.set(__self__, "max_versions", max_versions)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)
        if ssh_host_key_trust_on_first_use is not None:
            pulumi.set(__self__, "ssh_host_key_trust_on_first_use", ssh_host_key_trust_on_first_use)

    @_builtins.property
    @pulumi.getter
    def mount(self) -> pulumi.Input[_builtins.str]:
        """
        The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
        """
        return pulumi.get(self, "mount")

    @mount.setter
    def mount(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "mount", value)

    @_builtins.property
    @pulumi.getter(name="maxVersions")
    def max_versions(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
        """
        return pulumi.get(self, "max_versions")

    @max_versions.setter
    def max_versions(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "max_versions", value)

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The namespace to provision the resource in.
        The value should not contain leading or trailing forward slashes.
        The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @namespace.setter
    def namespace(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "namespace", value)

    @_builtins.property
    @pulumi.getter(name="sshHostKeyTrustOnFirstUse")
    def ssh_host_key_trust_on_first_use(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
        """
        return pulumi.get(self, "ssh_host_key_trust_on_first_use")

    @ssh_host_key_trust_on_first_use.setter
    def ssh_host_key_trust_on_first_use(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "ssh_host_key_trust_on_first_use", value)


@pulumi.input_type
class _SecretBackendState:
    def __init__(__self__, *,
                 max_versions: pulumi.Input[Optional[_builtins.int]] = None,
                 mount: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 ssh_host_key_trust_on_first_use: pulumi.Input[Optional[_builtins.bool]] = None):
        """
        Input properties used for looking up and filtering SecretBackend resources.

        :param pulumi.Input[_builtins.int] max_versions: The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
        :param pulumi.Input[_builtins.str] mount: The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] ssh_host_key_trust_on_first_use: If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
        """
        if max_versions is not None:
            pulumi.set(__self__, "max_versions", max_versions)
        if mount is not None:
            pulumi.set(__self__, "mount", mount)
        if namespace is not None:
            pulumi.set(__self__, "namespace", namespace)
        if ssh_host_key_trust_on_first_use is not None:
            pulumi.set(__self__, "ssh_host_key_trust_on_first_use", ssh_host_key_trust_on_first_use)

    @_builtins.property
    @pulumi.getter(name="maxVersions")
    def max_versions(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
        """
        return pulumi.get(self, "max_versions")

    @max_versions.setter
    def max_versions(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "max_versions", value)

    @_builtins.property
    @pulumi.getter
    def mount(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
        """
        return pulumi.get(self, "mount")

    @mount.setter
    def mount(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "mount", value)

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The namespace to provision the resource in.
        The value should not contain leading or trailing forward slashes.
        The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @namespace.setter
    def namespace(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "namespace", value)

    @_builtins.property
    @pulumi.getter(name="sshHostKeyTrustOnFirstUse")
    def ssh_host_key_trust_on_first_use(self) -> pulumi.Input[Optional[_builtins.bool]]:
        """
        If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
        """
        return pulumi.get(self, "ssh_host_key_trust_on_first_use")

    @ssh_host_key_trust_on_first_use.setter
    def ssh_host_key_trust_on_first_use(self, value: pulumi.Input[Optional[_builtins.bool]]):
        pulumi.set(self, "ssh_host_key_trust_on_first_use", value)


@pulumi.type_token("vault:os/secretBackend:SecretBackend")
class SecretBackend(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 max_versions: pulumi.Input[Optional[_builtins.int]] = None,
                 mount: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 ssh_host_key_trust_on_first_use: pulumi.Input[Optional[_builtins.bool]] = None,
                 __props__=None):
        """
        Manages OS Secrets Engine backend configuration in a Vault server. The OS Secrets Engine manages credentials
        for operating system accounts on remote hosts via SSH. This resource requires Vault 2.0.0 or later.

        The OS Secrets Engine mount itself is managed separately, typically with `Mount`. This resource only manages
        backend configuration for an existing OS mount.

        Before mounting the OS Secrets Engine, the external OS plugin must already be registered in Vault's plugin catalog.
        You can register it with the `Plugin` resource.

        The examples below use the canonical plugin name `vault-plugin-secrets-os`. If your Vault cluster registers the
        OS plugin under a different catalog name, use that name in `vault_mount.type` instead.

        See the [Vault documentation](https://www.vaultproject.io/docs/secrets/os) for more information.

        ## Example Usage

        ### Register Plugin And Configure Backend

        ```python
        import pulumi
        import pulumi_vault as vault

        os = vault.Plugin("os",
            type="secret",
            name="vault-plugin-secrets-os",
            version="v0.1.0+ent")
        os_mount = vault.Mount("os",
            path="os",
            type=os.name)
        os_secret_backend = vault.os.SecretBackend("os", mount=os_mount.path)
        ```

        ### Basic Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        os = vault.Mount("os",
            path="os",
            type="vault-plugin-secrets-os")
        os_secret_backend = vault.os.SecretBackend("os", mount=os.path)
        ```

        ### Advanced Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        os = vault.Mount("os",
            path="os-prod",
            type="vault-plugin-secrets-os")
        os_secret_backend = vault.os.SecretBackend("os",
            mount=os.path,
            max_versions=10,
            ssh_host_key_trust_on_first_use=True)
        ```

        ## Notes

        * This resource requires Vault 2.0.0 or later.
        * The OS Secrets Engine plugin must be registered before the mount is enabled. Use `Plugin` to manage catalog registration when appropriate.
        * Use `Mount` to create, tune, or remove the OS Secrets Engine mount.
        * When `ssh_host_key_trust_on_first_use` is enabled, the first connection to a host will automatically trust and store its SSH host key.

        ## Import

        OS Secret backend can be imported using the `mount`, e.g.

        ```sh
        $ pulumi import vault:os/secretBackend:SecretBackend os os
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.int] max_versions: The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
        :param pulumi.Input[_builtins.str] mount: The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] ssh_host_key_trust_on_first_use: If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: SecretBackendArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Manages OS Secrets Engine backend configuration in a Vault server. The OS Secrets Engine manages credentials
        for operating system accounts on remote hosts via SSH. This resource requires Vault 2.0.0 or later.

        The OS Secrets Engine mount itself is managed separately, typically with `Mount`. This resource only manages
        backend configuration for an existing OS mount.

        Before mounting the OS Secrets Engine, the external OS plugin must already be registered in Vault's plugin catalog.
        You can register it with the `Plugin` resource.

        The examples below use the canonical plugin name `vault-plugin-secrets-os`. If your Vault cluster registers the
        OS plugin under a different catalog name, use that name in `vault_mount.type` instead.

        See the [Vault documentation](https://www.vaultproject.io/docs/secrets/os) for more information.

        ## Example Usage

        ### Register Plugin And Configure Backend

        ```python
        import pulumi
        import pulumi_vault as vault

        os = vault.Plugin("os",
            type="secret",
            name="vault-plugin-secrets-os",
            version="v0.1.0+ent")
        os_mount = vault.Mount("os",
            path="os",
            type=os.name)
        os_secret_backend = vault.os.SecretBackend("os", mount=os_mount.path)
        ```

        ### Basic Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        os = vault.Mount("os",
            path="os",
            type="vault-plugin-secrets-os")
        os_secret_backend = vault.os.SecretBackend("os", mount=os.path)
        ```

        ### Advanced Configuration

        ```python
        import pulumi
        import pulumi_vault as vault

        os = vault.Mount("os",
            path="os-prod",
            type="vault-plugin-secrets-os")
        os_secret_backend = vault.os.SecretBackend("os",
            mount=os.path,
            max_versions=10,
            ssh_host_key_trust_on_first_use=True)
        ```

        ## Notes

        * This resource requires Vault 2.0.0 or later.
        * The OS Secrets Engine plugin must be registered before the mount is enabled. Use `Plugin` to manage catalog registration when appropriate.
        * Use `Mount` to create, tune, or remove the OS Secrets Engine mount.
        * When `ssh_host_key_trust_on_first_use` is enabled, the first connection to a host will automatically trust and store its SSH host key.

        ## Import

        OS Secret backend can be imported using the `mount`, e.g.

        ```sh
        $ pulumi import vault:os/secretBackend:SecretBackend os os
        ```


        :param str resource_name: The name of the resource.
        :param SecretBackendArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(SecretBackendArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 max_versions: pulumi.Input[Optional[_builtins.int]] = None,
                 mount: pulumi.Input[Optional[_builtins.str]] = None,
                 namespace: pulumi.Input[Optional[_builtins.str]] = None,
                 ssh_host_key_trust_on_first_use: pulumi.Input[Optional[_builtins.bool]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = SecretBackendArgs.__new__(SecretBackendArgs)

            __props__.__dict__["max_versions"] = max_versions
            if mount is None and not opts.urn:
                raise TypeError("Missing required property 'mount'")
            __props__.__dict__["mount"] = mount
            __props__.__dict__["namespace"] = namespace
            __props__.__dict__["ssh_host_key_trust_on_first_use"] = ssh_host_key_trust_on_first_use
        super(SecretBackend, __self__).__init__(
            'vault:os/secretBackend:SecretBackend',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            max_versions: pulumi.Input[Optional[_builtins.int]] = None,
            mount: pulumi.Input[Optional[_builtins.str]] = None,
            namespace: pulumi.Input[Optional[_builtins.str]] = None,
            ssh_host_key_trust_on_first_use: pulumi.Input[Optional[_builtins.bool]] = None) -> 'SecretBackend':
        """
        Get an existing SecretBackend resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.int] max_versions: The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
        :param pulumi.Input[_builtins.str] mount: The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
        :param pulumi.Input[_builtins.str] namespace: The namespace to provision the resource in.
               The value should not contain leading or trailing forward slashes.
               The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
               *Available only for Vault Enterprise*.
        :param pulumi.Input[_builtins.bool] ssh_host_key_trust_on_first_use: If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _SecretBackendState.__new__(_SecretBackendState)

        __props__.__dict__["max_versions"] = max_versions
        __props__.__dict__["mount"] = mount
        __props__.__dict__["namespace"] = namespace
        __props__.__dict__["ssh_host_key_trust_on_first_use"] = ssh_host_key_trust_on_first_use
        return SecretBackend(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="maxVersions")
    def max_versions(self) -> pulumi.Output[_builtins.int]:
        """
        The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
        """
        return pulumi.get(self, "max_versions")

    @_builtins.property
    @pulumi.getter
    def mount(self) -> pulumi.Output[_builtins.str]:
        """
        The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
        """
        return pulumi.get(self, "mount")

    @_builtins.property
    @pulumi.getter
    def namespace(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        The namespace to provision the resource in.
        The value should not contain leading or trailing forward slashes.
        The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
        *Available only for Vault Enterprise*.
        """
        return pulumi.get(self, "namespace")

    @_builtins.property
    @pulumi.getter(name="sshHostKeyTrustOnFirstUse")
    def ssh_host_key_trust_on_first_use(self) -> pulumi.Output[_builtins.bool]:
        """
        If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
        """
        return pulumi.get(self, "ssh_host_key_trust_on_first_use")


// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages custom HTTP headers for the Vault UI. This resource allows you to configure
 * custom HTTP response headers that will be sent by the Vault UI, enabling security
 * policies, CORS configuration, and custom organizational headers.
 *
 * > **Important** This resource requires **Vault 1.16.0 or later**. The `sys/config/ui/headers` API endpoint was introduced in Vault 1.16.0.
 *
 * > **Important** All operations on this resource require the `sudo` capability on the
 * `sys/config/ui/headers/*` path.
 *
 * ## Example Usage
 *
 * ### Basic Header Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const custom = new vault.config.UiHeader("custom", {
 *     name: "X-Custom-Header",
 *     values: ["custom-value"],
 * });
 * ```
 *
 * ### Security Headers
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * // Content Security Policy
 * const csp = new vault.config.UiHeader("csp", {
 *     name: "Content-Security-Policy",
 *     values: ["default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'"],
 * });
 * // X-Frame-Options
 * const frameOptions = new vault.config.UiHeader("frame_options", {
 *     name: "X-Frame-Options",
 *     values: ["DENY"],
 * });
 * // Strict Transport Security
 * const hsts = new vault.config.UiHeader("hsts", {
 *     name: "Strict-Transport-Security",
 *     values: ["max-age=31536000; includeSubDomains; preload"],
 * });
 * // X-Content-Type-Options
 * const contentTypeOptions = new vault.config.UiHeader("content_type_options", {
 *     name: "X-Content-Type-Options",
 *     values: ["nosniff"],
 * });
 * ```
 *
 * ### CORS Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const corsOrigin = new vault.config.UiHeader("cors_origin", {
 *     name: "Access-Control-Allow-Origin",
 *     values: [
 *         "https://example.com",
 *         "https://app.example.com",
 *     ],
 * });
 * const corsMethods = new vault.config.UiHeader("cors_methods", {
 *     name: "Access-Control-Allow-Methods",
 *     values: [
 *         "GET",
 *         "POST",
 *         "OPTIONS",
 *     ],
 * });
 * const corsHeaders = new vault.config.UiHeader("cors_headers", {
 *     name: "Access-Control-Allow-Headers",
 *     values: [
 *         "Content-Type",
 *         "Authorization",
 *     ],
 * });
 * ```
 *
 * ### Multiple Values
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const multiValue = new vault.config.UiHeader("multi_value", {
 *     name: "X-Multi-Value-Header",
 *     values: [
 *         "value1",
 *         "value2",
 *         "value3",
 *     ],
 * });
 * ```
 *
 * ## Migration from vault.generic.Endpoint
 *
 * If you're currently managing UI headers using `vault.generic.Endpoint`, you can migrate
 * to this dedicated resource:
 *
 * ### Before (using generic_endpoint)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const cspHeader = new vault.generic.Endpoint("csp_header", {
 *     path: "sys/config/ui/headers/Content-Security-Policy",
 *     dataJson: JSON.stringify({
 *         values: ["default-src 'self'"],
 *     }),
 * });
 * ```
 *
 * ### After (using dedicated resource)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const csp = new vault.config.UiHeader("csp", {
 *     name: "Content-Security-Policy",
 *     values: ["default-src 'self'"],
 * });
 * ```
 *
 * ### Migration Steps
 *
 * 1. Add the new `vault.config.UiHeader` resource to your configuration
 * 2. Import the existing header: `terraform import vault_config_ui_header.csp Content-Security-Policy`
 * 3. Remove the old `vault.generic.Endpoint` resource from your configuration
 * 4. Run `pulumi preview` to verify no changes are required
 *
 * ## Important Notes
 *
 * ### State Management
 *
 * This resource follows Terraform best practices by reading the actual configuration from Vault
 * after create and update operations. This ensures that the Terraform state always reflects the
 * actual state in Vault, including any server-side processing or normalization of values.
 *
 * ## Import
 *
 * UI header configurations can be imported using the header `name`, e.g.
 *
 * ```sh
 * $ pulumi import vault:config/uiHeader:UiHeader csp Content-Security-Policy
 * ```
 */
export class UiHeader extends pulumi.CustomResource {
    /**
     * Get an existing UiHeader resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: UiHeaderState, opts?: pulumi.CustomResourceOptions): UiHeader {
        return new UiHeader(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:config/uiHeader:UiHeader';

    /**
     * Returns true if the given object is an instance of UiHeader.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is UiHeader {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === UiHeader.__pulumiType;
    }

    /**
     * The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
     * Changing this will recreate the resource.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * A set of values for the header. At least one value is required.
     * Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
     *
     * > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
     */
    declare public readonly values: pulumi.Output<string[]>;

    /**
     * Create a UiHeader resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: UiHeaderArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: UiHeaderArgs | UiHeaderState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as UiHeaderState | undefined;
            resourceInputs["name"] = state?.name;
            resourceInputs["values"] = state?.values;
        } else {
            const args = argsOrState as UiHeaderArgs | undefined;
            if (args?.values === undefined && !opts.urn) {
                throw new Error("Missing required property 'values'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["values"] = args?.values;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(UiHeader.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering UiHeader resources.
 */
export interface UiHeaderState {
    /**
     * The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
     * Changing this will recreate the resource.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * A set of values for the header. At least one value is required.
     * Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
     *
     * > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
     */
    values?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a UiHeader resource.
 */
export interface UiHeaderArgs {
    /**
     * The name of the custom header (e.g., "Content-Security-Policy", "X-Frame-Options").
     * Changing this will recreate the resource.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * A set of values for the header. At least one value is required.
     * Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
     *
     * > **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
     */
    values: pulumi.Input<pulumi.Input<string>[]>;
}

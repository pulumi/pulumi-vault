// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages the global group policy application mode for Vault Enterprise. This resource controls how policies attached to identity groups are applied across namespace boundaries.
 *
 * **Important:** This is a singleton resource - only one instance can exist per Vault cluster. The resource must be managed from the root or administrative namespace.
 *
 * **Note** this feature is available only with Vault Enterprise 1.13.8+.
 *
 * ## Example Usage
 *
 * ### Basic Usage (Root Namespace)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * // Configure group policy application mode in root namespace
 * // Using the default mode
 * const test = new vault.config.GroupPolicyApplication("test", {groupPolicyApplicationMode: "within_namespace_hierarchy"});
 * ```
 *
 * ### Using "any" Mode
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const config = new vault.config.GroupPolicyApplication("config", {groupPolicyApplicationMode: "any"});
 * ```
 *
 * ### Explicit Root Namespace
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const config = new vault.config.GroupPolicyApplication("config", {groupPolicyApplicationMode: "within_namespace_hierarchy"});
 * ```
 *
 * ### Administrative Namespace
 *
 * When working with an administrative namespace, you can specify it directly in the resource:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const config = new vault.config.GroupPolicyApplication("config", {
 *     groupPolicyApplicationMode: "within_namespace_hierarchy",
 *     namespace: "admin",
 * });
 * ```
 *
 * ## Behavior Notes
 *
 * ### Singleton Resource
 *
 * This resource is a singleton, meaning only one instance can exist per Vault cluster. Multiple `vault.config.GroupPolicyApplication` resources in your Terraform configuration will conflict.
 *
 * ### Deletion Behavior
 *
 * When this resource is destroyed via `terraform destroy`, it does not delete the configuration from Vault. Instead, it resets the mode to the default value `withinNamespaceHierarchy`. This ensures that Vault always has a valid group policy application mode configured.
 *
 * ### Namespace Requirements
 *
 * This resource can only be managed from:
 * - The root namespace
 * - The administrative namespace (`"admin"`)
 *
 * Attempting to manage this resource from any other namespace will result in an error.
 *
 * ### Policy Application Scope
 *
 * **Important:** The group policy application mode only applies to ACL policies and no longer affects Sentinel RGPs for Vault ≥ 1.13.8, 1.14.4, 1.15.0.
 *
 * ### Replication Behavior
 *
 * This configuration will be replicated between primary and secondary clusters. Primaries cannot have a different mode than secondaries.
 *
 * ## Version Requirements
 *
 * - Requires Vault Enterprise 1.15.0 or later (TFVP support)
 *
 * ## Import
 *
 * The group policy application configuration can be imported using the path `sys/config/group-policy-application`:
 *
 * ```sh
 * $ pulumi import vault:config/groupPolicyApplication:GroupPolicyApplication config sys/config/group-policy-application
 * ```
 */
export class GroupPolicyApplication extends pulumi.CustomResource {
    /**
     * Get an existing GroupPolicyApplication resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: GroupPolicyApplicationState, opts?: pulumi.CustomResourceOptions): GroupPolicyApplication {
        return new GroupPolicyApplication(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:config/groupPolicyApplication:GroupPolicyApplication';

    /**
     * Returns true if the given object is an instance of GroupPolicyApplication.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is GroupPolicyApplication {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === GroupPolicyApplication.__pulumiType;
    }

    /**
     * Mode for group policy application. Must be either `withinNamespaceHierarchy` or `any`. Defaults to `withinNamespaceHierarchy`.
     */
    declare public readonly groupPolicyApplicationMode: pulumi.Output<string>;
    /**
     * Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
     * Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;

    /**
     * Create a GroupPolicyApplication resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: GroupPolicyApplicationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: GroupPolicyApplicationArgs | GroupPolicyApplicationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as GroupPolicyApplicationState | undefined;
            resourceInputs["groupPolicyApplicationMode"] = state?.groupPolicyApplicationMode;
            resourceInputs["namespace"] = state?.namespace;
        } else {
            const args = argsOrState as GroupPolicyApplicationArgs | undefined;
            if (args?.groupPolicyApplicationMode === undefined && !opts.urn) {
                throw new Error("Missing required property 'groupPolicyApplicationMode'");
            }
            resourceInputs["groupPolicyApplicationMode"] = args?.groupPolicyApplicationMode;
            resourceInputs["namespace"] = args?.namespace;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(GroupPolicyApplication.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering GroupPolicyApplication resources.
 */
export interface GroupPolicyApplicationState {
    /**
     * Mode for group policy application. Must be either `withinNamespaceHierarchy` or `any`. Defaults to `withinNamespaceHierarchy`.
     */
    groupPolicyApplicationMode?: pulumi.Input<string | undefined>;
    /**
     * Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
     * Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a GroupPolicyApplication resource.
 */
export interface GroupPolicyApplicationArgs {
    /**
     * Mode for group policy application. Must be either `withinNamespaceHierarchy` or `any`. Defaults to `withinNamespaceHierarchy`.
     */
    groupPolicyApplicationMode: pulumi.Input<string>;
    /**
     * Target namespace. Must be root (`""`) or administrative (`"admin"`) namespace. If omitted, the provider's configured namespace is used.
     * Set this to `""` to explicitly target the root namespace. The value should not contain leading or trailing forward slashes.
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

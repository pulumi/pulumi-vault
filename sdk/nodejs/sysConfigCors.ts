// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages the CORS (Cross-Origin Resource Sharing) configuration for Vault, controlling which origins can make cross-origin requests and which headers are allowed.
 *
 * > **Important** This resource requires `sudo` capability and must be called from the root namespace. CORS configuration does not replicate across Performance Replication clusters in Vault Enterprise.
 *
 * **Note** This feature is available in Vault 1.14+
 *
 * ## Example Usage
 *
 * ### Enable CORS for specific origins
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const example = new vault.SysConfigCors("example", {
 *     allowedOrigins: [
 *         "http://www.example.com",
 *         "https://app.example.com",
 *     ],
 *     allowedHeaders: [
 *         "X-Custom-Header",
 *         "X-Application-ID",
 *     ],
 * });
 * ```
 *
 * ### Allow all origins (wildcard)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const wildcard = new vault.SysConfigCors("wildcard", {allowedOrigins: ["*"]});
 * ```
 *
 * ### Production environment configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const production = new vault.SysConfigCors("production", {
 *     allowedOrigins: [
 *         "https://app.example.com",
 *         "https://admin.example.com",
 *         "https://api.example.com",
 *     ],
 *     allowedHeaders: [
 *         "X-Custom-Header",
 *         "X-Request-ID",
 *         "X-Application-Version",
 *     ],
 * });
 * ```
 *
 * ## Security Considerations
 *
 * * The wildcard `"*"` origin should be used cautiously and typically only in development environments
 * * Allowing all origins in production can expose Vault to cross-site request forgery attacks
 * * Custom headers should be carefully reviewed to ensure they don't expose sensitive information
 * * CORS settings must be configured from the root namespace
 * * In Vault Enterprise with Performance Replication, CORS configuration does not replicate across clusters - each secondary cluster must have its CORS configuration set independently
 * * To disable CORS, delete the resource using `terraform destroy` or remove it from your configuration. There is no `enabled = false` option
 *
 * ## API Documentation
 *
 * For more information on the Vault CORS configuration API, see the [Vault API documentation](https://developer.hashicorp.com/vault/api-docs/system/config-cors).
 *
 * ## Import
 *
 * CORS configuration can be imported using the fixed ID `sys/config/cors`:
 *
 * ```sh
 * $ pulumi import vault:index/sysConfigCors:SysConfigCors example sys/config/cors
 * ```
 */
export class SysConfigCors extends pulumi.CustomResource {
    /**
     * Get an existing SysConfigCors resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SysConfigCorsState, opts?: pulumi.CustomResourceOptions): SysConfigCors {
        return new SysConfigCors(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/sysConfigCors:SysConfigCors';

    /**
     * Returns true if the given object is an instance of SysConfigCors.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SysConfigCors {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SysConfigCors.__pulumiType;
    }

    /**
     * Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
     * - `Content-Type`
     * - `X-Requested-With`
     * - `X-Vault-AWS-IAM-Server-ID`
     * - `X-Vault-MFA`
     * - `X-Vault-No-Request-Forwarding`
     * - `X-Vault-Wrap-Format`
     * - `X-Vault-Wrap-TTL`
     * - `X-Vault-Policy-Override`
     * - `Authorization`
     * - `X-Vault-Token`
     */
    declare public readonly allowedHeaders: pulumi.Output<string[] | undefined>;
    /**
     * Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
     */
    declare public readonly allowedOrigins: pulumi.Output<string[]>;
    /**
     * (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
     */
    declare public /*out*/ readonly enabled: pulumi.Output<boolean>;

    /**
     * Create a SysConfigCors resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SysConfigCorsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SysConfigCorsArgs | SysConfigCorsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SysConfigCorsState | undefined;
            resourceInputs["allowedHeaders"] = state?.allowedHeaders;
            resourceInputs["allowedOrigins"] = state?.allowedOrigins;
            resourceInputs["enabled"] = state?.enabled;
        } else {
            const args = argsOrState as SysConfigCorsArgs | undefined;
            if (args?.allowedOrigins === undefined && !opts.urn) {
                throw new Error("Missing required property 'allowedOrigins'");
            }
            resourceInputs["allowedHeaders"] = args?.allowedHeaders;
            resourceInputs["allowedOrigins"] = args?.allowedOrigins;
            resourceInputs["enabled"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SysConfigCors.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SysConfigCors resources.
 */
export interface SysConfigCorsState {
    /**
     * Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
     * - `Content-Type`
     * - `X-Requested-With`
     * - `X-Vault-AWS-IAM-Server-ID`
     * - `X-Vault-MFA`
     * - `X-Vault-No-Request-Forwarding`
     * - `X-Vault-Wrap-Format`
     * - `X-Vault-Wrap-TTL`
     * - `X-Vault-Policy-Override`
     * - `Authorization`
     * - `X-Vault-Token`
     */
    allowedHeaders?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
     */
    allowedOrigins?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
     */
    enabled?: pulumi.Input<boolean | undefined>;
}

/**
 * The set of arguments for constructing a SysConfigCors resource.
 */
export interface SysConfigCorsArgs {
    /**
     * Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
     * - `Content-Type`
     * - `X-Requested-With`
     * - `X-Vault-AWS-IAM-Server-ID`
     * - `X-Vault-MFA`
     * - `X-Vault-No-Request-Forwarding`
     * - `X-Vault-Wrap-Format`
     * - `X-Vault-Wrap-TTL`
     * - `X-Vault-Policy-Override`
     * - `Authorization`
     * - `X-Vault-Token`
     */
    allowedHeaders?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Set of origins permitted to make cross-origin requests. Use `"*"` as the only value to allow all origins. Must contain at least one origin.
     */
    allowedOrigins: pulumi.Input<pulumi.Input<string>[]>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages imported KMIP Secret CAs in a Vault server. This resource imports an existing CA certificate. This feature requires Vault Enterprise. See the [Vault documentation](https://www.vaultproject.io/docs/secrets/kmip) for more information.
 *
 * ## Example Usage
 *
 * ### Import CA with Named Scope and Role
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const _default = new vault.kmip.SecretBackend("default", {
 *     path: "kmip",
 *     description: "Vault KMIP backend",
 * });
 * const named = new vault.kmip.SecretCaImported("named", {
 *     path: _default.path,
 *     name: "imported-ca",
 *     caPem: std.file({
 *         input: "path/to/ca-certificate.pem",
 *     }).then(invoke => invoke.result),
 *     scopeName: "production",
 *     roleName: "admin",
 * });
 * ```
 *
 * ### Import CA with Field-Based Mapping
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const _default = new vault.kmip.SecretBackend("default", {
 *     path: "kmip",
 *     description: "Vault KMIP backend",
 * });
 * const fieldBased = new vault.kmip.SecretCaImported("field_based", {
 *     path: _default.path,
 *     name: "imported-ca-fields",
 *     caPem: std.file({
 *         input: "path/to/ca-certificate.pem",
 *     }).then(invoke => invoke.result),
 *     scopeField: "O",
 *     roleField: "OU",
 * });
 * ```
 *
 * ### Import CA with Mixed Mapping
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const _default = new vault.kmip.SecretBackend("default", {
 *     path: "kmip",
 *     description: "Vault KMIP backend",
 * });
 * const mixed = new vault.kmip.SecretCaImported("mixed", {
 *     path: _default.path,
 *     name: "imported-ca-mixed",
 *     caPem: std.file({
 *         input: "path/to/ca-certificate.pem",
 *     }).then(invoke => invoke.result),
 *     scopeName: "production",
 *     roleField: "CN",
 * });
 * ```
 *
 * ## Configuration Requirements
 *
 * When configuring an imported CA, you must specify:
 *
 * * Exactly one of `scopeName` or `scopeField`
 * * Exactly one of `roleName` or `roleField`
 *
 * You can mix and match name-based and field-based configurations. For example, you can use `scopeName` with `roleField`.
 *
 * ## Updating
 *
 * The `scopeName`, `scopeField`, `roleName`, and `roleField` parameters can be updated after creation. All other parameters require replacement if changed.
 *
 * ## Import
 *
 * KMIP Secret CA Imported can be imported using the format `<path>/ca/<name>`, e.g.
 *
 * ```sh
 * $ pulumi import vault:kmip/secretCaImported:SecretCaImported example kmip/ca/my-ca
 * ```
 *
 * **Note:** When importing, the `caPem` value cannot be retrieved from Vault and will need to be set in your configuration. This value will be ignored during import verification.
 */
export class SecretCaImported extends pulumi.CustomResource {
    /**
     * Get an existing SecretCaImported resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretCaImportedState, opts?: pulumi.CustomResourceOptions): SecretCaImported {
        return new SecretCaImported(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:kmip/secretCaImported:SecretCaImported';

    /**
     * Returns true if the given object is an instance of SecretCaImported.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretCaImported {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretCaImported.__pulumiType;
    }

    /**
     * CA certificate in PEM format.
     */
    declare public readonly caPem: pulumi.Output<string>;
    /**
     * Name to identify the CA. This will be used in the CA's path.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * Path where KMIP backend is mounted. Must not begin or end with a `/`.
     */
    declare public readonly path: pulumi.Output<string>;
    /**
     * The field in the certificate to use for the role. Valid values are `CN`, `O`, `OU`, or `UID`. Must specify exactly one of `roleName` or `roleField`.
     */
    declare public readonly roleField: pulumi.Output<string | undefined>;
    /**
     * The role name to associate with this CA. Must specify exactly one of `roleName` or `roleField`.
     */
    declare public readonly roleName: pulumi.Output<string | undefined>;
    /**
     * The field in the certificate to use for the scope. Valid values are `CN`, `O`, `OU`, or `UID`. Must specify exactly one of `scopeName` or `scopeField`.
     */
    declare public readonly scopeField: pulumi.Output<string | undefined>;
    /**
     * The scope name to associate with this CA. Must specify exactly one of `scopeName` or `scopeField`.
     */
    declare public readonly scopeName: pulumi.Output<string | undefined>;

    /**
     * Create a SecretCaImported resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecretCaImportedArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretCaImportedArgs | SecretCaImportedState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretCaImportedState | undefined;
            resourceInputs["caPem"] = state?.caPem;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["path"] = state?.path;
            resourceInputs["roleField"] = state?.roleField;
            resourceInputs["roleName"] = state?.roleName;
            resourceInputs["scopeField"] = state?.scopeField;
            resourceInputs["scopeName"] = state?.scopeName;
        } else {
            const args = argsOrState as SecretCaImportedArgs | undefined;
            if (args?.caPem === undefined && !opts.urn) {
                throw new Error("Missing required property 'caPem'");
            }
            if (args?.path === undefined && !opts.urn) {
                throw new Error("Missing required property 'path'");
            }
            resourceInputs["caPem"] = args?.caPem;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["path"] = args?.path;
            resourceInputs["roleField"] = args?.roleField;
            resourceInputs["roleName"] = args?.roleName;
            resourceInputs["scopeField"] = args?.scopeField;
            resourceInputs["scopeName"] = args?.scopeName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecretCaImported.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretCaImported resources.
 */
export interface SecretCaImportedState {
    /**
     * CA certificate in PEM format.
     */
    caPem?: pulumi.Input<string | undefined>;
    /**
     * Name to identify the CA. This will be used in the CA's path.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Path where KMIP backend is mounted. Must not begin or end with a `/`.
     */
    path?: pulumi.Input<string | undefined>;
    /**
     * The field in the certificate to use for the role. Valid values are `CN`, `O`, `OU`, or `UID`. Must specify exactly one of `roleName` or `roleField`.
     */
    roleField?: pulumi.Input<string | undefined>;
    /**
     * The role name to associate with this CA. Must specify exactly one of `roleName` or `roleField`.
     */
    roleName?: pulumi.Input<string | undefined>;
    /**
     * The field in the certificate to use for the scope. Valid values are `CN`, `O`, `OU`, or `UID`. Must specify exactly one of `scopeName` or `scopeField`.
     */
    scopeField?: pulumi.Input<string | undefined>;
    /**
     * The scope name to associate with this CA. Must specify exactly one of `scopeName` or `scopeField`.
     */
    scopeName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SecretCaImported resource.
 */
export interface SecretCaImportedArgs {
    /**
     * CA certificate in PEM format.
     */
    caPem: pulumi.Input<string>;
    /**
     * Name to identify the CA. This will be used in the CA's path.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Path where KMIP backend is mounted. Must not begin or end with a `/`.
     */
    path: pulumi.Input<string>;
    /**
     * The field in the certificate to use for the role. Valid values are `CN`, `O`, `OU`, or `UID`. Must specify exactly one of `roleName` or `roleField`.
     */
    roleField?: pulumi.Input<string | undefined>;
    /**
     * The role name to associate with this CA. Must specify exactly one of `roleName` or `roleField`.
     */
    roleName?: pulumi.Input<string | undefined>;
    /**
     * The field in the certificate to use for the scope. Valid values are `CN`, `O`, `OU`, or `UID`. Must specify exactly one of `scopeName` or `scopeField`.
     */
    scopeField?: pulumi.Input<string | undefined>;
    /**
     * The scope name to associate with this CA. Must specify exactly one of `scopeName` or `scopeField`.
     */
    scopeName?: pulumi.Input<string | undefined>;
}

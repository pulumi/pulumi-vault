// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Distributes a key from Vault's Key Management secrets engine to an external Key Management Service provider (AWS KMS, Azure Key Vault, or GCP Cloud KMS). This resource manages the distribution of cryptographic keys to external KMS systems, allowing them to be used for encryption, decryption, signing, and verification operations in cloud environments.
 *
 * > **Important**
 * All data provided in the resource configuration will be written in cleartext to
 * state and plan files generated by Terraform, and will appear in the console
 * output when Terraform runs. Protect these artifacts accordingly. See the main provider documentation for more details.
 *
 * For more information on distributing keys with Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 *
 * **Note** this feature is available only with Vault Enterprise.
 *
 * ## Example Usage
 *
 * ### Distribute to AWS KMS
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const keymgmt = new vault.Mount("keymgmt", {
 *     path: "keymgmt",
 *     type: "keymgmt",
 * });
 * const encryptionKey = new vault.KeymgmtKey("encryption_key", {
 *     mount: keymgmt.path,
 *     name: "aws-encryption-key",
 *     type: "aes256-gcm96",
 * });
 * const aws = new vault.KeymgmtAwsKms("aws", {
 *     mount: keymgmt.path,
 *     name: "aws-kms",
 *     keyCollection: "us-west-2",
 *     credentialsWo: {
 *         access_key: awsAccessKeyId,
 *         secret_key: awsSecretAccessKey,
 *     },
 *     credentialsWoVersion: 1,
 * });
 * const awsDist = new vault.KeymgmtDistributeKey("aws_dist", {
 *     mount: keymgmt.path,
 *     kmsName: aws.name,
 *     keyName: encryptionKey.name,
 *     purposes: [
 *         "encrypt",
 *         "decrypt",
 *     ],
 *     protection: "hsm",
 * });
 * ```
 *
 * ### Distribute to AWS KMS (Using Environment Variables)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * // When AWS credentials are configured via environment variables or IAM roles
 * // on the Vault server, you can omit access_key and secret_key
 * const aws = new vault.KeymgmtAwsKms("aws", {
 *     mount: keymgmt.path,
 *     name: "aws-kms",
 *     keyCollection: "us-west-2",
 * });
 * const awsDist = new vault.KeymgmtDistributeKey("aws_dist", {
 *     mount: keymgmt.path,
 *     kmsName: aws.name,
 *     keyName: encryptionKey.name,
 *     purposes: [
 *         "encrypt",
 *         "decrypt",
 *     ],
 * });
 * ```
 *
 * ### Distribute to Azure Key Vault
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const signingKey = new vault.KeymgmtKey("signing_key", {
 *     mount: keymgmt.path,
 *     name: "azure-signing-key",
 *     type: "rsa-2048",
 * });
 * const azure = new vault.KeymgmtAzureKms("azure", {
 *     mount: keymgmt.path,
 *     name: "azure-kv",
 *     keyCollection: "my-keyvault",
 *     credentialsWo: {
 *         tenant_id: azureTenantId,
 *         client_id: azureClientId,
 *         client_secret: azureClientSecret,
 *     },
 *     credentialsWoVersion: 1,
 * });
 * const azureDist = new vault.KeymgmtDistributeKey("azure_dist", {
 *     mount: keymgmt.path,
 *     kmsName: azure.name,
 *     keyName: signingKey.name,
 *     purposes: [
 *         "sign",
 *         "verify",
 *     ],
 * });
 * ```
 *
 * ### Distribute to GCP Cloud KMS
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const gcpKey = new vault.KeymgmtKey("gcp_key", {
 *     mount: keymgmt.path,
 *     name: "gcp-encryption-key",
 *     type: "aes256-gcm96",
 * });
 * const gcp = new vault.KeymgmtGcpKms("gcp", {
 *     mount: keymgmt.path,
 *     name: "gcp-kms",
 *     keyCollection: "projects/my-project/locations/us-central1/keyRings/my-keyring",
 *     credentialsWo: {
 *         service_account_file: std.file({
 *             input: "gcp-credentials.json",
 *         }).then(invoke => invoke.result),
 *         project: "my-project",
 *         location: "us-central1",
 *     },
 *     credentialsWoVersion: 1,
 * });
 * const gcpDist = new vault.KeymgmtDistributeKey("gcp_dist", {
 *     mount: keymgmt.path,
 *     kmsName: gcp.name,
 *     keyName: gcpKey.name,
 *     purposes: [
 *         "encrypt",
 *         "decrypt",
 *     ],
 *     protection: "software",
 * });
 * ```
 *
 * ## Import
 *
 * Key distributions can be imported using the format `{path}/kms/{kms_name}/key/{key_name}`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/keymgmtDistributeKey:KeymgmtDistributeKey aws_dist keymgmt/kms/aws-kms/key/aws-encryption-key
 * ```
 *
 * > **Note:** When importing, the `purpose` and `protection` fields are not returned by the Vault API and will not be populated in state. Both fields carry `Forces new resource`, so a missing or mismatched value will trigger a destroy+recreate on the next `pulumi up`. Always set `purpose` (and `protection` if not using the default `hsm`) explicitly in your configuration after import.
 */
export class KeymgmtDistributeKey extends pulumi.CustomResource {
    /**
     * Get an existing KeymgmtDistributeKey resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KeymgmtDistributeKeyState, opts?: pulumi.CustomResourceOptions): KeymgmtDistributeKey {
        return new KeymgmtDistributeKey(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/keymgmtDistributeKey:KeymgmtDistributeKey';

    /**
     * Returns true if the given object is an instance of KeymgmtDistributeKey.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KeymgmtDistributeKey {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KeymgmtDistributeKey.__pulumiType;
    }

    /**
     * Specifies the name of the key to distribute to the given KMS provider.
     */
    declare public readonly keyName: pulumi.Output<string>;
    /**
     * Specifies the name of the KMS provider to distribute the given key to.
     */
    declare public readonly kmsName: pulumi.Output<string>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * Specifies the protection of the key. The protection defines where cryptographic operations are performed with the key in the KMS provider. The following values are supported: `hsm`, `software`. Defaults to `hsm`.
     */
    declare public readonly protection: pulumi.Output<string>;
    /**
     * Specifies the purpose of the key. The purpose defines a set of cryptographic capabilities that the key will have in the KMS provider. A key must have at least one of the supported purposes. The following values are supported : encrypt, decrypt, sign, verify, wrap, unwrap.
     */
    declare public readonly purposes: pulumi.Output<string[]>;
    /**
     * Map of distributed key versions to their identifiers in the KMS provider.
     */
    declare public /*out*/ readonly versions: pulumi.Output<{[key: string]: string}>;

    /**
     * Create a KeymgmtDistributeKey resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KeymgmtDistributeKeyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KeymgmtDistributeKeyArgs | KeymgmtDistributeKeyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KeymgmtDistributeKeyState | undefined;
            resourceInputs["keyName"] = state?.keyName;
            resourceInputs["kmsName"] = state?.kmsName;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["protection"] = state?.protection;
            resourceInputs["purposes"] = state?.purposes;
            resourceInputs["versions"] = state?.versions;
        } else {
            const args = argsOrState as KeymgmtDistributeKeyArgs | undefined;
            if (args?.keyName === undefined && !opts.urn) {
                throw new Error("Missing required property 'keyName'");
            }
            if (args?.kmsName === undefined && !opts.urn) {
                throw new Error("Missing required property 'kmsName'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            if (args?.purposes === undefined && !opts.urn) {
                throw new Error("Missing required property 'purposes'");
            }
            resourceInputs["keyName"] = args?.keyName;
            resourceInputs["kmsName"] = args?.kmsName;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["protection"] = args?.protection;
            resourceInputs["purposes"] = args?.purposes;
            resourceInputs["versions"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KeymgmtDistributeKey.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KeymgmtDistributeKey resources.
 */
export interface KeymgmtDistributeKeyState {
    /**
     * Specifies the name of the key to distribute to the given KMS provider.
     */
    keyName?: pulumi.Input<string | undefined>;
    /**
     * Specifies the name of the KMS provider to distribute the given key to.
     */
    kmsName?: pulumi.Input<string | undefined>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Specifies the protection of the key. The protection defines where cryptographic operations are performed with the key in the KMS provider. The following values are supported: `hsm`, `software`. Defaults to `hsm`.
     */
    protection?: pulumi.Input<string | undefined>;
    /**
     * Specifies the purpose of the key. The purpose defines a set of cryptographic capabilities that the key will have in the KMS provider. A key must have at least one of the supported purposes. The following values are supported : encrypt, decrypt, sign, verify, wrap, unwrap.
     */
    purposes?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Map of distributed key versions to their identifiers in the KMS provider.
     */
    versions?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
}

/**
 * The set of arguments for constructing a KeymgmtDistributeKey resource.
 */
export interface KeymgmtDistributeKeyArgs {
    /**
     * Specifies the name of the key to distribute to the given KMS provider.
     */
    keyName: pulumi.Input<string>;
    /**
     * Specifies the name of the KMS provider to distribute the given key to.
     */
    kmsName: pulumi.Input<string>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Specifies the protection of the key. The protection defines where cryptographic operations are performed with the key in the KMS provider. The following values are supported: `hsm`, `software`. Defaults to `hsm`.
     */
    protection?: pulumi.Input<string | undefined>;
    /**
     * Specifies the purpose of the key. The purpose defines a set of cryptographic capabilities that the key will have in the KMS provider. A key must have at least one of the supported purposes. The following values are supported : encrypt, decrypt, sign, verify, wrap, unwrap.
     */
    purposes: pulumi.Input<pulumi.Input<string>[]>;
}

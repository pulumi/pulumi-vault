// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Reads the current CORS (Cross-Origin Resource Sharing) configuration from Vault.
 *
 * > **Important** This data source reads from the root namespace only.
 *
 * **Note** This feature is available in Vault 1.14+
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const current = vault.getSysConfigCors({});
 * export const corsEnabled = current.then(current => current.enabled);
 * export const allowedOrigins = current.then(current => current.allowedOrigins);
 * export const allowedHeaders = current.then(current => current.allowedHeaders);
 * ```
 *
 * ### Using with resource
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const production = new vault.SysConfigCors("production", {
 *     allowedOrigins: [
 *         "https://app.example.com",
 *         "https://admin.example.com",
 *         "https://api.example.com",
 *     ],
 *     allowedHeaders: [
 *         "X-Custom-Header",
 *         "X-Request-ID",
 *         "X-Application-Version",
 *     ],
 * });
 * const current = vault.getSysConfigCors({});
 * export const corsConfiguration = {
 *     enabled: current.then(current => current.enabled),
 *     allowedOrigins: current.then(current => current.allowedOrigins),
 *     allowedHeaders: current.then(current => current.allowedHeaders),
 * };
 * ```
 *
 * ## API Documentation
 *
 * For more information on the Vault CORS configuration API, see the [Vault API documentation](https://developer.hashicorp.com/vault/api-docs/system/config-cors).
 */
export function getSysConfigCors(opts?: pulumi.InvokeOptions): Promise<GetSysConfigCorsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vault:index/getSysConfigCors:getSysConfigCors", {
    }, opts);
}

/**
 * A collection of values returned by getSysConfigCors.
 */
export interface GetSysConfigCorsResult {
    /**
     * Set of additional custom headers allowed on cross-origin requests. Returns an empty set if CORS is disabled or no custom headers are configured. This only includes custom headers that were explicitly configured, not the standard Vault headers (Content-Type, X-Requested-With, X-Vault-AWS-IAM-Server-ID, X-Vault-MFA, X-Vault-No-Request-Forwarding, X-Vault-Wrap-Format, X-Vault-Wrap-TTL, X-Vault-Policy-Override, Authorization, X-Vault-Token) that are automatically included when CORS is enabled.
     */
    readonly allowedHeaders: string[];
    /**
     * Set of origins permitted to make cross-origin requests. Returns an empty set if CORS is disabled.
     */
    readonly allowedOrigins: string[];
    /**
     * Whether CORS is currently enabled.
     */
    readonly enabled: boolean;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
}
/**
 * Reads the current CORS (Cross-Origin Resource Sharing) configuration from Vault.
 *
 * > **Important** This data source reads from the root namespace only.
 *
 * **Note** This feature is available in Vault 1.14+
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const current = vault.getSysConfigCors({});
 * export const corsEnabled = current.then(current => current.enabled);
 * export const allowedOrigins = current.then(current => current.allowedOrigins);
 * export const allowedHeaders = current.then(current => current.allowedHeaders);
 * ```
 *
 * ### Using with resource
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const production = new vault.SysConfigCors("production", {
 *     allowedOrigins: [
 *         "https://app.example.com",
 *         "https://admin.example.com",
 *         "https://api.example.com",
 *     ],
 *     allowedHeaders: [
 *         "X-Custom-Header",
 *         "X-Request-ID",
 *         "X-Application-Version",
 *     ],
 * });
 * const current = vault.getSysConfigCors({});
 * export const corsConfiguration = {
 *     enabled: current.then(current => current.enabled),
 *     allowedOrigins: current.then(current => current.allowedOrigins),
 *     allowedHeaders: current.then(current => current.allowedHeaders),
 * };
 * ```
 *
 * ## API Documentation
 *
 * For more information on the Vault CORS configuration API, see the [Vault API documentation](https://developer.hashicorp.com/vault/api-docs/system/config-cors).
 */
export function getSysConfigCorsOutput(opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetSysConfigCorsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vault:index/getSysConfigCors:getSysConfigCors", {
    }, opts);
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages the configuration for the [CloudFoundry (CF) auth method](https://developer.hashicorp.com/vault/docs/auth/cf) in Vault.
 *
 * > **Important** The `cfPasswordWo` field is write-only and will never be
 * stored in Terraform state. The password is sent to Vault on every update.
 * See Ephemeral Attributes Reference below.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const cf = new vault.AuthBackend("cf", {
 *     type: "cf",
 *     path: "cf",
 * });
 * const config = new vault.cf.AuthBackendConfig("config", {
 *     mount: cf.path,
 *     identityCaCertificates: [std.file({
 *         input: "ca.pem",
 *     }).then(invoke => std.trimspace({
 *         input: invoke.result,
 *     })).then(invoke => invoke.result)],
 *     cfApiAddr: "https://api.my-cf.example.com",
 *     cfUsername: "admin",
 *     cfPasswordWo: cfPassword,
 *     cfPasswordWoVersion: 1,
 *     loginMaxSecondsNotBefore: 300,
 * });
 * ```
 *
 * ## Ephemeral Attributes Reference
 *
 * The following write-only attributes are supported:
 *
 * * `cfPasswordWo` - (Required) The password for authenticating to the CF API.
 * This attribute is write-only and is never stored in Terraform state. It is sent
 * to Vault on create and on every update. Because Terraform cannot detect changes
 * to write-only values, increment `cfPasswordWoVersion` to trigger an update
 * when only this password changes.
 *
 * * `cfPasswordWoVersion` - (Required) Version counter for `cfPasswordWo`.
 * Increment this value to trigger an update when only the write-only password changes.
 *
 * ## Import
 *
 * CF auth backend configs can be imported using `auth/`, the `mount` path, and
 * `/config`, e.g.
 *
 * ```sh
 * $ pulumi import vault:cf/authBackendConfig:AuthBackendConfig config auth/cf/config
 * ```
 *
 * The namespace can be set using the environment variable `TERRAFORM_VAULT_NAMESPACE`.
 */
export class AuthBackendConfig extends pulumi.CustomResource {
    /**
     * Get an existing AuthBackendConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AuthBackendConfigState, opts?: pulumi.CustomResourceOptions): AuthBackendConfig {
        return new AuthBackendConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:cf/authBackendConfig:AuthBackendConfig';

    /**
     * Returns true if the given object is an instance of AuthBackendConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AuthBackendConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AuthBackendConfig.__pulumiType;
    }

    /**
     * The full API address of the CF deployment, used to
     * verify that a given `CF_INSTANCE_CERT` references an application, space, and
     * organization that currently exist.
     */
    declare public readonly cfApiAddr: pulumi.Output<string>;
    /**
     * A set of PEM-encoded certificates
     * presented by the CF API. Configures Vault to trust these certificates when
     * making API calls.
     */
    declare public readonly cfApiTrustedCertificates: pulumi.Output<string[] | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The password for authenticating to the CF API. This attribute is write-only and is never stored in Terraform state.
     */
    declare public readonly cfPasswordWo: pulumi.Output<string>;
    /**
     * Version counter for 'cf_password_wo'. Increment this value to trigger an update when only the write-only password changes.
     */
    declare public readonly cfPasswordWoVersion: pulumi.Output<number>;
    /**
     * The timeout for CF API calls in seconds. Defaults to
     * `0` (no timeout). Removing this field from your configuration resets the value
     * to `0` in Vault. Requires Vault 1.19.4 or later due to a bug fix in that release.
     */
    declare public readonly cfTimeout: pulumi.Output<number | undefined>;
    /**
     * The username for authenticating to the CF API.
     */
    declare public readonly cfUsername: pulumi.Output<string>;
    /**
     * A set of root CA PEM certificates used
     * to verify that `CF_INSTANCE_CERT` presented at login was issued by the proper
     * authority.
     */
    declare public readonly identityCaCertificates: pulumi.Output<string[]>;
    /**
     * The maximum number of
     * seconds in the future when a login signature could have been created. Defaults
     * to `60`. Because this field is `Computed`, removing it from your configuration
     * does **not** reset the value in Vault — Vault retains whatever was previously
     * set. To reset to the default, set the field explicitly to `60`.
     */
    declare public readonly loginMaxSecondsNotAfter: pulumi.Output<number>;
    /**
     * The maximum number of
     * seconds in the past when a login signature could have been created. Defaults
     * to `300`. Because this field is `Computed`, removing it from your configuration
     * does **not** reset the value in Vault — Vault retains whatever was previously
     * set. To reset to the default, set the field explicitly to `300`.
     */
    declare public readonly loginMaxSecondsNotBefore: pulumi.Output<number>;
    /**
     * The mount path for the CF auth engine in Vault.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;

    /**
     * Create a AuthBackendConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AuthBackendConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AuthBackendConfigArgs | AuthBackendConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AuthBackendConfigState | undefined;
            resourceInputs["cfApiAddr"] = state?.cfApiAddr;
            resourceInputs["cfApiTrustedCertificates"] = state?.cfApiTrustedCertificates;
            resourceInputs["cfPasswordWo"] = state?.cfPasswordWo;
            resourceInputs["cfPasswordWoVersion"] = state?.cfPasswordWoVersion;
            resourceInputs["cfTimeout"] = state?.cfTimeout;
            resourceInputs["cfUsername"] = state?.cfUsername;
            resourceInputs["identityCaCertificates"] = state?.identityCaCertificates;
            resourceInputs["loginMaxSecondsNotAfter"] = state?.loginMaxSecondsNotAfter;
            resourceInputs["loginMaxSecondsNotBefore"] = state?.loginMaxSecondsNotBefore;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["namespace"] = state?.namespace;
        } else {
            const args = argsOrState as AuthBackendConfigArgs | undefined;
            if (args?.cfApiAddr === undefined && !opts.urn) {
                throw new Error("Missing required property 'cfApiAddr'");
            }
            if (args?.cfPasswordWo === undefined && !opts.urn) {
                throw new Error("Missing required property 'cfPasswordWo'");
            }
            if (args?.cfPasswordWoVersion === undefined && !opts.urn) {
                throw new Error("Missing required property 'cfPasswordWoVersion'");
            }
            if (args?.cfUsername === undefined && !opts.urn) {
                throw new Error("Missing required property 'cfUsername'");
            }
            if (args?.identityCaCertificates === undefined && !opts.urn) {
                throw new Error("Missing required property 'identityCaCertificates'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            resourceInputs["cfApiAddr"] = args?.cfApiAddr;
            resourceInputs["cfApiTrustedCertificates"] = args?.cfApiTrustedCertificates;
            resourceInputs["cfPasswordWo"] = args?.cfPasswordWo ? pulumi.secret(args.cfPasswordWo) : undefined;
            resourceInputs["cfPasswordWoVersion"] = args?.cfPasswordWoVersion;
            resourceInputs["cfTimeout"] = args?.cfTimeout;
            resourceInputs["cfUsername"] = args?.cfUsername;
            resourceInputs["identityCaCertificates"] = args?.identityCaCertificates;
            resourceInputs["loginMaxSecondsNotAfter"] = args?.loginMaxSecondsNotAfter;
            resourceInputs["loginMaxSecondsNotBefore"] = args?.loginMaxSecondsNotBefore;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["namespace"] = args?.namespace;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["cfPasswordWo"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(AuthBackendConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AuthBackendConfig resources.
 */
export interface AuthBackendConfigState {
    /**
     * The full API address of the CF deployment, used to
     * verify that a given `CF_INSTANCE_CERT` references an application, space, and
     * organization that currently exist.
     */
    cfApiAddr?: pulumi.Input<string | undefined>;
    /**
     * A set of PEM-encoded certificates
     * presented by the CF API. Configures Vault to trust these certificates when
     * making API calls.
     */
    cfApiTrustedCertificates?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The password for authenticating to the CF API. This attribute is write-only and is never stored in Terraform state.
     */
    cfPasswordWo?: pulumi.Input<string | undefined>;
    /**
     * Version counter for 'cf_password_wo'. Increment this value to trigger an update when only the write-only password changes.
     */
    cfPasswordWoVersion?: pulumi.Input<number | undefined>;
    /**
     * The timeout for CF API calls in seconds. Defaults to
     * `0` (no timeout). Removing this field from your configuration resets the value
     * to `0` in Vault. Requires Vault 1.19.4 or later due to a bug fix in that release.
     */
    cfTimeout?: pulumi.Input<number | undefined>;
    /**
     * The username for authenticating to the CF API.
     */
    cfUsername?: pulumi.Input<string | undefined>;
    /**
     * A set of root CA PEM certificates used
     * to verify that `CF_INSTANCE_CERT` presented at login was issued by the proper
     * authority.
     */
    identityCaCertificates?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The maximum number of
     * seconds in the future when a login signature could have been created. Defaults
     * to `60`. Because this field is `Computed`, removing it from your configuration
     * does **not** reset the value in Vault — Vault retains whatever was previously
     * set. To reset to the default, set the field explicitly to `60`.
     */
    loginMaxSecondsNotAfter?: pulumi.Input<number | undefined>;
    /**
     * The maximum number of
     * seconds in the past when a login signature could have been created. Defaults
     * to `300`. Because this field is `Computed`, removing it from your configuration
     * does **not** reset the value in Vault — Vault retains whatever was previously
     * set. To reset to the default, set the field explicitly to `300`.
     */
    loginMaxSecondsNotBefore?: pulumi.Input<number | undefined>;
    /**
     * The mount path for the CF auth engine in Vault.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a AuthBackendConfig resource.
 */
export interface AuthBackendConfigArgs {
    /**
     * The full API address of the CF deployment, used to
     * verify that a given `CF_INSTANCE_CERT` references an application, space, and
     * organization that currently exist.
     */
    cfApiAddr: pulumi.Input<string>;
    /**
     * A set of PEM-encoded certificates
     * presented by the CF API. Configures Vault to trust these certificates when
     * making API calls.
     */
    cfApiTrustedCertificates?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The password for authenticating to the CF API. This attribute is write-only and is never stored in Terraform state.
     */
    cfPasswordWo: pulumi.Input<string>;
    /**
     * Version counter for 'cf_password_wo'. Increment this value to trigger an update when only the write-only password changes.
     */
    cfPasswordWoVersion: pulumi.Input<number>;
    /**
     * The timeout for CF API calls in seconds. Defaults to
     * `0` (no timeout). Removing this field from your configuration resets the value
     * to `0` in Vault. Requires Vault 1.19.4 or later due to a bug fix in that release.
     */
    cfTimeout?: pulumi.Input<number | undefined>;
    /**
     * The username for authenticating to the CF API.
     */
    cfUsername: pulumi.Input<string>;
    /**
     * A set of root CA PEM certificates used
     * to verify that `CF_INSTANCE_CERT` presented at login was issued by the proper
     * authority.
     */
    identityCaCertificates: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The maximum number of
     * seconds in the future when a login signature could have been created. Defaults
     * to `60`. Because this field is `Computed`, removing it from your configuration
     * does **not** reset the value in Vault — Vault retains whatever was previously
     * set. To reset to the default, set the field explicitly to `60`.
     */
    loginMaxSecondsNotAfter?: pulumi.Input<number | undefined>;
    /**
     * The maximum number of
     * seconds in the past when a login signature could have been created. Defaults
     * to `300`. Because this field is `Computed`, removing it from your configuration
     * does **not** reset the value in Vault — Vault retains whatever was previously
     * set. To reset to the default, set the field explicitly to `300`.
     */
    loginMaxSecondsNotBefore?: pulumi.Input<number | undefined>;
    /**
     * The mount path for the CF auth engine in Vault.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Configure the SPIFFE trust domain and associated trust bundle for the backend
 *
 * > **Important** All data provided in the resource configuration will be
 * written in cleartext to state and plan files generated by Terraform, and
 * will appear in the console output when Terraform runs. Protect these
 * artifacts accordingly. See
 * the main provider documentation
 * for more details.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const spiffeMount = new vault.AuthBackend("spiffe_mount", {
 *     type: "spiffe",
 *     path: "spiffe",
 *     tune: {
 *         passthroughRequestHeaders: ["Authorization"],
 *     },
 * });
 * const spiffeConfig = new vault.SpiffeAuthBackendConfig("spiffe_config", {
 *     mount: spiffeMount.path,
 *     trustDomain: "example.org",
 *     profile: "https_web_bundle",
 *     endpointUrl: "https://example.org:8443/",
 * });
 * ```
 *
 * ## Import
 *
 * The SPIFFE config can be imported using the resource's `id`.
 * In the case of the example above the `id` would be `auth/spiffe/config`,
 * where the `spiffe` component is the resource's `mount`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/spiffeAuthBackendConfig:SpiffeAuthBackendConfig spiffe_config auth/spiffe/config
 * ```
 */
export class SpiffeAuthBackendConfig extends pulumi.CustomResource {
    /**
     * Get an existing SpiffeAuthBackendConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SpiffeAuthBackendConfigState, opts?: pulumi.CustomResourceOptions): SpiffeAuthBackendConfig {
        return new SpiffeAuthBackendConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/spiffeAuthBackendConfig:SpiffeAuthBackendConfig';

    /**
     * Returns true if the given object is an instance of SpiffeAuthBackendConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SpiffeAuthBackendConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SpiffeAuthBackendConfig.__pulumiType;
    }

    /**
     * A list of allowed audience values for JWT based SVIDs.
     */
    declare public readonly audiences: pulumi.Output<string[] | undefined>;
    /**
     * A PEM encoded X.509 certificate or a JWKS document based on what
     * the `profile` argument is set.
     */
    declare public readonly bundle: pulumi.Output<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * If true, tells Vault not to fetch the remote trust
     * bundle to validate the configuration
     */
    declare public readonly deferBundleFetch: pulumi.Output<boolean | undefined>;
    /**
     * A PEM encoded CA certificate to use
     * to validate the TLS connection to the `endpointUrl` when the `profile` argument
     * is set to `httpsWebBundle` or `httpsWebPem`.
     */
    declare public readonly endpointRootCaTruststorePem: pulumi.Output<string | undefined>;
    /**
     * The SPIFFE ID to expect in the TLS certificate when
     * the `profile` argument is set to `httpsSpiffeBundle`.
     */
    declare public readonly endpointSpiffeId: pulumi.Output<string | undefined>;
    /**
     * The URL to fetch the trust bundle from. Required if the
     * `profile` argument is set to `httpsSpiffeBundle`, `httpsWebBundle`, or
     * `httpsWebPem`.
     */
    declare public readonly endpointUrl: pulumi.Output<string | undefined>;
    /**
     * The PKI secret backend the resource belongs to.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * Sets the profile type and fetch mechanism for the profile
     * used to fetch the trust bundle. Must be one of:
     * * httpsSpiffeBundle - fetch the trust bundle in JWKS format from a SPIFFE endpoint.
     * * httpsWebBundle - fetch the trust bundle in JWKS format from an HTTPS endpoint.
     * * httpsWebPem - fetch a valid X.509 certificate as the trust bundle from an HTTPS endpoint.
     * * static - use the trust bundle explicitly configured in the profile definition.
     */
    declare public readonly profile: pulumi.Output<string>;
    /**
     * The SPIFFE trust domain used by the backend.
     */
    declare public readonly trustDomain: pulumi.Output<string>;

    /**
     * Create a SpiffeAuthBackendConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SpiffeAuthBackendConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SpiffeAuthBackendConfigArgs | SpiffeAuthBackendConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SpiffeAuthBackendConfigState | undefined;
            resourceInputs["audiences"] = state?.audiences;
            resourceInputs["bundle"] = state?.bundle;
            resourceInputs["deferBundleFetch"] = state?.deferBundleFetch;
            resourceInputs["endpointRootCaTruststorePem"] = state?.endpointRootCaTruststorePem;
            resourceInputs["endpointSpiffeId"] = state?.endpointSpiffeId;
            resourceInputs["endpointUrl"] = state?.endpointUrl;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["profile"] = state?.profile;
            resourceInputs["trustDomain"] = state?.trustDomain;
        } else {
            const args = argsOrState as SpiffeAuthBackendConfigArgs | undefined;
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            if (args?.profile === undefined && !opts.urn) {
                throw new Error("Missing required property 'profile'");
            }
            if (args?.trustDomain === undefined && !opts.urn) {
                throw new Error("Missing required property 'trustDomain'");
            }
            resourceInputs["audiences"] = args?.audiences;
            resourceInputs["bundle"] = args?.bundle;
            resourceInputs["deferBundleFetch"] = args?.deferBundleFetch ? pulumi.secret(args.deferBundleFetch) : undefined;
            resourceInputs["endpointRootCaTruststorePem"] = args?.endpointRootCaTruststorePem;
            resourceInputs["endpointSpiffeId"] = args?.endpointSpiffeId;
            resourceInputs["endpointUrl"] = args?.endpointUrl;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["profile"] = args?.profile;
            resourceInputs["trustDomain"] = args?.trustDomain;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["deferBundleFetch"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(SpiffeAuthBackendConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SpiffeAuthBackendConfig resources.
 */
export interface SpiffeAuthBackendConfigState {
    /**
     * A list of allowed audience values for JWT based SVIDs.
     */
    audiences?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * A PEM encoded X.509 certificate or a JWKS document based on what
     * the `profile` argument is set.
     */
    bundle?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * If true, tells Vault not to fetch the remote trust
     * bundle to validate the configuration
     */
    deferBundleFetch?: pulumi.Input<boolean | undefined>;
    /**
     * A PEM encoded CA certificate to use
     * to validate the TLS connection to the `endpointUrl` when the `profile` argument
     * is set to `httpsWebBundle` or `httpsWebPem`.
     */
    endpointRootCaTruststorePem?: pulumi.Input<string | undefined>;
    /**
     * The SPIFFE ID to expect in the TLS certificate when
     * the `profile` argument is set to `httpsSpiffeBundle`.
     */
    endpointSpiffeId?: pulumi.Input<string | undefined>;
    /**
     * The URL to fetch the trust bundle from. Required if the
     * `profile` argument is set to `httpsSpiffeBundle`, `httpsWebBundle`, or
     * `httpsWebPem`.
     */
    endpointUrl?: pulumi.Input<string | undefined>;
    /**
     * The PKI secret backend the resource belongs to.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Sets the profile type and fetch mechanism for the profile
     * used to fetch the trust bundle. Must be one of:
     * * httpsSpiffeBundle - fetch the trust bundle in JWKS format from a SPIFFE endpoint.
     * * httpsWebBundle - fetch the trust bundle in JWKS format from an HTTPS endpoint.
     * * httpsWebPem - fetch a valid X.509 certificate as the trust bundle from an HTTPS endpoint.
     * * static - use the trust bundle explicitly configured in the profile definition.
     */
    profile?: pulumi.Input<string | undefined>;
    /**
     * The SPIFFE trust domain used by the backend.
     */
    trustDomain?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SpiffeAuthBackendConfig resource.
 */
export interface SpiffeAuthBackendConfigArgs {
    /**
     * A list of allowed audience values for JWT based SVIDs.
     */
    audiences?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * A PEM encoded X.509 certificate or a JWKS document based on what
     * the `profile` argument is set.
     */
    bundle?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * If true, tells Vault not to fetch the remote trust
     * bundle to validate the configuration
     */
    deferBundleFetch?: pulumi.Input<boolean | undefined>;
    /**
     * A PEM encoded CA certificate to use
     * to validate the TLS connection to the `endpointUrl` when the `profile` argument
     * is set to `httpsWebBundle` or `httpsWebPem`.
     */
    endpointRootCaTruststorePem?: pulumi.Input<string | undefined>;
    /**
     * The SPIFFE ID to expect in the TLS certificate when
     * the `profile` argument is set to `httpsSpiffeBundle`.
     */
    endpointSpiffeId?: pulumi.Input<string | undefined>;
    /**
     * The URL to fetch the trust bundle from. Required if the
     * `profile` argument is set to `httpsSpiffeBundle`, `httpsWebBundle`, or
     * `httpsWebPem`.
     */
    endpointUrl?: pulumi.Input<string | undefined>;
    /**
     * The PKI secret backend the resource belongs to.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Sets the profile type and fetch mechanism for the profile
     * used to fetch the trust bundle. Must be one of:
     * * httpsSpiffeBundle - fetch the trust bundle in JWKS format from a SPIFFE endpoint.
     * * httpsWebBundle - fetch the trust bundle in JWKS format from an HTTPS endpoint.
     * * httpsWebPem - fetch a valid X.509 certificate as the trust bundle from an HTTPS endpoint.
     * * static - use the trust bundle explicitly configured in the profile definition.
     */
    profile: pulumi.Input<string>;
    /**
     * The SPIFFE trust domain used by the backend.
     */
    trustDomain: pulumi.Input<string>;
}

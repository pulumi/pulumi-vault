// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Configure the SPIFFE trust domain.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const spiffeSecrets = new vault.Mount("spiffe_secrets", {
 *     path: "spiffe",
 *     type: "spiffe",
 * });
 * const spiffeConfig = new vault.SpiffeSecretBackendConfig("spiffe_config", {
 *     mount: spiffeSecrets.path,
 *     trustDomain: "example.com",
 * });
 * ```
 *
 * ## Import
 *
 * The SPIFFE config can be imported using the resource's `id`.
 * In the case of the example above the `id` would be `spiffe/config`,
 * where the `spiffe` component is the resource's `mount`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/spiffeSecretBackendConfig:SpiffeSecretBackendConfig spiffe_config spiffe/config
 * ```
 */
export class SpiffeSecretBackendConfig extends pulumi.CustomResource {
    /**
     * Get an existing SpiffeSecretBackendConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SpiffeSecretBackendConfigState, opts?: pulumi.CustomResourceOptions): SpiffeSecretBackendConfig {
        return new SpiffeSecretBackendConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/spiffeSecretBackendConfig:SpiffeSecretBackendConfig';

    /**
     * Returns true if the given object is an instance of SpiffeSecretBackendConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SpiffeSecretBackendConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SpiffeSecretBackendConfig.__pulumiType;
    }

    /**
     * The value to put as the refresh hint in trust bundles we publish.
     * May not exceed key_lifetime/10. Uses
     * [duration](https://developer.hashicorp.com/vault/docs/concepts/duration-format) format strings.
     */
    declare public readonly bundleRefreshHint: pulumi.Output<string>;
    /**
     * The base URL to use for JWT issuer claims (`iss`), including
     * `https://` schema, host, and optional port. Must be reachable by whatever systems consume the JWTs.
     */
    declare public readonly jwtIssuerUrl: pulumi.Output<string>;
    /**
     * When set true, attempts to generate JWT SVIDs will fail if the resulting
     * SPIFFEID exceeds 255 chars, the limit for JWT sub claims in OIDC.
     */
    declare public readonly jwtOidcCompatibilityMode: pulumi.Output<boolean>;
    /**
     * Signing algorithm to use. Allowed values are: RS256 (default),
     * RS384, RS512, ES256, ES384, ES512.
     */
    declare public readonly jwtSigningAlgorithm: pulumi.Output<string>;
    /**
     * How often to generate a new signing key. Uses
     * [duration](https://developer.hashicorp.com/vault/docs/concepts/duration-format) format strings.
     */
    declare public readonly keyLifetime: pulumi.Output<string>;
    /**
     * The PKI secret backend the resource belongs to.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * The SPIFFE trust domain used by the backend.
     */
    declare public readonly trustDomain: pulumi.Output<string>;

    /**
     * Create a SpiffeSecretBackendConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SpiffeSecretBackendConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SpiffeSecretBackendConfigArgs | SpiffeSecretBackendConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SpiffeSecretBackendConfigState | undefined;
            resourceInputs["bundleRefreshHint"] = state?.bundleRefreshHint;
            resourceInputs["jwtIssuerUrl"] = state?.jwtIssuerUrl;
            resourceInputs["jwtOidcCompatibilityMode"] = state?.jwtOidcCompatibilityMode;
            resourceInputs["jwtSigningAlgorithm"] = state?.jwtSigningAlgorithm;
            resourceInputs["keyLifetime"] = state?.keyLifetime;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["trustDomain"] = state?.trustDomain;
        } else {
            const args = argsOrState as SpiffeSecretBackendConfigArgs | undefined;
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            if (args?.trustDomain === undefined && !opts.urn) {
                throw new Error("Missing required property 'trustDomain'");
            }
            resourceInputs["bundleRefreshHint"] = args?.bundleRefreshHint;
            resourceInputs["jwtIssuerUrl"] = args?.jwtIssuerUrl;
            resourceInputs["jwtOidcCompatibilityMode"] = args?.jwtOidcCompatibilityMode;
            resourceInputs["jwtSigningAlgorithm"] = args?.jwtSigningAlgorithm;
            resourceInputs["keyLifetime"] = args?.keyLifetime;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["trustDomain"] = args?.trustDomain;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SpiffeSecretBackendConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SpiffeSecretBackendConfig resources.
 */
export interface SpiffeSecretBackendConfigState {
    /**
     * The value to put as the refresh hint in trust bundles we publish.
     * May not exceed key_lifetime/10. Uses
     * [duration](https://developer.hashicorp.com/vault/docs/concepts/duration-format) format strings.
     */
    bundleRefreshHint?: pulumi.Input<string | undefined>;
    /**
     * The base URL to use for JWT issuer claims (`iss`), including
     * `https://` schema, host, and optional port. Must be reachable by whatever systems consume the JWTs.
     */
    jwtIssuerUrl?: pulumi.Input<string | undefined>;
    /**
     * When set true, attempts to generate JWT SVIDs will fail if the resulting
     * SPIFFEID exceeds 255 chars, the limit for JWT sub claims in OIDC.
     */
    jwtOidcCompatibilityMode?: pulumi.Input<boolean | undefined>;
    /**
     * Signing algorithm to use. Allowed values are: RS256 (default),
     * RS384, RS512, ES256, ES384, ES512.
     */
    jwtSigningAlgorithm?: pulumi.Input<string | undefined>;
    /**
     * How often to generate a new signing key. Uses
     * [duration](https://developer.hashicorp.com/vault/docs/concepts/duration-format) format strings.
     */
    keyLifetime?: pulumi.Input<string | undefined>;
    /**
     * The PKI secret backend the resource belongs to.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * The SPIFFE trust domain used by the backend.
     */
    trustDomain?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SpiffeSecretBackendConfig resource.
 */
export interface SpiffeSecretBackendConfigArgs {
    /**
     * The value to put as the refresh hint in trust bundles we publish.
     * May not exceed key_lifetime/10. Uses
     * [duration](https://developer.hashicorp.com/vault/docs/concepts/duration-format) format strings.
     */
    bundleRefreshHint?: pulumi.Input<string | undefined>;
    /**
     * The base URL to use for JWT issuer claims (`iss`), including
     * `https://` schema, host, and optional port. Must be reachable by whatever systems consume the JWTs.
     */
    jwtIssuerUrl?: pulumi.Input<string | undefined>;
    /**
     * When set true, attempts to generate JWT SVIDs will fail if the resulting
     * SPIFFEID exceeds 255 chars, the limit for JWT sub claims in OIDC.
     */
    jwtOidcCompatibilityMode?: pulumi.Input<boolean | undefined>;
    /**
     * Signing algorithm to use. Allowed values are: RS256 (default),
     * RS384, RS512, ES256, ES384, ES512.
     */
    jwtSigningAlgorithm?: pulumi.Input<string | undefined>;
    /**
     * How often to generate a new signing key. Uses
     * [duration](https://developer.hashicorp.com/vault/docs/concepts/duration-format) format strings.
     */
    keyLifetime?: pulumi.Input<string | undefined>;
    /**
     * The PKI secret backend the resource belongs to.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * The SPIFFE trust domain used by the backend.
     */
    trustDomain: pulumi.Input<string>;
}

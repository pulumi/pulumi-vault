// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages an AWS KMS provider in the Vault Key Management secrets engine. This resource configures Vault to integrate with AWS Key Management Service, allowing keys created in Vault to be distributed to AWS KMS for use in AWS services.
 *
 * Once configured, keys can be distributed to AWS KMS using the `vault.KeymgmtDistributeKey` resource.
 *
 * > **Important** This resource requires **Terraform 1.11+** for write-only attribute support.
 * The `credentialsWo` field is write-only and will never be stored in Terraform state.
 * See the main provider documentation for more details.
 *
 * For more information on managing AWS KMS with Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 *
 * **Note** this feature is available only with Vault Enterprise.
 *
 * ## Example Usage
 *
 * ### Basic Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const keymgmt = new vault.Mount("keymgmt", {
 *     path: "keymgmt",
 *     type: "keymgmt",
 * });
 * const usWest = new vault.KeymgmtAwsKms("us_west", {
 *     mount: keymgmt.path,
 *     name: "aws-us-west-2",
 *     keyCollection: "us-west-2",
 *     credentialsWo: {
 *         access_key: awsAccessKeyId,
 *         secret_key: awsSecretAccessKey,
 *     },
 *     credentialsWoVersion: 1,
 * });
 * ```
 *
 * ### Using AWS Environment Variables or IAM Roles
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * // When credentials are not provided, Vault will use AWS SDK's credential chain:
 * // 1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_SESSION_TOKEN)
 * // 2. Shared credentials file (~/.aws/credentials)
 * // 3. IAM instance profile (when running on EC2)
 * // 4. ECS task credentials (when running on ECS)
 * const production = new vault.KeymgmtAwsKms("production", {
 *     mount: keymgmt.path,
 *     name: "aws-production",
 *     keyCollection: "us-east-1",
 * });
 * // Distribute a key to AWS KMS
 * const encryptionKey = new vault.KeymgmtKey("encryption_key", {
 *     mount: keymgmt.path,
 *     name: "aws-encryption-key",
 *     type: "aes256-gcm96",
 * });
 * const awsDist = new vault.KeymgmtDistributeKey("aws_dist", {
 *     path: keymgmt.path,
 *     kmsName: production.name,
 *     keyName: encryptionKey.name,
 *     purposes: [
 *         "encrypt",
 *         "decrypt",
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * AWS KMS providers can be imported using the format `{path}/kms/{name}`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/keymgmtAwsKms:KeymgmtAwsKms us_west keymgmt/kms/aws-us-west-2
 * ```
 *
 * > **Note:** Import sets the `mount` attribute from the import ID. The `credentialsWo` and `credentialsWoVersion` fields will not be populated as they are not returned by the Vault API. You must supply these values in your configuration after import. The corresponding `vault.Mount` resource must also be present in your configuration (or separately imported).
 */
export class KeymgmtAwsKms extends pulumi.CustomResource {
    /**
     * Get an existing KeymgmtAwsKms resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KeymgmtAwsKmsState, opts?: pulumi.CustomResourceOptions): KeymgmtAwsKms {
        return new KeymgmtAwsKms(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/keymgmtAwsKms:KeymgmtAwsKms';

    /**
     * Returns true if the given object is an instance of KeymgmtAwsKms.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KeymgmtAwsKms {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KeymgmtAwsKms.__pulumiType;
    }

    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The credentials to use for authentication with AWS KMS. Supplying values for this parameter is optional, as credentials may also be specified as environment variables. Credentials provided to this parameter will take precedence over credentials provided via environment variables. This value is write-only and will not be stored in Terraform state.
     * The following values are supported:
     */
    declare public readonly credentialsWo: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * Version number for the write-only credentials. Increment this value to trigger a credential rotation. Changing this value will cause the credentials to be re-sent to Vault during the next apply. For more info see updating write-only attributes.
     */
    declare public readonly credentialsWoVersion: pulumi.Output<number | undefined>;
    /**
     * Refers to the name of an AWS region. Cannot be changed after creation.
     */
    declare public readonly keyCollection: pulumi.Output<string>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * Specifies the name of the AWS KMS provider. Cannot be changed after creation.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;

    /**
     * Create a KeymgmtAwsKms resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KeymgmtAwsKmsArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KeymgmtAwsKmsArgs | KeymgmtAwsKmsState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KeymgmtAwsKmsState | undefined;
            resourceInputs["credentialsWo"] = state?.credentialsWo;
            resourceInputs["credentialsWoVersion"] = state?.credentialsWoVersion;
            resourceInputs["keyCollection"] = state?.keyCollection;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
        } else {
            const args = argsOrState as KeymgmtAwsKmsArgs | undefined;
            if (args?.keyCollection === undefined && !opts.urn) {
                throw new Error("Missing required property 'keyCollection'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            resourceInputs["credentialsWo"] = args?.credentialsWo ? pulumi.secret(args.credentialsWo) : undefined;
            resourceInputs["credentialsWoVersion"] = args?.credentialsWoVersion;
            resourceInputs["keyCollection"] = args?.keyCollection;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["credentialsWo"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(KeymgmtAwsKms.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KeymgmtAwsKms resources.
 */
export interface KeymgmtAwsKmsState {
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The credentials to use for authentication with AWS KMS. Supplying values for this parameter is optional, as credentials may also be specified as environment variables. Credentials provided to this parameter will take precedence over credentials provided via environment variables. This value is write-only and will not be stored in Terraform state.
     * The following values are supported:
     */
    credentialsWo?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * Version number for the write-only credentials. Increment this value to trigger a credential rotation. Changing this value will cause the credentials to be re-sent to Vault during the next apply. For more info see updating write-only attributes.
     */
    credentialsWoVersion?: pulumi.Input<number | undefined>;
    /**
     * Refers to the name of an AWS region. Cannot be changed after creation.
     */
    keyCollection?: pulumi.Input<string | undefined>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * Specifies the name of the AWS KMS provider. Cannot be changed after creation.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a KeymgmtAwsKms resource.
 */
export interface KeymgmtAwsKmsArgs {
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The credentials to use for authentication with AWS KMS. Supplying values for this parameter is optional, as credentials may also be specified as environment variables. Credentials provided to this parameter will take precedence over credentials provided via environment variables. This value is write-only and will not be stored in Terraform state.
     * The following values are supported:
     */
    credentialsWo?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * Version number for the write-only credentials. Increment this value to trigger a credential rotation. Changing this value will cause the credentials to be re-sent to Vault during the next apply. For more info see updating write-only attributes.
     */
    credentialsWoVersion?: pulumi.Input<number | undefined>;
    /**
     * Refers to the name of an AWS region. Cannot be changed after creation.
     */
    keyCollection: pulumi.Input<string>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount: pulumi.Input<string>;
    /**
     * Specifies the name of the AWS KMS provider. Cannot be changed after creation.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages key replication across multiple AWS regions in AWS KMS. This resource enables AWS KMS multi-region keys, allowing the same key material to be used across different AWS regions for high availability and disaster recovery.
 *
 * **Note:** This resource is only applicable to AWS KMS and does not work with Azure Key Vault or GCP Cloud KMS.
 *
 * When you replicate a key to AWS KMS, it becomes a multi-region key that can be used for encryption and decryption operations in multiple AWS regions while maintaining the same key material.
 *
 * > **Important**
 * All data provided in the resource configuration will be written in cleartext to
 * state and plan files generated by Terraform, and will appear in the console
 * output when Terraform runs. Protect these artifacts accordingly. See the main provider documentation for more details.
 *
 * For more information on AWS KMS multi-region keys with Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 *
 * **Note** this feature is available only with Vault Enterprise and requires a configured AWS KMS provider.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const keymgmt = new vault.Mount("keymgmt", {
 *     path: "keymgmt",
 *     type: "keymgmt",
 * });
 * const key = new vault.KeymgmtKey("key", {
 *     mount: keymgmt.path,
 *     name: "multi-region-key",
 *     type: "aes256-gcm96",
 *     replicaRegions: [
 *         "us-east-1",
 *         "eu-west-1",
 *     ],
 * });
 * const aws = new vault.KeymgmtAwsKms("aws", {
 *     mount: keymgmt.path,
 *     name: "aws-kms",
 *     keyCollection: "us-west-2",
 *     credentialsWo: {
 *         access_key: awsAccessKeyId,
 *         secret_key: awsSecretAccessKey,
 *     },
 *     credentialsWoVersion: 1,
 * });
 * const dist = new vault.KeymgmtDistributeKey("dist", {
 *     mount: keymgmt.path,
 *     kmsName: aws.name,
 *     keyName: key.name,
 *     purposes: [
 *         "encrypt",
 *         "decrypt",
 *     ],
 * });
 * const replicate = new vault.KeymgmtReplicateKey("replicate", {
 *     mount: keymgmt.path,
 *     kmsName: aws.name,
 *     keyName: key.name,
 * }, {
 *     dependsOn: [dist],
 * });
 * ```
 *
 * ## Import
 *
 * Key replication resources can be imported using the format `{path}/kms/{kms_name}/key/{key_name}/replicate`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/keymgmtReplicateKey:KeymgmtReplicateKey us_east keymgmt/kms/aws-us-west-2/key/multi-region-key/replicate
 * ```
 *
 * **Note**: The key must have `replicaRegions` configured in `vault.KeymgmtKey` and must be distributed to the AWS KMS provider using `vault.KeymgmtDistributeKey` before replication.
 */
export class KeymgmtReplicateKey extends pulumi.CustomResource {
    /**
     * Get an existing KeymgmtReplicateKey resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KeymgmtReplicateKeyState, opts?: pulumi.CustomResourceOptions): KeymgmtReplicateKey {
        return new KeymgmtReplicateKey(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/keymgmtReplicateKey:KeymgmtReplicateKey';

    /**
     * Returns true if the given object is an instance of KeymgmtReplicateKey.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KeymgmtReplicateKey {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KeymgmtReplicateKey.__pulumiType;
    }

    /**
     * Specifies the name of the key to replicate. The key must have `replicaRegions` configured and must already be distributed to the AWS KMS provider.
     */
    declare public readonly keyName: pulumi.Output<string>;
    /**
     * Specifies the name of the AWS KMS provider.
     */
    declare public readonly kmsName: pulumi.Output<string>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;

    /**
     * Create a KeymgmtReplicateKey resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KeymgmtReplicateKeyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KeymgmtReplicateKeyArgs | KeymgmtReplicateKeyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KeymgmtReplicateKeyState | undefined;
            resourceInputs["keyName"] = state?.keyName;
            resourceInputs["kmsName"] = state?.kmsName;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["namespace"] = state?.namespace;
        } else {
            const args = argsOrState as KeymgmtReplicateKeyArgs | undefined;
            if (args?.keyName === undefined && !opts.urn) {
                throw new Error("Missing required property 'keyName'");
            }
            if (args?.kmsName === undefined && !opts.urn) {
                throw new Error("Missing required property 'kmsName'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            resourceInputs["keyName"] = args?.keyName;
            resourceInputs["kmsName"] = args?.kmsName;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["namespace"] = args?.namespace;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KeymgmtReplicateKey.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KeymgmtReplicateKey resources.
 */
export interface KeymgmtReplicateKeyState {
    /**
     * Specifies the name of the key to replicate. The key must have `replicaRegions` configured and must already be distributed to the AWS KMS provider.
     */
    keyName?: pulumi.Input<string | undefined>;
    /**
     * Specifies the name of the AWS KMS provider.
     */
    kmsName?: pulumi.Input<string | undefined>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a KeymgmtReplicateKey resource.
 */
export interface KeymgmtReplicateKeyArgs {
    /**
     * Specifies the name of the key to replicate. The key must have `replicaRegions` configured and must already be distributed to the AWS KMS provider.
     */
    keyName: pulumi.Input<string>;
    /**
     * Specifies the name of the AWS KMS provider.
     */
    kmsName: pulumi.Input<string>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

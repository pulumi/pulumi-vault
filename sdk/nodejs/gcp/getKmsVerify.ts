// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Verifies a digital signature against a digest using a GCP KMS signing key through Vault. This data source
 * performs read-only signature verification operations.
 *
 * ## Example Usage
 *
 * ### Basic Signature Verification
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const gcpkms = new vault.gcp.KmsSecretBackend("gcpkms", {
 *     path: "gcpkms",
 *     credentialsWo: std.file({
 *         input: "gcp-credentials.json",
 *     }).then(invoke => invoke.result),
 *     credentialsWoVersion: 1,
 * });
 * const signingKey = new vault.gcp.KmsSecretBackendKey("signing_key", {
 *     mount: gcpkms.path,
 *     keyName: "signing-key",
 *     keyRing: "projects/my-project/locations/us-central1/keyRings/my-keyring",
 *     purpose: "asymmetric_sign",
 *     algorithm: "rsa_sign_pss_2048_sha256",
 * });
 * const signatureCheck = vault.gcp.getKmsVerifyOutput({
 *     mount: gcpkms.path,
 *     keyName: signingKey.keyName,
 *     digest: std.base64encode({
 *         input: "my message digest",
 *     }).then(invoke => invoke.result),
 *     signature: "BASE64_ENCODED_SIGNATURE",
 *     keyVersion: 1,
 * });
 * export const signatureIsValid = signatureCheck.valid;
 * ```
 */
export function getKmsVerify(args: GetKmsVerifyArgs, opts?: pulumi.InvokeOptions): Promise<GetKmsVerifyResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vault:gcp/getKmsVerify:getKmsVerify", {
        "digest": args.digest,
        "keyName": args.keyName,
        "keyVersion": args.keyVersion,
        "mount": args.mount,
        "namespace": args.namespace,
        "signature": args.signature,
    }, opts);
}

/**
 * A collection of arguments for invoking getKmsVerify.
 */
export interface GetKmsVerifyArgs {
    /**
     * Base64-encoded digest to verify. The digest should be created using the hash algorithm
     * specified in the key's algorithm (e.g., SHA256 for RSA_SIGN_PSS_2048_SHA256).
     */
    digest: string;
    /**
     * Name of the signing key to use for verification. This must reference a key with
     * purpose `ASYMMETRIC_SIGN`.
     */
    keyName: string;
    /**
     * Specific version of the key to use for verification.
     */
    keyVersion: number;
    /**
     * Path where the GCP KMS secrets engine is mounted.
     */
    mount: string;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: string;
    /**
     * Base64-encoded signature to verify against the digest.
     */
    signature: string;
}

/**
 * A collection of values returned by getKmsVerify.
 */
export interface GetKmsVerifyResult {
    readonly digest: string;
    readonly keyName: string;
    readonly keyVersion: number;
    readonly mount: string;
    readonly namespace?: string;
    readonly signature: string;
    /**
     * Boolean indicating whether the signature is valid (`true`) or invalid (`false`).
     */
    readonly valid: boolean;
}
/**
 * Verifies a digital signature against a digest using a GCP KMS signing key through Vault. This data source
 * performs read-only signature verification operations.
 *
 * ## Example Usage
 *
 * ### Basic Signature Verification
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const gcpkms = new vault.gcp.KmsSecretBackend("gcpkms", {
 *     path: "gcpkms",
 *     credentialsWo: std.file({
 *         input: "gcp-credentials.json",
 *     }).then(invoke => invoke.result),
 *     credentialsWoVersion: 1,
 * });
 * const signingKey = new vault.gcp.KmsSecretBackendKey("signing_key", {
 *     mount: gcpkms.path,
 *     keyName: "signing-key",
 *     keyRing: "projects/my-project/locations/us-central1/keyRings/my-keyring",
 *     purpose: "asymmetric_sign",
 *     algorithm: "rsa_sign_pss_2048_sha256",
 * });
 * const signatureCheck = vault.gcp.getKmsVerifyOutput({
 *     mount: gcpkms.path,
 *     keyName: signingKey.keyName,
 *     digest: std.base64encode({
 *         input: "my message digest",
 *     }).then(invoke => invoke.result),
 *     signature: "BASE64_ENCODED_SIGNATURE",
 *     keyVersion: 1,
 * });
 * export const signatureIsValid = signatureCheck.valid;
 * ```
 */
export function getKmsVerifyOutput(args: GetKmsVerifyOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetKmsVerifyResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vault:gcp/getKmsVerify:getKmsVerify", {
        "digest": args.digest,
        "keyName": args.keyName,
        "keyVersion": args.keyVersion,
        "mount": args.mount,
        "namespace": args.namespace,
        "signature": args.signature,
    }, opts);
}

/**
 * A collection of arguments for invoking getKmsVerify.
 */
export interface GetKmsVerifyOutputArgs {
    /**
     * Base64-encoded digest to verify. The digest should be created using the hash algorithm
     * specified in the key's algorithm (e.g., SHA256 for RSA_SIGN_PSS_2048_SHA256).
     */
    digest: pulumi.Input<string>;
    /**
     * Name of the signing key to use for verification. This must reference a key with
     * purpose `ASYMMETRIC_SIGN`.
     */
    keyName: pulumi.Input<string>;
    /**
     * Specific version of the key to use for verification.
     */
    keyVersion: pulumi.Input<number>;
    /**
     * Path where the GCP KMS secrets engine is mounted.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Base64-encoded signature to verify against the digest.
     */
    signature: pulumi.Input<string>;
}

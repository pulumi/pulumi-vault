// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages account configurations in the OS Secrets Engine. Accounts represent operating system user
 * accounts on remote hosts that Vault will manage, including automatic password rotation.
 * This resource requires Vault 2.0.0 or later.
 *
 * > **Important** The `passwordWo` field is write-only, create-only, and will not be read back from Vault.
 * It can only be set during resource creation. To update the password after creation, use the Vault CLI or API
 * to call the reset endpoint directly (see "Password Management" section below).
 * All data provided in the resource configuration will be written in cleartext to state and plan
 * files generated by Terraform. Protect these artifacts accordingly. See
 * the main provider documentation for more details.
 *
 * See the [Vault documentation](https://www.vaultproject.io/docs/secrets/os) for more information.
 *
 * The OS Secrets Engine mount itself is managed separately, typically with `vault.Mount`. This resource manages
 * accounts beneath an existing OS mount and host.
 *
 * Before mounting the OS Secrets Engine, the external OS plugin must already be registered in Vault's plugin catalog.
 * You can register it with the `vault.Plugin` resource.
 *
 * The examples below use the canonical plugin name `vault-plugin-secrets-os`. If your Vault cluster registers the
 * OS plugin under a different catalog name, use that name in `vault_mount.type` instead.
 *
 * ## Example Usage
 *
 * ### Register Plugin And Configure Account
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Plugin("os", {
 *     type: "secret",
 *     name: "vault-plugin-secrets-os",
 *     version: "v0.1.0+ent",
 * });
 * const osMount = new vault.Mount("os", {
 *     path: "os",
 *     type: os.name,
 * });
 * const osOsSecretBackend = new vault.OsSecretBackend("os", {mount: osMount.path});
 * const server = new vault.OsSecretBackendHost("server", {
 *     mount: osOsSecretBackend.mount,
 *     name: "web-server",
 *     address: "192.168.1.100",
 *     port: 22,
 * });
 * const admin = new vault.OsSecretBackendAccount("admin", {
 *     mount: osOsSecretBackend.mount,
 *     host: server.name,
 *     name: "admin-account",
 *     username: "admin",
 *     passwordWo: "initial-secure-password-123",
 * });
 * ```
 *
 * ### Basic Account Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Mount("os", {
 *     path: "os",
 *     type: "vault-plugin-secrets-os",
 * });
 * const osOsSecretBackend = new vault.OsSecretBackend("os", {mount: os.path});
 * const server = new vault.OsSecretBackendHost("server", {
 *     mount: osOsSecretBackend.mount,
 *     name: "web-server",
 *     address: "192.168.1.100",
 *     port: 22,
 * });
 * const admin = new vault.OsSecretBackendAccount("admin", {
 *     mount: osOsSecretBackend.mount,
 *     host: server.name,
 *     name: "admin-account",
 *     username: "admin",
 *     passwordWo: "initial-secure-password-123",
 * });
 * ```
 *
 * ### Account with Rotation Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Mount("os", {
 *     path: "os",
 *     type: "vault-plugin-secrets-os",
 * });
 * const osOsSecretBackend = new vault.OsSecretBackend("os", {mount: os.path});
 * const database = new vault.OsSecretBackendHost("database", {
 *     mount: osOsSecretBackend.mount,
 *     name: "db-server",
 *     address: "10.0.1.50",
 *     port: 22,
 * });
 * const dbadmin = new vault.OsSecretBackendAccount("dbadmin", {
 *     mount: osOsSecretBackend.mount,
 *     host: database.name,
 *     name: "dbadmin",
 *     username: "postgres",
 *     passwordWo: "initial-password-456",
 *     rotationPeriod: 86400,
 *     verifyConnection: false,
 * });
 * ```
 *
 * ### Account with Scheduled Rotation
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Mount("os", {
 *     path: "os",
 *     type: "vault-plugin-secrets-os",
 * });
 * const osOsSecretBackend = new vault.OsSecretBackend("os", {mount: os.path});
 * const appServer = new vault.OsSecretBackendHost("app_server", {
 *     mount: osOsSecretBackend.mount,
 *     name: "app-01",
 *     address: "192.168.1.200",
 *     port: 22,
 * });
 * const service = new vault.OsSecretBackendAccount("service", {
 *     mount: osOsSecretBackend.mount,
 *     host: appServer.name,
 *     name: "service-account",
 *     username: "appuser",
 *     passwordWo: initialPassword,
 *     rotationSchedule: "0 3 * * 0",
 *     rotationWindow: 3600,
 * });
 * ```
 *
 * ## Password Management
 *
 * The `passwordWo` field is **create-only** by design. This aligns with Vault's security model where password updates
 * should be performed through dedicated reset operations rather than general configuration updates.
 *
 * ### Updating Passwords After Creation
 *
 * To update an account password after the resource has been created, use the Vault CLI or API directly:
 *
 * **Using Vault CLI:**
 *
 * **Using Vault API:**
 *
 * **Example:**
 *
 * This approach ensures that password updates are explicit operations separate from other configuration changes,
 * providing better audit trails and security controls.
 *
 * ## Notes
 *
 * * This resource requires Vault 2.0.0 or later.
 * * The OS Secrets Engine plugin must be registered before the mount is enabled. Use `vault.Plugin` to manage catalog registration when appropriate.
 * * Use `vault.Mount` to create, tune, or remove the OS Secrets Engine mount before managing accounts with this resource.
 * * The account must reference an existing host configuration.
 * * The `passwordWo` field is write-only and create-only for security reasons. Once set, Vault will manage the password through rotation, but the current password cannot be retrieved through the API.
 * * To manually update a password after creation, use the Vault CLI or API reset endpoint (see "Password Management" section above).
 * * `verifyConnection` defaults to `true`. In environments where SSH connectivity is intentionally unavailable during creation, set it explicitly to `false`.
 * * Use either `rotationPeriod` or `rotationSchedule`.
 * * After the initial password is set, Vault will rotate it according to the configured schedule. Applications should retrieve the current password from Vault rather than storing it.
 * * Changing `mount`, `host`, `name`, or `passwordWo` will cause the resource to be recreated.
 * * The computed fields `lastVaultRotation` and `nextVaultRotation` are populated by Vault when rotation metadata is available.
 *
 * ## Security Considerations
 *
 * * Store the initial `passwordWo` value in a secure location such as Terraform variables or a secrets management system.
 * * Use `sensitive = true` in variable definitions for passwords.
 * * The `passwordWo` field is create-only and cannot be updated through Terraform after resource creation. This is intentional to enforce proper password management practices.
 * * For password updates after creation, use the Vault CLI or API reset endpoint, which provides proper audit logging.
 * * Ensure Terraform state files are properly secured as they will contain the initial password in cleartext.
 * * Applications should retrieve credentials from Vault dynamically rather than storing them.
 * * When importing existing accounts, use `lifecycle { ignoreChanges = [passwordWo] }` to prevent resource recreation.
 *
 * ## Import
 *
 * OS Secret backend account can be imported using the format `<mount>/hosts/<host>/accounts/<name>`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/osSecretBackendAccount:OsSecretBackendAccount admin os/hosts/web-server/accounts/admin-account
 * ```
 *
 * > **Note:** When importing, the `passwordWo` field will not be populated since it is write-only.
 * You must provide a placeholder value in your configuration (e.g., `passwordWo = "PLACEHOLDER"`).
 * After import, use `lifecycle { ignoreChanges = [passwordWo] }` to prevent Terraform from trying to recreate the resource.
 */
export class OsSecretBackendAccount extends pulumi.CustomResource {
    /**
     * Get an existing OsSecretBackendAccount resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OsSecretBackendAccountState, opts?: pulumi.CustomResourceOptions): OsSecretBackendAccount {
        return new OsSecretBackendAccount(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/osSecretBackendAccount:OsSecretBackendAccount';

    /**
     * Returns true if the given object is an instance of OsSecretBackendAccount.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is OsSecretBackendAccount {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === OsSecretBackendAccount.__pulumiType;
    }

    /**
     * Custom metadata associated with the account.
     */
    declare public readonly customMetadata: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * Disables automated rotation for the account.
     */
    declare public readonly disableAutomatedRotation: pulumi.Output<boolean>;
    /**
     * The name of the host where this account exists.
     */
    declare public readonly host: pulumi.Output<string>;
    /**
     * (Computed) The timestamp of the last password rotation performed by Vault.
     */
    declare public /*out*/ readonly lastVaultRotation: pulumi.Output<string>;
    /**
     * The path where the OS secrets engine is mounted.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * Unique name for the account within the host.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * (Computed) The timestamp when the next password rotation is scheduled to occur.
     */
    declare public /*out*/ readonly nextVaultRotation: pulumi.Output<string>;
    /**
     * Reference to a parent account that manages rotation for this account.
     */
    declare public readonly parentAccountRef: pulumi.Output<string | undefined>;
    /**
     * Password policy to use for generated passwords.
     */
    declare public readonly passwordPolicy: pulumi.Output<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The initial password for the account. This field is write-only and create-only - it can only be set during resource creation and will not be read back from Vault. To update the password after creation, use the Vault CLI or API reset endpoint (see "Password Management" section below). Changing this value will force resource recreation.
     */
    declare public readonly passwordWo: pulumi.Output<string>;
    /**
     * The period between automatic password rotations, in seconds. Mutually exclusive with `rotationSchedule`.
     */
    declare public readonly rotationPeriod: pulumi.Output<number>;
    /**
     * A cron-style schedule for password rotation (for example, `"0 3 * * 0"`). Mutually exclusive with `rotationPeriod`.
     */
    declare public readonly rotationSchedule: pulumi.Output<string>;
    /**
     * The rotation window, in seconds.
     */
    declare public readonly rotationWindow: pulumi.Output<number>;
    /**
     * The username of the operating system account on the remote host.
     */
    declare public readonly username: pulumi.Output<string>;
    /**
     * Whether Vault should verify the host connection and supplied credentials during account onboarding. Defaults to `true`.
     */
    declare public readonly verifyConnection: pulumi.Output<boolean>;

    /**
     * Create a OsSecretBackendAccount resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OsSecretBackendAccountArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OsSecretBackendAccountArgs | OsSecretBackendAccountState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OsSecretBackendAccountState | undefined;
            resourceInputs["customMetadata"] = state?.customMetadata;
            resourceInputs["disableAutomatedRotation"] = state?.disableAutomatedRotation;
            resourceInputs["host"] = state?.host;
            resourceInputs["lastVaultRotation"] = state?.lastVaultRotation;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["nextVaultRotation"] = state?.nextVaultRotation;
            resourceInputs["parentAccountRef"] = state?.parentAccountRef;
            resourceInputs["passwordPolicy"] = state?.passwordPolicy;
            resourceInputs["passwordWo"] = state?.passwordWo;
            resourceInputs["rotationPeriod"] = state?.rotationPeriod;
            resourceInputs["rotationSchedule"] = state?.rotationSchedule;
            resourceInputs["rotationWindow"] = state?.rotationWindow;
            resourceInputs["username"] = state?.username;
            resourceInputs["verifyConnection"] = state?.verifyConnection;
        } else {
            const args = argsOrState as OsSecretBackendAccountArgs | undefined;
            if (args?.host === undefined && !opts.urn) {
                throw new Error("Missing required property 'host'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            if (args?.passwordWo === undefined && !opts.urn) {
                throw new Error("Missing required property 'passwordWo'");
            }
            if (args?.username === undefined && !opts.urn) {
                throw new Error("Missing required property 'username'");
            }
            resourceInputs["customMetadata"] = args?.customMetadata;
            resourceInputs["disableAutomatedRotation"] = args?.disableAutomatedRotation;
            resourceInputs["host"] = args?.host;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["parentAccountRef"] = args?.parentAccountRef;
            resourceInputs["passwordPolicy"] = args?.passwordPolicy;
            resourceInputs["passwordWo"] = args?.passwordWo ? pulumi.secret(args.passwordWo) : undefined;
            resourceInputs["rotationPeriod"] = args?.rotationPeriod;
            resourceInputs["rotationSchedule"] = args?.rotationSchedule;
            resourceInputs["rotationWindow"] = args?.rotationWindow;
            resourceInputs["username"] = args?.username;
            resourceInputs["verifyConnection"] = args?.verifyConnection;
            resourceInputs["lastVaultRotation"] = undefined /*out*/;
            resourceInputs["nextVaultRotation"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["passwordWo"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(OsSecretBackendAccount.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering OsSecretBackendAccount resources.
 */
export interface OsSecretBackendAccountState {
    /**
     * Custom metadata associated with the account.
     */
    customMetadata?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * Disables automated rotation for the account.
     */
    disableAutomatedRotation?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the host where this account exists.
     */
    host?: pulumi.Input<string | undefined>;
    /**
     * (Computed) The timestamp of the last password rotation performed by Vault.
     */
    lastVaultRotation?: pulumi.Input<string | undefined>;
    /**
     * The path where the OS secrets engine is mounted.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * Unique name for the account within the host.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * (Computed) The timestamp when the next password rotation is scheduled to occur.
     */
    nextVaultRotation?: pulumi.Input<string | undefined>;
    /**
     * Reference to a parent account that manages rotation for this account.
     */
    parentAccountRef?: pulumi.Input<string | undefined>;
    /**
     * Password policy to use for generated passwords.
     */
    passwordPolicy?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The initial password for the account. This field is write-only and create-only - it can only be set during resource creation and will not be read back from Vault. To update the password after creation, use the Vault CLI or API reset endpoint (see "Password Management" section below). Changing this value will force resource recreation.
     */
    passwordWo?: pulumi.Input<string | undefined>;
    /**
     * The period between automatic password rotations, in seconds. Mutually exclusive with `rotationSchedule`.
     */
    rotationPeriod?: pulumi.Input<number | undefined>;
    /**
     * A cron-style schedule for password rotation (for example, `"0 3 * * 0"`). Mutually exclusive with `rotationPeriod`.
     */
    rotationSchedule?: pulumi.Input<string | undefined>;
    /**
     * The rotation window, in seconds.
     */
    rotationWindow?: pulumi.Input<number | undefined>;
    /**
     * The username of the operating system account on the remote host.
     */
    username?: pulumi.Input<string | undefined>;
    /**
     * Whether Vault should verify the host connection and supplied credentials during account onboarding. Defaults to `true`.
     */
    verifyConnection?: pulumi.Input<boolean | undefined>;
}

/**
 * The set of arguments for constructing a OsSecretBackendAccount resource.
 */
export interface OsSecretBackendAccountArgs {
    /**
     * Custom metadata associated with the account.
     */
    customMetadata?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * Disables automated rotation for the account.
     */
    disableAutomatedRotation?: pulumi.Input<boolean | undefined>;
    /**
     * The name of the host where this account exists.
     */
    host: pulumi.Input<string>;
    /**
     * The path where the OS secrets engine is mounted.
     */
    mount: pulumi.Input<string>;
    /**
     * Unique name for the account within the host.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Reference to a parent account that manages rotation for this account.
     */
    parentAccountRef?: pulumi.Input<string | undefined>;
    /**
     * Password policy to use for generated passwords.
     */
    passwordPolicy?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The initial password for the account. This field is write-only and create-only - it can only be set during resource creation and will not be read back from Vault. To update the password after creation, use the Vault CLI or API reset endpoint (see "Password Management" section below). Changing this value will force resource recreation.
     */
    passwordWo: pulumi.Input<string>;
    /**
     * The period between automatic password rotations, in seconds. Mutually exclusive with `rotationSchedule`.
     */
    rotationPeriod?: pulumi.Input<number | undefined>;
    /**
     * A cron-style schedule for password rotation (for example, `"0 3 * * 0"`). Mutually exclusive with `rotationPeriod`.
     */
    rotationSchedule?: pulumi.Input<string | undefined>;
    /**
     * The rotation window, in seconds.
     */
    rotationWindow?: pulumi.Input<number | undefined>;
    /**
     * The username of the operating system account on the remote host.
     */
    username: pulumi.Input<string>;
    /**
     * Whether Vault should verify the host connection and supplied credentials during account onboarding. Defaults to `true`.
     */
    verifyConnection?: pulumi.Input<boolean | undefined>;
}

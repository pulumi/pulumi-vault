// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages keys in the Vault Key Management secrets engine. This resource creates and manages cryptographic keys that can be distributed to external Key Management Services (KMS).
 *
 * Keys created with this resource can be distributed to AWS KMS, Azure Key Vault, or GCP Cloud KMS using the appropriate distribution resources.
 *
 * > **Important**
 * All data provided in the resource configuration will be written in cleartext to
 * state and plan files generated by Terraform, and will appear in the console
 * output when Terraform runs. Protect these artifacts accordingly. See the main provider documentation for more details.
 *
 * For more information on Key Management in Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 *
 * **Note** this feature is available only with Vault Enterprise.
 *
 * ## Example Usage
 *
 * ### Basic Symmetric Key
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const keymgmt = new vault.Mount("keymgmt", {
 *     path: "keymgmt",
 *     type: "keymgmt",
 * });
 * const aesKey = new vault.KeymgmtKey("aes_key", {
 *     mount: keymgmt.path,
 *     name: "aes-encryption-key",
 *     type: "aes256-gcm96",
 * });
 * ```
 *
 * ### Default RSA Key (type omitted)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const defaultKey = new vault.KeymgmtKey("default_key", {
 *     mount: keymgmt.path,
 *     name: "my-key",
 * });
 * ```
 *
 * ### RSA Key with Deletion Protection
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const rsaKey = new vault.KeymgmtKey("rsa_key", {
 *     mount: keymgmt.path,
 *     name: "rsa-signing-key",
 *     deletionAllowed: false,
 * });
 * ```
 *
 * ### Key with Minimum Enabled Version
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const versionedKey = new vault.KeymgmtKey("versioned_key", {
 *     mount: keymgmt.path,
 *     name: "versioned-key",
 *     type: "aes256-gcm96",
 *     minEnabledVersion: 2,
 * });
 * ```
 *
 * ### Key with Replication to Multiple Regions
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const replicatedKey = new vault.KeymgmtKey("replicated_key", {
 *     mount: keymgmt.path,
 *     name: "multi-region-key",
 *     type: "aes256-gcm96",
 *     replicaRegions: [
 *         "us-west-1",
 *         "us-east-1",
 *         "eu-west-1",
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * Key Management keys can be imported using the format `{path}/key/{name}`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/keymgmtKey:KeymgmtKey aes_key keymgmt/key/aes-encryption-key
 * ```
 */
export class KeymgmtKey extends pulumi.CustomResource {
    /**
     * Get an existing KeymgmtKey resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KeymgmtKeyState, opts?: pulumi.CustomResourceOptions): KeymgmtKey {
        return new KeymgmtKey(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/keymgmtKey:KeymgmtKey';

    /**
     * Returns true if the given object is an instance of KeymgmtKey.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KeymgmtKey {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KeymgmtKey.__pulumiType;
    }

    /**
     * Specifies if the key is allowed to be deleted.
     */
    declare public readonly deletionAllowed: pulumi.Output<boolean | undefined>;
    /**
     * Specifies the latest version of the key.
     */
    declare public /*out*/ readonly latestVersion: pulumi.Output<number>;
    /**
     * Specifies the minimum enabled version of the key. All versions of the key
     * less than the specified version will be disabled for cryptographic operations in the KMS provider that the key
     * has been distributed to. Setting this value to `0` means that all versions will be enabled.
     */
    declare public readonly minEnabledVersion: pulumi.Output<number>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * Specifies the name of the key to create.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * Specifies the regions in which the key should be replicated. Supported only for AWS KMS.
     */
    declare public readonly replicaRegions: pulumi.Output<string[] | undefined>;
    /**
     * Specifies the type of cryptographic key to create. Defaults to `rsa-2048`.
     * The following key types are supported:
     * - `aes256-gcm96` - AES-GCM with a 256-bit AES key and a 96-bit nonce (symmetric)
     * - `rsa-2048` - RSA with bit size of 2048 (asymmetric) (**default**)
     * - `rsa-3072` - RSA with bit size of 3072 (asymmetric)
     * - `rsa-4096` - RSA with bit size of 4096 (asymmetric)
     * - `ecdsa-p256` - ECDSA using the P-256 elliptic curve (asymmetric)
     * - `ecdsa-p384` -  ECDSA using the P-384 elliptic curve (asymmetric)
     * - `ecdsa-p521` - ECDSA using the P-521 elliptic curve (asymmetric)
     */
    declare public readonly type: pulumi.Output<string>;

    /**
     * Create a KeymgmtKey resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KeymgmtKeyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KeymgmtKeyArgs | KeymgmtKeyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KeymgmtKeyState | undefined;
            resourceInputs["deletionAllowed"] = state?.deletionAllowed;
            resourceInputs["latestVersion"] = state?.latestVersion;
            resourceInputs["minEnabledVersion"] = state?.minEnabledVersion;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["replicaRegions"] = state?.replicaRegions;
            resourceInputs["type"] = state?.type;
        } else {
            const args = argsOrState as KeymgmtKeyArgs | undefined;
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            resourceInputs["deletionAllowed"] = args?.deletionAllowed;
            resourceInputs["minEnabledVersion"] = args?.minEnabledVersion;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["replicaRegions"] = args?.replicaRegions;
            resourceInputs["type"] = args?.type;
            resourceInputs["latestVersion"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KeymgmtKey.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KeymgmtKey resources.
 */
export interface KeymgmtKeyState {
    /**
     * Specifies if the key is allowed to be deleted.
     */
    deletionAllowed?: pulumi.Input<boolean | undefined>;
    /**
     * Specifies the latest version of the key.
     */
    latestVersion?: pulumi.Input<number | undefined>;
    /**
     * Specifies the minimum enabled version of the key. All versions of the key
     * less than the specified version will be disabled for cryptographic operations in the KMS provider that the key
     * has been distributed to. Setting this value to `0` means that all versions will be enabled.
     */
    minEnabledVersion?: pulumi.Input<number | undefined>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * Specifies the name of the key to create.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Specifies the regions in which the key should be replicated. Supported only for AWS KMS.
     */
    replicaRegions?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Specifies the type of cryptographic key to create. Defaults to `rsa-2048`.
     * The following key types are supported:
     * - `aes256-gcm96` - AES-GCM with a 256-bit AES key and a 96-bit nonce (symmetric)
     * - `rsa-2048` - RSA with bit size of 2048 (asymmetric) (**default**)
     * - `rsa-3072` - RSA with bit size of 3072 (asymmetric)
     * - `rsa-4096` - RSA with bit size of 4096 (asymmetric)
     * - `ecdsa-p256` - ECDSA using the P-256 elliptic curve (asymmetric)
     * - `ecdsa-p384` -  ECDSA using the P-384 elliptic curve (asymmetric)
     * - `ecdsa-p521` - ECDSA using the P-521 elliptic curve (asymmetric)
     */
    type?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a KeymgmtKey resource.
 */
export interface KeymgmtKeyArgs {
    /**
     * Specifies if the key is allowed to be deleted.
     */
    deletionAllowed?: pulumi.Input<boolean | undefined>;
    /**
     * Specifies the minimum enabled version of the key. All versions of the key
     * less than the specified version will be disabled for cryptographic operations in the KMS provider that the key
     * has been distributed to. Setting this value to `0` means that all versions will be enabled.
     */
    minEnabledVersion?: pulumi.Input<number | undefined>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount: pulumi.Input<string>;
    /**
     * Specifies the name of the key to create.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Specifies the regions in which the key should be replicated. Supported only for AWS KMS.
     */
    replicaRegions?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Specifies the type of cryptographic key to create. Defaults to `rsa-2048`.
     * The following key types are supported:
     * - `aes256-gcm96` - AES-GCM with a 256-bit AES key and a 96-bit nonce (symmetric)
     * - `rsa-2048` - RSA with bit size of 2048 (asymmetric) (**default**)
     * - `rsa-3072` - RSA with bit size of 3072 (asymmetric)
     * - `rsa-4096` - RSA with bit size of 4096 (asymmetric)
     * - `ecdsa-p256` - ECDSA using the P-256 elliptic curve (asymmetric)
     * - `ecdsa-p384` -  ECDSA using the P-384 elliptic curve (asymmetric)
     * - `ecdsa-p521` - ECDSA using the P-521 elliptic curve (asymmetric)
     */
    type?: pulumi.Input<string | undefined>;
}

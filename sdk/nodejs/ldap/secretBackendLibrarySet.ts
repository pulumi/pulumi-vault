// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const config = new vault.ldap.SecretBackend("config", {
 *     path: "ldap",
 *     binddn: "CN=Administrator,CN=Users,DC=corp,DC=example,DC=net",
 *     bindpass: "SuperSecretPassw0rd",
 *     url: "ldaps://localhost",
 *     insecureTls: true,
 *     userdn: "CN=Users,DC=corp,DC=example,DC=net",
 * });
 * const qa = new vault.ldap.SecretBackendLibrarySet("qa", {
 *     mount: config.path,
 *     serviceAccountNames: [
 *         "Bob",
 *         "Mary",
 *     ],
 *     ttl: 60,
 *     disableCheckInEnforcement: true,
 *     maxTtl: 120,
 * });
 * ```
 *
 * ## Import
 *
 * LDAP secret backend libraries can be imported using the `path`, e.g.
 *
 * ```sh
 * $ pulumi import vault:ldap/secretBackendLibrarySet:SecretBackendLibrarySet qa ldap/library/bob
 * ```
 */
export class SecretBackendLibrarySet extends pulumi.CustomResource {
    /**
     * Get an existing SecretBackendLibrarySet resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretBackendLibrarySetState, opts?: pulumi.CustomResourceOptions): SecretBackendLibrarySet {
        return new SecretBackendLibrarySet(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:ldap/secretBackendLibrarySet:SecretBackendLibrarySet';

    /**
     * Returns true if the given object is an instance of SecretBackendLibrarySet.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretBackendLibrarySet {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretBackendLibrarySet.__pulumiType;
    }

    /**
     * Disable enforcing that service
     * accounts must be checked in by the entity or client token that checked them
     * out. Defaults to false.
     */
    public readonly disableCheckInEnforcement!: pulumi.Output<boolean | undefined>;
    /**
     * The maximum password time-to-live in seconds. Defaults
     * to the configuration maxTtl if not provided.
     */
    public readonly maxTtl!: pulumi.Output<number>;
    /**
     * The path where the LDAP secrets backend is mounted.
     */
    public readonly mount!: pulumi.Output<string | undefined>;
    /**
     * The name to identify this set of service accounts.
     * Must be unique within the backend.
     */
    public readonly name!: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    public readonly namespace!: pulumi.Output<string | undefined>;
    /**
     * Specifies the slice of service accounts mapped to this set.
     */
    public readonly serviceAccountNames!: pulumi.Output<string[]>;
    /**
     * The password time-to-live in seconds. Defaults to the configuration
     * ttl if not provided.
     */
    public readonly ttl!: pulumi.Output<number>;

    /**
     * Create a SecretBackendLibrarySet resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecretBackendLibrarySetArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretBackendLibrarySetArgs | SecretBackendLibrarySetState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretBackendLibrarySetState | undefined;
            resourceInputs["disableCheckInEnforcement"] = state ? state.disableCheckInEnforcement : undefined;
            resourceInputs["maxTtl"] = state ? state.maxTtl : undefined;
            resourceInputs["mount"] = state ? state.mount : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["namespace"] = state ? state.namespace : undefined;
            resourceInputs["serviceAccountNames"] = state ? state.serviceAccountNames : undefined;
            resourceInputs["ttl"] = state ? state.ttl : undefined;
        } else {
            const args = argsOrState as SecretBackendLibrarySetArgs | undefined;
            if ((!args || args.serviceAccountNames === undefined) && !opts.urn) {
                throw new Error("Missing required property 'serviceAccountNames'");
            }
            resourceInputs["disableCheckInEnforcement"] = args ? args.disableCheckInEnforcement : undefined;
            resourceInputs["maxTtl"] = args ? args.maxTtl : undefined;
            resourceInputs["mount"] = args ? args.mount : undefined;
            resourceInputs["name"] = args ? args.name : undefined;
            resourceInputs["namespace"] = args ? args.namespace : undefined;
            resourceInputs["serviceAccountNames"] = args ? args.serviceAccountNames : undefined;
            resourceInputs["ttl"] = args ? args.ttl : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecretBackendLibrarySet.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretBackendLibrarySet resources.
 */
export interface SecretBackendLibrarySetState {
    /**
     * Disable enforcing that service
     * accounts must be checked in by the entity or client token that checked them
     * out. Defaults to false.
     */
    disableCheckInEnforcement?: pulumi.Input<boolean>;
    /**
     * The maximum password time-to-live in seconds. Defaults
     * to the configuration maxTtl if not provided.
     */
    maxTtl?: pulumi.Input<number>;
    /**
     * The path where the LDAP secrets backend is mounted.
     */
    mount?: pulumi.Input<string>;
    /**
     * The name to identify this set of service accounts.
     * Must be unique within the backend.
     */
    name?: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string>;
    /**
     * Specifies the slice of service accounts mapped to this set.
     */
    serviceAccountNames?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The password time-to-live in seconds. Defaults to the configuration
     * ttl if not provided.
     */
    ttl?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a SecretBackendLibrarySet resource.
 */
export interface SecretBackendLibrarySetArgs {
    /**
     * Disable enforcing that service
     * accounts must be checked in by the entity or client token that checked them
     * out. Defaults to false.
     */
    disableCheckInEnforcement?: pulumi.Input<boolean>;
    /**
     * The maximum password time-to-live in seconds. Defaults
     * to the configuration maxTtl if not provided.
     */
    maxTtl?: pulumi.Input<number>;
    /**
     * The path where the LDAP secrets backend is mounted.
     */
    mount?: pulumi.Input<string>;
    /**
     * The name to identify this set of service accounts.
     * Must be unique within the backend.
     */
    name?: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string>;
    /**
     * Specifies the slice of service accounts mapped to this set.
     */
    serviceAccountNames: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The password time-to-live in seconds. Defaults to the configuration
     * ttl if not provided.
     */
    ttl?: pulumi.Input<number>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * This resource supports the "/transform/transformations/tokenization/{name}" Vault endpoint.
 *
 * If a tokenization transformation with the given name doesn't exist, it will be created. If a tokenization transformation with the given name exists, it will be updated with the new attributes.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const example = new vault.Mount("example", {
 *     path: "transform",
 *     type: "transform",
 * });
 * const exampleTransformationTokenization = new vault.transform.TransformationTokenization("example", {
 *     path: example.path,
 *     name: "tkn-example",
 *     maxTtl: 86400,
 *     deletionAllowed: true,
 *     mappingMode: "default",
 *     allowedRoles: ["payments"],
 * });
 * ```
 *
 * ## Import
 *
 * Transform tokenization transformations can be imported using `path/name`, e.g.
 *
 * ```sh
 * $ pulumi import vault:transform/transformationTokenization:TransformationTokenization example transform/transformations/tokenization/tkn-example
 * ```
 *
 * Note that the `convergent` field cannot be recovered from Vault on import and will be empty after import.
 */
export class TransformationTokenization extends pulumi.CustomResource {
    /**
     * Get an existing TransformationTokenization resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TransformationTokenizationState, opts?: pulumi.CustomResourceOptions): TransformationTokenization {
        return new TransformationTokenization(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:transform/transformationTokenization:TransformationTokenization';

    /**
     * Returns true if the given object is an instance of TransformationTokenization.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TransformationTokenization {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TransformationTokenization.__pulumiType;
    }

    /**
     * Specifies a list of allowed roles that this transformation can be assigned to.
     * A role using this transformation must exist in this list in order for encode and decode operations to properly function. Default is `[]`.
     */
    declare public readonly allowedRoles: pulumi.Output<string[] | undefined>;
    /**
     * Specifies whether to use convergent tokenization, where tokenization of
     * the same plaintext more than once results in the same token. Default is `false`.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    declare public readonly convergent: pulumi.Output<boolean | undefined>;
    /**
     * If true, this transform can be deleted. Otherwise deletion is blocked while this value remains false. Note that deleting the transform deletes the underlying key, making decoding of tokenized values impossible without restoring from a backup. Default is `false`.
     */
    declare public readonly deletionAllowed: pulumi.Output<boolean | undefined>;
    /**
     * Specifies the mapping mode for stored tokenization values.
     * Can be `"default"` or `"exportable"`. `"default"` is strongly recommended for highest security.
     * `"exportable"` allows for all plaintexts to be decoded via the export-decoded endpoint in an emergency.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    declare public readonly mappingMode: pulumi.Output<string>;
    /**
     * The maximum TTL of a token. If `0` or unspecified, tokens may have no expiration. Default is `0`.
     */
    declare public readonly maxTtl: pulumi.Output<number>;
    /**
     * Name of the transformation to create/update.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * Path to where the back-end is mounted within Vault.
     */
    declare public readonly path: pulumi.Output<string>;
    /**
     * The list of tokenization stores to use for tokenization state. Default is `["builtin/internal"]`.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    declare public readonly stores: pulumi.Output<string[]>;

    /**
     * Create a TransformationTokenization resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: TransformationTokenizationArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TransformationTokenizationArgs | TransformationTokenizationState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TransformationTokenizationState | undefined;
            resourceInputs["allowedRoles"] = state?.allowedRoles;
            resourceInputs["convergent"] = state?.convergent;
            resourceInputs["deletionAllowed"] = state?.deletionAllowed;
            resourceInputs["mappingMode"] = state?.mappingMode;
            resourceInputs["maxTtl"] = state?.maxTtl;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["path"] = state?.path;
            resourceInputs["stores"] = state?.stores;
        } else {
            const args = argsOrState as TransformationTokenizationArgs | undefined;
            if (args?.path === undefined && !opts.urn) {
                throw new Error("Missing required property 'path'");
            }
            resourceInputs["allowedRoles"] = args?.allowedRoles;
            resourceInputs["convergent"] = args?.convergent;
            resourceInputs["deletionAllowed"] = args?.deletionAllowed;
            resourceInputs["mappingMode"] = args?.mappingMode;
            resourceInputs["maxTtl"] = args?.maxTtl;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["path"] = args?.path;
            resourceInputs["stores"] = args?.stores;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(TransformationTokenization.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TransformationTokenization resources.
 */
export interface TransformationTokenizationState {
    /**
     * Specifies a list of allowed roles that this transformation can be assigned to.
     * A role using this transformation must exist in this list in order for encode and decode operations to properly function. Default is `[]`.
     */
    allowedRoles?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Specifies whether to use convergent tokenization, where tokenization of
     * the same plaintext more than once results in the same token. Default is `false`.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    convergent?: pulumi.Input<boolean | undefined>;
    /**
     * If true, this transform can be deleted. Otherwise deletion is blocked while this value remains false. Note that deleting the transform deletes the underlying key, making decoding of tokenized values impossible without restoring from a backup. Default is `false`.
     */
    deletionAllowed?: pulumi.Input<boolean | undefined>;
    /**
     * Specifies the mapping mode for stored tokenization values.
     * Can be `"default"` or `"exportable"`. `"default"` is strongly recommended for highest security.
     * `"exportable"` allows for all plaintexts to be decoded via the export-decoded endpoint in an emergency.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    mappingMode?: pulumi.Input<string | undefined>;
    /**
     * The maximum TTL of a token. If `0` or unspecified, tokens may have no expiration. Default is `0`.
     */
    maxTtl?: pulumi.Input<number | undefined>;
    /**
     * Name of the transformation to create/update.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Path to where the back-end is mounted within Vault.
     */
    path?: pulumi.Input<string | undefined>;
    /**
     * The list of tokenization stores to use for tokenization state. Default is `["builtin/internal"]`.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    stores?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a TransformationTokenization resource.
 */
export interface TransformationTokenizationArgs {
    /**
     * Specifies a list of allowed roles that this transformation can be assigned to.
     * A role using this transformation must exist in this list in order for encode and decode operations to properly function. Default is `[]`.
     */
    allowedRoles?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * Specifies whether to use convergent tokenization, where tokenization of
     * the same plaintext more than once results in the same token. Default is `false`.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    convergent?: pulumi.Input<boolean | undefined>;
    /**
     * If true, this transform can be deleted. Otherwise deletion is blocked while this value remains false. Note that deleting the transform deletes the underlying key, making decoding of tokenized values impossible without restoring from a backup. Default is `false`.
     */
    deletionAllowed?: pulumi.Input<boolean | undefined>;
    /**
     * Specifies the mapping mode for stored tokenization values.
     * Can be `"default"` or `"exportable"`. `"default"` is strongly recommended for highest security.
     * `"exportable"` allows for all plaintexts to be decoded via the export-decoded endpoint in an emergency.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    mappingMode?: pulumi.Input<string | undefined>;
    /**
     * The maximum TTL of a token. If `0` or unspecified, tokens may have no expiration. Default is `0`.
     */
    maxTtl?: pulumi.Input<number | undefined>;
    /**
     * Name of the transformation to create/update.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Path to where the back-end is mounted within Vault.
     */
    path: pulumi.Input<string>;
    /**
     * The list of tokenization stores to use for tokenization state. Default is `["builtin/internal"]`.
     * **Note:** This field is immutable and cannot be changed after creation. Changing this value will force recreation of the resource.
     */
    stores?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

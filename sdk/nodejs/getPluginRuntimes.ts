// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Lists plugin runtimes registered in Vault's plugin runtimes catalog.
 *
 * > **Important** This data source requires Vault 1.15 or later.
 *
 * ## Example Usage
 *
 * ### List all plugin runtimes
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const all = vault.getPluginRuntimes({});
 * ```
 *
 * ### Filter by runtime type
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const containers = vault.getPluginRuntimes({
 *     type: "container",
 * });
 * ```
 */
export function getPluginRuntimes(args?: GetPluginRuntimesArgs, opts?: pulumi.InvokeOptions): Promise<GetPluginRuntimesResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vault:index/getPluginRuntimes:getPluginRuntimes", {
        "namespace": args.namespace,
        "type": args.type,
    }, opts);
}

/**
 * A collection of arguments for invoking getPluginRuntimes.
 */
export interface GetPluginRuntimesArgs {
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: string;
    /**
     * The plugin runtime type to list. Currently only `container` is supported.
     */
    type?: string;
}

/**
 * A collection of values returned by getPluginRuntimes.
 */
export interface GetPluginRuntimesResult {
    /**
     * Unique identifier for this data source. Values are:
     * * `plugin-runtimes` when listing all runtime types.
     * * `plugin-runtimes/{type}` when `type` is provided.
     */
    readonly id: string;
    readonly namespace?: string;
    /**
     * List of plugin runtimes. Each object contains:
     */
    readonly runtimes: outputs.GetPluginRuntimesRuntime[];
    /**
     * The runtime type.
     */
    readonly type?: string;
}
/**
 * Lists plugin runtimes registered in Vault's plugin runtimes catalog.
 *
 * > **Important** This data source requires Vault 1.15 or later.
 *
 * ## Example Usage
 *
 * ### List all plugin runtimes
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const all = vault.getPluginRuntimes({});
 * ```
 *
 * ### Filter by runtime type
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const containers = vault.getPluginRuntimes({
 *     type: "container",
 * });
 * ```
 */
export function getPluginRuntimesOutput(args?: GetPluginRuntimesOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetPluginRuntimesResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vault:index/getPluginRuntimes:getPluginRuntimes", {
        "namespace": args.namespace,
        "type": args.type,
    }, opts);
}

/**
 * A collection of arguments for invoking getPluginRuntimes.
 */
export interface GetPluginRuntimesOutputArgs {
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * The plugin runtime type to list. Currently only `container` is supported.
     */
    type?: pulumi.Input<string | undefined>;
}

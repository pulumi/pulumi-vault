// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages the configuration of a Kerberos Auth Backend in Vault.
 *
 * This resource configures the Kerberos authentication method by providing the keytab
 * and service account information required for Vault to authenticate users via Kerberos.
 *
 * For more information, see the
 * [Vault docs](https://www.vaultproject.io/docs/auth/kerberos).
 *
 * > **Important** The `keytabWo` field is write-only and is not stored in Terraform state.
 * It is only sent to Vault when the `keytabWoVersion` changes or during initial creation.
 * See the main provider documentation for more details.
 *
 * > **Note** Vault does not support deleting auth backend configurations via the API.
 * When this resource is destroyed or replaced (e.g., when changing the `mount`), it is
 * only removed from Terraform state. The configuration remains in Vault until the auth
 * mount itself is deleted.
 *
 * ## Example Usage
 *
 * ### Basic Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const kerberos = new vault.AuthBackend("kerberos", {
 *     type: "kerberos",
 *     path: "kerberos",
 * });
 * const config = new vault.KerberosAuthBackendConfig("config", {
 *     mount: kerberos.path,
 *     keytabWo: std.filebase64({
 *         input: "/path/to/vault.keytab",
 *     }).then(invoke => invoke.result),
 *     keytabWoVersion: 1,
 *     serviceAccount: "vault/localhost@EXAMPLE.COM",
 * });
 * ```
 *
 * ### Full Configuration with All Options
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const kerberos = new vault.AuthBackend("kerberos", {
 *     type: "kerberos",
 *     path: "kerberos",
 * });
 * const config = new vault.KerberosAuthBackendConfig("config", {
 *     mount: kerberos.path,
 *     keytabWo: std.filebase64({
 *         input: "/path/to/vault.keytab",
 *     }).then(invoke => invoke.result),
 *     keytabWoVersion: 1,
 *     serviceAccount: "vault/localhost@EXAMPLE.COM",
 *     removeInstanceName: true,
 *     addGroupAliases: true,
 * });
 * ```
 *
 * ### Using Namespace (Vault Enterprise)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const example = new vault.Namespace("example", {path: "example-namespace"});
 * const kerberos = new vault.AuthBackend("kerberos", {
 *     namespace: example.path,
 *     type: "kerberos",
 *     path: "kerberos",
 * });
 * const config = new vault.KerberosAuthBackendConfig("config", {
 *     namespace: example.path,
 *     mount: kerberos.path,
 *     keytabWo: std.filebase64({
 *         input: "/path/to/vault.keytab",
 *     }).then(invoke => invoke.result),
 *     keytabWoVersion: 1,
 *     serviceAccount: "vault/localhost@EXAMPLE.COM",
 * });
 * ```
 *
 * ## Import
 *
 * Kerberos auth backend configurations can be imported using the `path`, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig config auth/kerberos/config
 * ```
 *
 * > **Note** The `keytabWo` field cannot be imported as it is write-only and not stored
 * in state. You will need to provide it in your configuration after import.
 *
 * ### Importing with Namespace (Vault Enterprise)
 *
 * For Vault Enterprise with namespaces, set the `TERRAFORM_VAULT_NAMESPACE_IMPORT` environment variable
 * before importing:
 */
export class KerberosAuthBackendConfig extends pulumi.CustomResource {
    /**
     * Get an existing KerberosAuthBackendConfig resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KerberosAuthBackendConfigState, opts?: pulumi.CustomResourceOptions): KerberosAuthBackendConfig {
        return new KerberosAuthBackendConfig(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig';

    /**
     * Returns true if the given object is an instance of KerberosAuthBackendConfig.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KerberosAuthBackendConfig {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KerberosAuthBackendConfig.__pulumiType;
    }

    /**
     * Adds group aliases during authentication. When
     * enabled, Vault will create entity aliases for each group the user belongs to.
     * Defaults to `false`.
     */
    declare public readonly addGroupAliases: pulumi.Output<boolean | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded keytab file content. This is a write-only
     * field and is not stored in Terraform state. The keytab must contain an entry
     * matching the `serviceAccount`.
     */
    declare public readonly keytabWo: pulumi.Output<string>;
    /**
     * Version identifier for keytab updates. Increment this
     * value to trigger a keytab update in Vault. This allows you to rotate the keytab without
     * forcing resource replacement.
     */
    declare public readonly keytabWoVersion: pulumi.Output<number>;
    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * Removes instance names from Kerberos service
     * principal names during authentication. This can be useful when the instance name
     * is not relevant for authentication. Defaults to `false`.
     */
    declare public readonly removeInstanceName: pulumi.Output<boolean | undefined>;
    /**
     * The Kerberos service account associated with the
     * keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vaultSvc`).
     */
    declare public readonly serviceAccount: pulumi.Output<string>;

    /**
     * Create a KerberosAuthBackendConfig resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KerberosAuthBackendConfigArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KerberosAuthBackendConfigArgs | KerberosAuthBackendConfigState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KerberosAuthBackendConfigState | undefined;
            resourceInputs["addGroupAliases"] = state?.addGroupAliases;
            resourceInputs["keytabWo"] = state?.keytabWo;
            resourceInputs["keytabWoVersion"] = state?.keytabWoVersion;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["removeInstanceName"] = state?.removeInstanceName;
            resourceInputs["serviceAccount"] = state?.serviceAccount;
        } else {
            const args = argsOrState as KerberosAuthBackendConfigArgs | undefined;
            if (args?.keytabWo === undefined && !opts.urn) {
                throw new Error("Missing required property 'keytabWo'");
            }
            if (args?.keytabWoVersion === undefined && !opts.urn) {
                throw new Error("Missing required property 'keytabWoVersion'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            if (args?.serviceAccount === undefined && !opts.urn) {
                throw new Error("Missing required property 'serviceAccount'");
            }
            resourceInputs["addGroupAliases"] = args?.addGroupAliases;
            resourceInputs["keytabWo"] = args?.keytabWo ? pulumi.secret(args.keytabWo) : undefined;
            resourceInputs["keytabWoVersion"] = args?.keytabWoVersion;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["removeInstanceName"] = args?.removeInstanceName;
            resourceInputs["serviceAccount"] = args?.serviceAccount;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["keytabWo"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(KerberosAuthBackendConfig.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KerberosAuthBackendConfig resources.
 */
export interface KerberosAuthBackendConfigState {
    /**
     * Adds group aliases during authentication. When
     * enabled, Vault will create entity aliases for each group the user belongs to.
     * Defaults to `false`.
     */
    addGroupAliases?: pulumi.Input<boolean | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded keytab file content. This is a write-only
     * field and is not stored in Terraform state. The keytab must contain an entry
     * matching the `serviceAccount`.
     */
    keytabWo?: pulumi.Input<string | undefined>;
    /**
     * Version identifier for keytab updates. Increment this
     * value to trigger a keytab update in Vault. This allows you to rotate the keytab without
     * forcing resource replacement.
     */
    keytabWoVersion?: pulumi.Input<number | undefined>;
    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Removes instance names from Kerberos service
     * principal names during authentication. This can be useful when the instance name
     * is not relevant for authentication. Defaults to `false`.
     */
    removeInstanceName?: pulumi.Input<boolean | undefined>;
    /**
     * The Kerberos service account associated with the
     * keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vaultSvc`).
     */
    serviceAccount?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a KerberosAuthBackendConfig resource.
 */
export interface KerberosAuthBackendConfigArgs {
    /**
     * Adds group aliases during authentication. When
     * enabled, Vault will create entity aliases for each group the user belongs to.
     * Defaults to `false`.
     */
    addGroupAliases?: pulumi.Input<boolean | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded keytab file content. This is a write-only
     * field and is not stored in Terraform state. The keytab must contain an entry
     * matching the `serviceAccount`.
     */
    keytabWo: pulumi.Input<string>;
    /**
     * Version identifier for keytab updates. Increment this
     * value to trigger a keytab update in Vault. This allows you to rotate the keytab without
     * forcing resource replacement.
     */
    keytabWoVersion: pulumi.Input<number>;
    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Removes instance names from Kerberos service
     * principal names during authentication. This can be useful when the instance name
     * is not relevant for authentication. Defaults to `false`.
     */
    removeInstanceName?: pulumi.Input<boolean | undefined>;
    /**
     * The Kerberos service account associated with the
     * keytab entry (e.g., `vault/localhost@EXAMPLE.COM` or `vaultSvc`).
     */
    serviceAccount: pulumi.Input<string>;
}

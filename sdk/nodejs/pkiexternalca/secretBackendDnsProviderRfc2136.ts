// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages an RFC2136 DNS provider configuration for PKI External CA DNS-01 ACME challenges.
 * RFC2136 allows Vault to perform dynamic DNS updates directly against an authoritative nameserver
 * (e.g. BIND with `allow-update`).
 *
 * > **Important** All data provided in the resource configuration will be
 * written in cleartext to state and plan files generated by Terraform, and
 * will appear in the console output when Terraform runs. Protect these
 * artifacts accordingly. See
 * the main provider documentation
 * for more details.
 *
 * > This resource requires **Vault 2.1.0** or later.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const pki_external_ca = new vault.Mount("pki-external-ca", {
 *     path: "pki-external-ca",
 *     type: "pki-external-ca",
 * });
 * const example = new vault.pkiexternalca.SecretBackendDnsProviderRfc2136("example", {
 *     mount: pki_external_ca.path,
 *     name: "my-rfc2136-provider",
 *     identifiers: [
 *         "example.com",
 *         "*.example.com",
 *     ],
 *     nameserver: "192.168.1.1:53",
 *     tsigKeyName: "vault-key.",
 *     tsigSecretWo: tsigSecret,
 *     tsigSecretWoVersion: 1,
 *     tsigAlgorithm: "hmac-sha256",
 * });
 * ```
 *
 * ## Import
 *
 * RFC2136 DNS provider configurations can be imported using the format `<mount>/config/dns/rfc2136/<name>`, e.g.
 *
 * ```sh
 * $ pulumi import vault:pkiexternalca/secretBackendDnsProviderRfc2136:SecretBackendDnsProviderRfc2136 example pki-external-ca/config/dns/rfc2136/my-rfc2136-provider
 * ```
 */
export class SecretBackendDnsProviderRfc2136 extends pulumi.CustomResource {
    /**
     * Get an existing SecretBackendDnsProviderRfc2136 resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretBackendDnsProviderRfc2136State, opts?: pulumi.CustomResourceOptions): SecretBackendDnsProviderRfc2136 {
        return new SecretBackendDnsProviderRfc2136(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:pkiexternalca/secretBackendDnsProviderRfc2136:SecretBackendDnsProviderRfc2136';

    /**
     * Returns true if the given object is an instance of SecretBackendDnsProviderRfc2136.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretBackendDnsProviderRfc2136 {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretBackendDnsProviderRfc2136.__pulumiType;
    }

    /**
     * The date and time the provider was created.
     */
    declare public /*out*/ readonly creationDate: pulumi.Output<string>;
    /**
     * List of domain identifiers this provider handles. Supports wildcard patterns with a leftmost `*` (e.g. `*.example.com`).
     */
    declare public readonly identifiers: pulumi.Output<string[]>;
    /**
     * The date and time the provider was last updated.
     */
    declare public /*out*/ readonly lastUpdatedDate: pulumi.Output<string>;
    /**
     * The path where the PKI External CA secret backend is mounted.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * Name of the DNS provider configuration. Must be unique within the backend.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Address of the authoritative nameserver to send RFC2136 dynamic update requests to, in `IP:port` format (e.g. `192.168.1.1:53`). Also used as the verification nameserver for this provider.
     */
    declare public readonly nameserver: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * TSIG algorithm to use. Valid values are `hmac-sha1`, `hmac-sha224`, `hmac-sha256`, `hmac-sha384`, `hmac-sha512`. Defaults to `hmac-sha256`.
     */
    declare public readonly tsigAlgorithm: pulumi.Output<string | undefined>;
    /**
     * TSIG key name for authenticated DNS updates (e.g. `vault-key.`).
     */
    declare public readonly tsigKeyName: pulumi.Output<string>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * TSIG secret as a base64-encoded string. Write-only — never stored in Terraform state.
     */
    declare public readonly tsigSecretWo: pulumi.Output<string>;
    /**
     * Version counter for `tsigSecretWo`. Increment this to trigger an update to the TSIG secret in Vault without changing any other field.
     */
    declare public readonly tsigSecretWoVersion: pulumi.Output<number>;
    /**
     * TTL for DNS TXT records used in DNS-01 challenges. Defaults to `60s`.
     */
    declare public readonly ttl: pulumi.Output<number>;

    /**
     * Create a SecretBackendDnsProviderRfc2136 resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecretBackendDnsProviderRfc2136Args, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretBackendDnsProviderRfc2136Args | SecretBackendDnsProviderRfc2136State, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretBackendDnsProviderRfc2136State | undefined;
            resourceInputs["creationDate"] = state?.creationDate;
            resourceInputs["identifiers"] = state?.identifiers;
            resourceInputs["lastUpdatedDate"] = state?.lastUpdatedDate;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["name"] = state?.name;
            resourceInputs["nameserver"] = state?.nameserver;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["tsigAlgorithm"] = state?.tsigAlgorithm;
            resourceInputs["tsigKeyName"] = state?.tsigKeyName;
            resourceInputs["tsigSecretWo"] = state?.tsigSecretWo;
            resourceInputs["tsigSecretWoVersion"] = state?.tsigSecretWoVersion;
            resourceInputs["ttl"] = state?.ttl;
        } else {
            const args = argsOrState as SecretBackendDnsProviderRfc2136Args | undefined;
            if (args?.identifiers === undefined && !opts.urn) {
                throw new Error("Missing required property 'identifiers'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            if (args?.nameserver === undefined && !opts.urn) {
                throw new Error("Missing required property 'nameserver'");
            }
            if (args?.tsigKeyName === undefined && !opts.urn) {
                throw new Error("Missing required property 'tsigKeyName'");
            }
            if (args?.tsigSecretWo === undefined && !opts.urn) {
                throw new Error("Missing required property 'tsigSecretWo'");
            }
            if (args?.tsigSecretWoVersion === undefined && !opts.urn) {
                throw new Error("Missing required property 'tsigSecretWoVersion'");
            }
            resourceInputs["identifiers"] = args?.identifiers;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["name"] = args?.name;
            resourceInputs["nameserver"] = args?.nameserver;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["tsigAlgorithm"] = args?.tsigAlgorithm;
            resourceInputs["tsigKeyName"] = args?.tsigKeyName;
            resourceInputs["tsigSecretWo"] = args?.tsigSecretWo ? pulumi.secret(args.tsigSecretWo) : undefined;
            resourceInputs["tsigSecretWoVersion"] = args?.tsigSecretWoVersion;
            resourceInputs["ttl"] = args?.ttl;
            resourceInputs["creationDate"] = undefined /*out*/;
            resourceInputs["lastUpdatedDate"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["tsigSecretWo"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(SecretBackendDnsProviderRfc2136.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretBackendDnsProviderRfc2136 resources.
 */
export interface SecretBackendDnsProviderRfc2136State {
    /**
     * The date and time the provider was created.
     */
    creationDate?: pulumi.Input<string | undefined>;
    /**
     * List of domain identifiers this provider handles. Supports wildcard patterns with a leftmost `*` (e.g. `*.example.com`).
     */
    identifiers?: pulumi.Input<pulumi.Input<string>[] | undefined>;
    /**
     * The date and time the provider was last updated.
     */
    lastUpdatedDate?: pulumi.Input<string | undefined>;
    /**
     * The path where the PKI External CA secret backend is mounted.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * Name of the DNS provider configuration. Must be unique within the backend.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Address of the authoritative nameserver to send RFC2136 dynamic update requests to, in `IP:port` format (e.g. `192.168.1.1:53`). Also used as the verification nameserver for this provider.
     */
    nameserver?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * TSIG algorithm to use. Valid values are `hmac-sha1`, `hmac-sha224`, `hmac-sha256`, `hmac-sha384`, `hmac-sha512`. Defaults to `hmac-sha256`.
     */
    tsigAlgorithm?: pulumi.Input<string | undefined>;
    /**
     * TSIG key name for authenticated DNS updates (e.g. `vault-key.`).
     */
    tsigKeyName?: pulumi.Input<string | undefined>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * TSIG secret as a base64-encoded string. Write-only — never stored in Terraform state.
     */
    tsigSecretWo?: pulumi.Input<string | undefined>;
    /**
     * Version counter for `tsigSecretWo`. Increment this to trigger an update to the TSIG secret in Vault without changing any other field.
     */
    tsigSecretWoVersion?: pulumi.Input<number | undefined>;
    /**
     * TTL for DNS TXT records used in DNS-01 challenges. Defaults to `60s`.
     */
    ttl?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a SecretBackendDnsProviderRfc2136 resource.
 */
export interface SecretBackendDnsProviderRfc2136Args {
    /**
     * List of domain identifiers this provider handles. Supports wildcard patterns with a leftmost `*` (e.g. `*.example.com`).
     */
    identifiers: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The path where the PKI External CA secret backend is mounted.
     */
    mount: pulumi.Input<string>;
    /**
     * Name of the DNS provider configuration. Must be unique within the backend.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * Address of the authoritative nameserver to send RFC2136 dynamic update requests to, in `IP:port` format (e.g. `192.168.1.1:53`). Also used as the verification nameserver for this provider.
     */
    nameserver: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * TSIG algorithm to use. Valid values are `hmac-sha1`, `hmac-sha224`, `hmac-sha256`, `hmac-sha384`, `hmac-sha512`. Defaults to `hmac-sha256`.
     */
    tsigAlgorithm?: pulumi.Input<string | undefined>;
    /**
     * TSIG key name for authenticated DNS updates (e.g. `vault-key.`).
     */
    tsigKeyName: pulumi.Input<string>;
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * TSIG secret as a base64-encoded string. Write-only — never stored in Terraform state.
     */
    tsigSecretWo: pulumi.Input<string>;
    /**
     * Version counter for `tsigSecretWo`. Increment this to trigger an update to the TSIG secret in Vault without changing any other field.
     */
    tsigSecretWoVersion: pulumi.Input<number>;
    /**
     * TTL for DNS TXT records used in DNS-01 challenges. Defaults to `60s`.
     */
    ttl?: pulumi.Input<number | undefined>;
}

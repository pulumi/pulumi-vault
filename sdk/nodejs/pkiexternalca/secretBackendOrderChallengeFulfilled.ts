// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Marks an ACME challenge as fulfilled for a specific identifier in an order. This resource notifies Vault that the challenge has been completed and the ACME server can now validate it.
 *
 * > **Note** This resource should be used after you have completed the challenge requirements (e.g., placed the HTTP-01 challenge file, created the DNS-01 TXT record, or configured the TLS-ALPN-01 certificate). Use the `vault.pkiexternalca.getSecretBackendOrderChallenge` data source to retrieve the challenge details first.
 *
 * ## Example Usage
 *
 * ### With HTTP-01 Challenge
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as local from "@pulumi/local";
 * import * as vault from "@pulumi/vault";
 *
 * const pki_external_ca = new vault.Mount("pki-external-ca", {
 *     path: "pki-external-ca",
 *     type: "pki-external-ca",
 * });
 * const exampleSecretBackendAcmeAccount = new vault.pkiexternalca.SecretBackendAcmeAccount("example", {
 *     mount: pki_external_ca.path,
 *     name: "my-acme-account",
 *     directoryUrl: "https://acme-v02.api.letsencrypt.org/directory",
 *     emailContacts: ["admin@example.com"],
 * });
 * const exampleSecretBackendRole = new vault.pkiexternalca.SecretBackendRole("example", {
 *     mount: pki_external_ca.path,
 *     name: "example-role",
 *     acmeAccountName: exampleSecretBackendAcmeAccount.name,
 *     allowedDomains: ["example.com"],
 *     allowedDomainOptions: [
 *         "bare_domains",
 *         "subdomains",
 *     ],
 *     allowedChallengeTypes: ["http-01"],
 * });
 * const exampleSecretBackendOrder = new vault.pkiexternalca.SecretBackendOrder("example", {
 *     mount: pki_external_ca.path,
 *     roleName: exampleSecretBackendRole.name,
 *     identifiers: ["www.example.com"],
 * });
 * // Retrieve challenge details
 * const example = vault.pkiexternalca.getSecretBackendOrderChallengeOutput({
 *     mount: pki_external_ca.path,
 *     roleName: exampleSecretBackendRole.name,
 *     orderId: exampleSecretBackendOrder.orderId,
 *     challengeType: "http-01",
 *     identifier: "www.example.com",
 * });
 * // Deploy the challenge (example using local_file)
 * const challenge = new local.index.File("challenge", {
 *     filename: `/var/www/html/.well-known/acme-challenge/${example.token}`,
 *     content: example.keyAuthorization,
 * });
 * // Mark challenge as fulfilled after deployment
 * const exampleSecretBackendOrderChallengeFulfilled = new vault.pkiexternalca.SecretBackendOrderChallengeFulfilled("example", {
 *     mount: pki_external_ca.path,
 *     roleName: exampleSecretBackendRole.name,
 *     orderId: exampleSecretBackendOrder.orderId,
 *     challengeType: "http-01",
 *     identifier: "www.example.com",
 * }, {
 *     dependsOn: [challenge],
 * });
 * ```
 *
 * ### With DNS-01 Challenge
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 * import * as vault from "@pulumi/vault";
 *
 * // Retrieve DNS challenge details
 * const dns = vault.pkiexternalca.getSecretBackendOrderChallenge({
 *     mount: pki_external_ca.path,
 *     roleName: exampleVaultPkiExternalCaSecretBackendRole.name,
 *     orderId: example.orderId,
 *     challengeType: "dns-01",
 *     identifier: "www.example.com",
 * });
 * // Create DNS TXT record (example using AWS Route53)
 * const acmeChallenge = new aws.index.Route53Record("acme_challenge", {
 *     zoneId: exampleAwsRoute53Zone.zoneId,
 *     name: "_acme-challenge.www.example.com",
 *     type: "TXT",
 *     ttl: 60,
 *     records: [dns.keyAuthorization],
 * });
 * // Mark challenge as fulfilled
 * const dnsSecretBackendOrderChallengeFulfilled = new vault.pkiexternalca.SecretBackendOrderChallengeFulfilled("dns", {
 *     mount: pki_external_ca.path,
 *     roleName: exampleVaultPkiExternalCaSecretBackendRole.name,
 *     orderId: example.orderId,
 *     challengeType: "dns-01",
 *     identifier: "www.example.com",
 * }, {
 *     dependsOn: [acmeChallenge],
 * });
 * ```
 *
 * ## Import
 *
 * PKI External CA order challenge fulfilled resources can be imported using the format `<mount>/role/<role_name>/order/<order_id>/fulfilled-challenge/<challenge_type>/<identifier>`, e.g.
 *
 * ```sh
 * $ pulumi import vault:pkiexternalca/secretBackendOrderChallengeFulfilled:SecretBackendOrderChallengeFulfilled example pki-external-ca/role/example-role/order/abc123/fulfilled-challenge/http-01/www.example.com
 * ```
 *
 * > **Note** This resource represents an action (marking a challenge as fulfilled) rather than a persistent object. Deletion removes it from state without making any API calls, as the action cannot be undone.
 */
export class SecretBackendOrderChallengeFulfilled extends pulumi.CustomResource {
    /**
     * Get an existing SecretBackendOrderChallengeFulfilled resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretBackendOrderChallengeFulfilledState, opts?: pulumi.CustomResourceOptions): SecretBackendOrderChallengeFulfilled {
        return new SecretBackendOrderChallengeFulfilled(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:pkiexternalca/secretBackendOrderChallengeFulfilled:SecretBackendOrderChallengeFulfilled';

    /**
     * Returns true if the given object is an instance of SecretBackendOrderChallengeFulfilled.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretBackendOrderChallengeFulfilled {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretBackendOrderChallengeFulfilled.__pulumiType;
    }

    /**
     * The type of ACME challenge that was fulfilled. Valid values are `http-01`, `dns-01`, `tls-alpn-01`.
     */
    declare public readonly challengeType: pulumi.Output<string>;
    /**
     * The identifier (domain name) for which the challenge was fulfilled.
     */
    declare public readonly identifier: pulumi.Output<string>;
    /**
     * The path where the PKI External CA secret backend is mounted.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * The unique identifier for the ACME order.
     */
    declare public readonly orderId: pulumi.Output<string>;
    /**
     * Name of the role associated with the order.
     */
    declare public readonly roleName: pulumi.Output<string>;

    /**
     * Create a SecretBackendOrderChallengeFulfilled resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecretBackendOrderChallengeFulfilledArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretBackendOrderChallengeFulfilledArgs | SecretBackendOrderChallengeFulfilledState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretBackendOrderChallengeFulfilledState | undefined;
            resourceInputs["challengeType"] = state?.challengeType;
            resourceInputs["identifier"] = state?.identifier;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["orderId"] = state?.orderId;
            resourceInputs["roleName"] = state?.roleName;
        } else {
            const args = argsOrState as SecretBackendOrderChallengeFulfilledArgs | undefined;
            if (args?.challengeType === undefined && !opts.urn) {
                throw new Error("Missing required property 'challengeType'");
            }
            if (args?.identifier === undefined && !opts.urn) {
                throw new Error("Missing required property 'identifier'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            if (args?.orderId === undefined && !opts.urn) {
                throw new Error("Missing required property 'orderId'");
            }
            if (args?.roleName === undefined && !opts.urn) {
                throw new Error("Missing required property 'roleName'");
            }
            resourceInputs["challengeType"] = args?.challengeType;
            resourceInputs["identifier"] = args?.identifier;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["orderId"] = args?.orderId;
            resourceInputs["roleName"] = args?.roleName;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecretBackendOrderChallengeFulfilled.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretBackendOrderChallengeFulfilled resources.
 */
export interface SecretBackendOrderChallengeFulfilledState {
    /**
     * The type of ACME challenge that was fulfilled. Valid values are `http-01`, `dns-01`, `tls-alpn-01`.
     */
    challengeType?: pulumi.Input<string | undefined>;
    /**
     * The identifier (domain name) for which the challenge was fulfilled.
     */
    identifier?: pulumi.Input<string | undefined>;
    /**
     * The path where the PKI External CA secret backend is mounted.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * The unique identifier for the ACME order.
     */
    orderId?: pulumi.Input<string | undefined>;
    /**
     * Name of the role associated with the order.
     */
    roleName?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SecretBackendOrderChallengeFulfilled resource.
 */
export interface SecretBackendOrderChallengeFulfilledArgs {
    /**
     * The type of ACME challenge that was fulfilled. Valid values are `http-01`, `dns-01`, `tls-alpn-01`.
     */
    challengeType: pulumi.Input<string>;
    /**
     * The identifier (domain name) for which the challenge was fulfilled.
     */
    identifier: pulumi.Input<string>;
    /**
     * The path where the PKI External CA secret backend is mounted.
     */
    mount: pulumi.Input<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * The unique identifier for the ACME order.
     */
    orderId: pulumi.Input<string>;
    /**
     * Name of the role associated with the order.
     */
    roleName: pulumi.Input<string>;
}

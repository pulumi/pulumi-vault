// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Rotates a key in the Vault Key Management secrets engine. This resource creates a new version of an existing key, which is useful for implementing key rotation policies and maintaining security best practices.
 *
 * Key rotation creates a new cryptographic key version while keeping the old versions available for decryption of data encrypted with previous versions.
 *
 * > **Important**
 * All data provided in the resource configuration will be written in cleartext to
 * state and plan files generated by Terraform, and will appear in the console
 * output when Terraform runs. Protect these artifacts accordingly. See the main provider documentation for more details.
 *
 * For more information on key rotation in Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 *
 * **Note** this feature is available only with Vault Enterprise.
 *
 * ## Example Usage
 *
 * ### Basic Key Rotation
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const keymgmt = new vault.Mount("keymgmt", {
 *     path: "keymgmt",
 *     type: "keymgmt",
 * });
 * const encryptionKey = new vault.keymgmt.Key("encryption_key", {
 *     mount: keymgmt.path,
 *     name: "rotation-example",
 *     type: "aes256-gcm96",
 * });
 * const rotate = new vault.keymgmt.KeyRotate("rotate", {
 *     mount: keymgmt.path,
 *     name: encryptionKey.name,
 * });
 * ```
 *
 * ## Import
 *
 * Key rotation resources can be imported using the format `{path}/key/{name}/rotate`, e.g.
 *
 * ```sh
 * $ pulumi import vault:keymgmt/keyRotate:KeyRotate rotate keymgmt/key/rotation-example/rotate
 * ```
 *
 * > **Note:** This resource rotates the key once when created. Subsequent `pulumi up` operations will not trigger additional rotations unless the resource is destroyed and recreated.
 */
export class KeyRotate extends pulumi.CustomResource {
    /**
     * Get an existing KeyRotate resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KeyRotateState, opts?: pulumi.CustomResourceOptions): KeyRotate {
        return new KeyRotate(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:keymgmt/keyRotate:KeyRotate';

    /**
     * Returns true if the given object is an instance of KeyRotate.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KeyRotate {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KeyRotate.__pulumiType;
    }

    /**
     * Specifies the latest version of the key.
     */
    declare public /*out*/ readonly latestVersion: pulumi.Output<number>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * Specifies the name of the key to rotate.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;

    /**
     * Create a KeyRotate resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KeyRotateArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KeyRotateArgs | KeyRotateState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KeyRotateState | undefined;
            resourceInputs["latestVersion"] = state?.latestVersion;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
        } else {
            const args = argsOrState as KeyRotateArgs | undefined;
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            resourceInputs["mount"] = args?.mount;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["latestVersion"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KeyRotate.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KeyRotate resources.
 */
export interface KeyRotateState {
    /**
     * Specifies the latest version of the key.
     */
    latestVersion?: pulumi.Input<number | undefined>;
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * Specifies the name of the key to rotate.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a KeyRotate resource.
 */
export interface KeyRotateArgs {
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = "keymgmt"`. Use
     * `vault_mount.keymgmt.path` here.
     */
    mount: pulumi.Input<string>;
    /**
     * Specifies the name of the key to rotate.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "../utilities";

/**
 * Manages host configurations in the OS Secrets Engine. Hosts represent remote systems where
 * Vault will manage operating system account credentials via SSH. This resource requires Vault 2.0.0 or later.
 *
 * The OS Secrets Engine mount itself is managed separately, typically with `vault.Mount`. This resource manages
 * hosts beneath an existing OS mount.
 *
 * Before mounting the OS Secrets Engine, the external OS plugin must already be registered in Vault's plugin catalog.
 * You can register it with the `vault.Plugin` resource.
 *
 * The examples below use the canonical plugin name `vault-plugin-secrets-os`. If your Vault cluster registers the
 * OS plugin under a different catalog name, use that name in `vault_mount.type` instead.
 *
 * See the [Vault documentation](https://www.vaultproject.io/docs/secrets/os) for more information.
 *
 * ## Example Usage
 *
 * ### Register Plugin And Configure Host
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Plugin("os", {
 *     type: "secret",
 *     name: "vault-plugin-secrets-os",
 *     version: "v0.1.0+ent",
 * });
 * const osMount = new vault.Mount("os", {
 *     path: "os",
 *     type: os.name,
 * });
 * const osSecretBackend = new vault.os.SecretBackend("os", {mount: osMount.path});
 * const example = new vault.os.SecretBackendHost("example", {
 *     mount: osSecretBackend.mount,
 *     name: "web-server-01",
 *     address: "192.168.1.100",
 *     port: 22,
 * });
 * ```
 *
 * ### Basic Host Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Mount("os", {
 *     path: "os",
 *     type: "vault-plugin-secrets-os",
 * });
 * const osSecretBackend = new vault.os.SecretBackend("os", {mount: os.path});
 * const example = new vault.os.SecretBackendHost("example", {
 *     mount: osSecretBackend.mount,
 *     name: "web-server-01",
 *     address: "192.168.1.100",
 *     port: 22,
 * });
 * ```
 *
 * ### Advanced Host Configuration
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Mount("os", {
 *     path: "os",
 *     type: "vault-plugin-secrets-os",
 * });
 * const osSecretBackend = new vault.os.SecretBackend("os", {mount: os.path});
 * const production = new vault.os.SecretBackendHost("production", {
 *     mount: osSecretBackend.mount,
 *     name: "prod-db-01",
 *     address: "10.0.1.50",
 *     port: 2222,
 *     rotationSchedule: "0 2 * * *",
 *     rotationWindow: 3600,
 *     customMetadata: {
 *         environment: "production",
 *         team: "database",
 *         criticality: "high",
 *     },
 * });
 * ```
 *
 * ### Host with SSH Host Key
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const os = new vault.Mount("os", {
 *     path: "os",
 *     type: "vault-plugin-secrets-os",
 * });
 * const osSecretBackend = new vault.os.SecretBackend("os", {mount: os.path});
 * const secure = new vault.os.SecretBackendHost("secure", {
 *     mount: osSecretBackend.mount,
 *     name: "secure-host",
 *     address: "192.168.1.200",
 *     port: 22,
 *     sshHostKey: "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQC...",
 * });
 * ```
 *
 * ## Notes
 *
 * * This resource requires Vault 2.0.0 or later.
 * * The OS Secrets Engine plugin must be registered before the mount is enabled. Use `vault.Plugin` to manage catalog registration when appropriate.
 * * Use `vault.Mount` to create, tune, or remove the OS Secrets Engine mount before managing hosts with this resource.
 * * The host must be configured before accounts can be created on it.
 * * When `sshHostKey` is not provided, the backend's `sshHostKeyTrustOnFirstUse` setting determines whether the host key will be automatically trusted on first connection.
 * * Use either `rotationPeriod` or `rotationSchedule`.
 * * Custom metadata is stored alongside the host configuration and can be used for organizational purposes, but does not affect host behavior.
 * * Changing `mount` or `name` will cause the resource to be recreated.
 *
 * ## Import
 *
 * OS Secret backend host can be imported using the format `<mount>/hosts/<name>`, e.g.
 *
 * ```sh
 * $ pulumi import vault:os/secretBackendHost:SecretBackendHost example os/hosts/web-server-01
 * ```
 */
export class SecretBackendHost extends pulumi.CustomResource {
    /**
     * Get an existing SecretBackendHost resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretBackendHostState, opts?: pulumi.CustomResourceOptions): SecretBackendHost {
        return new SecretBackendHost(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:os/secretBackendHost:SecretBackendHost';

    /**
     * Returns true if the given object is an instance of SecretBackendHost.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretBackendHost {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretBackendHost.__pulumiType;
    }

    /**
     * The address of the host (IP address or hostname).
     */
    declare public readonly address: pulumi.Output<string>;
    /**
     * A map of string key-value pairs for storing custom metadata about the host.
     */
    declare public readonly customMetadata: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * Disables automated rotation for the host.
     */
    declare public readonly disableAutomatedRotation: pulumi.Output<boolean>;
    /**
     * The path where the OS secrets engine is mounted.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * Unique name for the host within the mount.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * The password policy inherited by accounts on this host unless overridden at the account level.
     */
    declare public readonly passwordPolicy: pulumi.Output<string | undefined>;
    /**
     * The port to connect to on the host. Defaults to `22`.
     */
    declare public readonly port: pulumi.Output<number>;
    /**
     * How often to rotate credentials, in seconds. Mutually exclusive with `rotationSchedule`.
     */
    declare public readonly rotationPeriod: pulumi.Output<number>;
    /**
     * A cron-style schedule for credential rotation (for example, `"0 2 * * *"`). Mutually exclusive with `rotationPeriod`.
     */
    declare public readonly rotationSchedule: pulumi.Output<string>;
    /**
     * The rotation window, in seconds. This is typically used with `rotationSchedule`.
     */
    declare public readonly rotationWindow: pulumi.Output<number>;
    /**
     * The SSH host key for the remote host. If not provided and `sshHostKeyTrustOnFirstUse` is enabled on the backend, Vault can learn and persist the key on first connection.
     */
    declare public readonly sshHostKey: pulumi.Output<string>;

    /**
     * Create a SecretBackendHost resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: SecretBackendHostArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretBackendHostArgs | SecretBackendHostState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretBackendHostState | undefined;
            resourceInputs["address"] = state?.address;
            resourceInputs["customMetadata"] = state?.customMetadata;
            resourceInputs["disableAutomatedRotation"] = state?.disableAutomatedRotation;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["passwordPolicy"] = state?.passwordPolicy;
            resourceInputs["port"] = state?.port;
            resourceInputs["rotationPeriod"] = state?.rotationPeriod;
            resourceInputs["rotationSchedule"] = state?.rotationSchedule;
            resourceInputs["rotationWindow"] = state?.rotationWindow;
            resourceInputs["sshHostKey"] = state?.sshHostKey;
        } else {
            const args = argsOrState as SecretBackendHostArgs | undefined;
            if (args?.address === undefined && !opts.urn) {
                throw new Error("Missing required property 'address'");
            }
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            resourceInputs["address"] = args?.address;
            resourceInputs["customMetadata"] = args?.customMetadata;
            resourceInputs["disableAutomatedRotation"] = args?.disableAutomatedRotation;
            resourceInputs["mount"] = args?.mount;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["passwordPolicy"] = args?.passwordPolicy;
            resourceInputs["port"] = args?.port;
            resourceInputs["rotationPeriod"] = args?.rotationPeriod;
            resourceInputs["rotationSchedule"] = args?.rotationSchedule;
            resourceInputs["rotationWindow"] = args?.rotationWindow;
            resourceInputs["sshHostKey"] = args?.sshHostKey;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecretBackendHost.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretBackendHost resources.
 */
export interface SecretBackendHostState {
    /**
     * The address of the host (IP address or hostname).
     */
    address?: pulumi.Input<string | undefined>;
    /**
     * A map of string key-value pairs for storing custom metadata about the host.
     */
    customMetadata?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * Disables automated rotation for the host.
     */
    disableAutomatedRotation?: pulumi.Input<boolean | undefined>;
    /**
     * The path where the OS secrets engine is mounted.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * Unique name for the host within the mount.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * The password policy inherited by accounts on this host unless overridden at the account level.
     */
    passwordPolicy?: pulumi.Input<string | undefined>;
    /**
     * The port to connect to on the host. Defaults to `22`.
     */
    port?: pulumi.Input<number | undefined>;
    /**
     * How often to rotate credentials, in seconds. Mutually exclusive with `rotationSchedule`.
     */
    rotationPeriod?: pulumi.Input<number | undefined>;
    /**
     * A cron-style schedule for credential rotation (for example, `"0 2 * * *"`). Mutually exclusive with `rotationPeriod`.
     */
    rotationSchedule?: pulumi.Input<string | undefined>;
    /**
     * The rotation window, in seconds. This is typically used with `rotationSchedule`.
     */
    rotationWindow?: pulumi.Input<number | undefined>;
    /**
     * The SSH host key for the remote host. If not provided and `sshHostKeyTrustOnFirstUse` is enabled on the backend, Vault can learn and persist the key on first connection.
     */
    sshHostKey?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SecretBackendHost resource.
 */
export interface SecretBackendHostArgs {
    /**
     * The address of the host (IP address or hostname).
     */
    address: pulumi.Input<string>;
    /**
     * A map of string key-value pairs for storing custom metadata about the host.
     */
    customMetadata?: pulumi.Input<{[key: string]: pulumi.Input<string>} | undefined>;
    /**
     * Disables automated rotation for the host.
     */
    disableAutomatedRotation?: pulumi.Input<boolean | undefined>;
    /**
     * The path where the OS secrets engine is mounted.
     */
    mount: pulumi.Input<string>;
    /**
     * Unique name for the host within the mount.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * The password policy inherited by accounts on this host unless overridden at the account level.
     */
    passwordPolicy?: pulumi.Input<string | undefined>;
    /**
     * The port to connect to on the host. Defaults to `22`.
     */
    port?: pulumi.Input<number | undefined>;
    /**
     * How often to rotate credentials, in seconds. Mutually exclusive with `rotationSchedule`.
     */
    rotationPeriod?: pulumi.Input<number | undefined>;
    /**
     * A cron-style schedule for credential rotation (for example, `"0 2 * * *"`). Mutually exclusive with `rotationPeriod`.
     */
    rotationSchedule?: pulumi.Input<string | undefined>;
    /**
     * The rotation window, in seconds. This is typically used with `rotationSchedule`.
     */
    rotationWindow?: pulumi.Input<number | undefined>;
    /**
     * The SSH host key for the remote host. If not provided and `sshHostKeyTrustOnFirstUse` is enabled on the backend, Vault can learn and persist the key on first connection.
     */
    sshHostKey?: pulumi.Input<string | undefined>;
}

// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Manages LDAP group to Vault policy mappings for the Kerberos authentication method in Vault.
 *
 * This resource allows you to map LDAP groups to Vault policies when using the Kerberos
 * authentication method. After a user successfully authenticates via Kerberos, Vault queries
 * LDAP to determine the user's group memberships. These group mappings then
 * determine which Vault policies are assigned to the authenticated user's token.
 *
 * For more information, see the
 * [Vault Kerberos Auth Method documentation](https://www.vaultproject.io/docs/auth/kerberos).
 *
 * > **Note** This resource requires that LDAP integration be configured for the Kerberos
 * auth method using `vault.KerberosAuthBackendLdapConfig`.
 * Without LDAP configuration, group mappings cannot be resolved.
 *
 * ## Example Usage
 *
 * ### Basic Group Mapping
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vault from "@pulumi/vault";
 *
 * const kerberos = new vault.AuthBackend("kerberos", {
 *     type: "kerberos",
 *     path: "kerberos",
 * });
 * const kerberosKerberosAuthBackendConfig = new vault.KerberosAuthBackendConfig("kerberos", {
 *     mount: kerberos.path,
 *     keytabWo: std.filebase64({
 *         input: "/path/to/vault.keytab",
 *     }).then(invoke => invoke.result),
 *     keytabWoVersion: 1,
 *     serviceAccount: "vault/localhost@EXAMPLE.COM",
 * });
 * const ldap = new vault.KerberosAuthBackendLdapConfig("ldap", {
 *     mount: kerberos.path,
 *     url: "ldap://ldap.example.com",
 *     binddn: "cn=vault,ou=Users,dc=example,dc=com",
 *     userdn: "ou=People,dc=example,dc=org",
 *     groupdn: "ou=Groups,dc=example,dc=org",
 * });
 * const developers = new vault.KerberosAuthBackendGroup("developers", {
 *     mount: kerberos.path,
 *     name: "developers",
 *     policies: [
 *         "dev-policy",
 *         "read-only",
 *     ],
 * });
 * ```
 *
 * ### Multiple Group Mappings
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const kerberos = new vault.AuthBackend("kerberos", {
 *     type: "kerberos",
 *     path: "kerberos",
 * });
 * const admins = new vault.KerberosAuthBackendGroup("admins", {
 *     mount: kerberos.path,
 *     name: "admins",
 *     policies: [
 *         "admin-policy",
 *         "default",
 *     ],
 * });
 * const developers = new vault.KerberosAuthBackendGroup("developers", {
 *     mount: kerberos.path,
 *     name: "developers",
 *     policies: [
 *         "dev-policy",
 *         "default",
 *     ],
 * });
 * const readonly = new vault.KerberosAuthBackendGroup("readonly", {
 *     mount: kerberos.path,
 *     name: "readonly-users",
 *     policies: ["read-only"],
 * });
 * ```
 *
 * ### Using Namespace (Vault Enterprise)
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const example = new vault.Namespace("example", {path: "example-namespace"});
 * const kerberos = new vault.AuthBackend("kerberos", {
 *     namespace: example.path,
 *     type: "kerberos",
 *     path: "kerberos",
 * });
 * const team = new vault.KerberosAuthBackendGroup("team", {
 *     namespace: example.path,
 *     mount: kerberos.path,
 *     name: "team-alpha",
 *     policies: ["team-alpha-policy"],
 * });
 * ```
 *
 * ### Group Without Policies
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vault from "@pulumi/vault";
 *
 * const kerberos = new vault.AuthBackend("kerberos", {
 *     type: "kerberos",
 *     path: "kerberos",
 * });
 * // Create a group mapping without policies
 * // Useful for tracking groups or for later policy assignment
 * const contractors = new vault.KerberosAuthBackendGroup("contractors", {
 *     mount: kerberos.path,
 *     name: "contractors",
 * });
 * ```
 *
 * ## Import
 *
 * Kerberos auth backend group mappings can be imported using the path, e.g.
 *
 * ```sh
 * $ pulumi import vault:index/kerberosAuthBackendGroup:KerberosAuthBackendGroup developers auth/kerberos/groups/developers
 * ```
 *
 * ### Importing with Namespace (Vault Enterprise)
 *
 * For Vault Enterprise with namespaces, set the `TERRAFORM_VAULT_NAMESPACE_IMPORT` environment variable
 * before importing:
 */
export class KerberosAuthBackendGroup extends pulumi.CustomResource {
    /**
     * Get an existing KerberosAuthBackendGroup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: KerberosAuthBackendGroupState, opts?: pulumi.CustomResourceOptions): KerberosAuthBackendGroup {
        return new KerberosAuthBackendGroup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vault:index/kerberosAuthBackendGroup:KerberosAuthBackendGroup';

    /**
     * Returns true if the given object is an instance of KerberosAuthBackendGroup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is KerberosAuthBackendGroup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === KerberosAuthBackendGroup.__pulumiType;
    }

    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     */
    declare public readonly mount: pulumi.Output<string>;
    /**
     * The name of the LDAP group to map to Vault policies.
     * This should match the group name as it appears in your LDAP directory.
     * Changing this will force a new resource to be created.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    declare public readonly namespace: pulumi.Output<string | undefined>;
    /**
     * Set of Vault policy names to associate with this group.
     * Users who are members of this LDAP group will receive these policies when they
     * authenticate via Kerberos.
     */
    declare public readonly policies: pulumi.Output<string[] | undefined>;

    /**
     * Create a KerberosAuthBackendGroup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: KerberosAuthBackendGroupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: KerberosAuthBackendGroupArgs | KerberosAuthBackendGroupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as KerberosAuthBackendGroupState | undefined;
            resourceInputs["mount"] = state?.mount;
            resourceInputs["name"] = state?.name;
            resourceInputs["namespace"] = state?.namespace;
            resourceInputs["policies"] = state?.policies;
        } else {
            const args = argsOrState as KerberosAuthBackendGroupArgs | undefined;
            if (args?.mount === undefined && !opts.urn) {
                throw new Error("Missing required property 'mount'");
            }
            resourceInputs["mount"] = args?.mount;
            resourceInputs["name"] = args?.name;
            resourceInputs["namespace"] = args?.namespace;
            resourceInputs["policies"] = args?.policies;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(KerberosAuthBackendGroup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering KerberosAuthBackendGroup resources.
 */
export interface KerberosAuthBackendGroupState {
    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     */
    mount?: pulumi.Input<string | undefined>;
    /**
     * The name of the LDAP group to map to Vault policies.
     * This should match the group name as it appears in your LDAP directory.
     * Changing this will force a new resource to be created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Set of Vault policy names to associate with this group.
     * Users who are members of this LDAP group will receive these policies when they
     * authenticate via Kerberos.
     */
    policies?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a KerberosAuthBackendGroup resource.
 */
export interface KerberosAuthBackendGroupArgs {
    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     */
    mount: pulumi.Input<string>;
    /**
     * The name of the LDAP group to map to Vault policies.
     * This should match the group name as it appears in your LDAP directory.
     * Changing this will force a new resource to be created.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider's configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     */
    namespace?: pulumi.Input<string | undefined>;
    /**
     * Set of Vault policy names to associate with this group.
     * Users who are members of this LDAP group will receive these policies when they
     * authenticate via Kerberos.
     */
    policies?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

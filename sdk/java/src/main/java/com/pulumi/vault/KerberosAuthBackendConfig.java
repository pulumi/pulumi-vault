// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.KerberosAuthBackendConfigArgs;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.inputs.KerberosAuthBackendConfigState;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.String;
import java.util.List;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages the configuration of a Kerberos Auth Backend in Vault.
 * 
 * This resource configures the Kerberos authentication method by providing the keytab
 * and service account information required for Vault to authenticate users via Kerberos.
 * 
 * For more information, see the
 * [Vault docs](https://www.vaultproject.io/docs/auth/kerberos).
 * 
 * &gt; **Important** The `keytabWo` field is write-only and is not stored in Terraform state.
 * It is only sent to Vault when the `keytabWoVersion` changes or during initial creation.
 * See the main provider documentation for more details.
 * 
 * &gt; **Note** Vault does not support deleting auth backend configurations via the API.
 * When this resource is destroyed or replaced (e.g., when changing the `mount`), it is
 * only removed from Terraform state. The configuration remains in Vault until the auth
 * mount itself is deleted.
 * 
 * ## Example Usage
 * 
 * ### Basic Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.AuthBackend;
 * import com.pulumi.vault.AuthBackendArgs;
 * import com.pulumi.vault.KerberosAuthBackendConfig;
 * import com.pulumi.vault.KerberosAuthBackendConfigArgs;
 * import com.pulumi.std.StdFunctions;
 * import com.pulumi.std.inputs.Filebase64Args;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App }{{@code
 *     public static void main(String[] args) }{{@code
 *         Pulumi.run(App::stack);
 *     }}{@code
 * 
 *     public static void stack(Context ctx) }{{@code
 *         var kerberos = new AuthBackend("kerberos", AuthBackendArgs.builder()
 *             .type("kerberos")
 *             .path("kerberos")
 *             .build());
 * 
 *         var config = new KerberosAuthBackendConfig("config", KerberosAuthBackendConfigArgs.builder()
 *             .mount(kerberos.path())
 *             .keytabWo(StdFunctions.filebase64(Filebase64Args.builder()
 *                 .input("/path/to/vault.keytab")
 *                 .build()).result())
 *             .keytabWoVersion(1)
 *             .serviceAccount("vault/localhost}{@literal @}{@code EXAMPLE.COM")
 *             .build());
 * 
 *     }}{@code
 * }}{@code
 * }
 * </pre>
 * 
 * ### Full Configuration with All Options
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.AuthBackend;
 * import com.pulumi.vault.AuthBackendArgs;
 * import com.pulumi.vault.KerberosAuthBackendConfig;
 * import com.pulumi.vault.KerberosAuthBackendConfigArgs;
 * import com.pulumi.std.StdFunctions;
 * import com.pulumi.std.inputs.Filebase64Args;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App }{{@code
 *     public static void main(String[] args) }{{@code
 *         Pulumi.run(App::stack);
 *     }}{@code
 * 
 *     public static void stack(Context ctx) }{{@code
 *         var kerberos = new AuthBackend("kerberos", AuthBackendArgs.builder()
 *             .type("kerberos")
 *             .path("kerberos")
 *             .build());
 * 
 *         var config = new KerberosAuthBackendConfig("config", KerberosAuthBackendConfigArgs.builder()
 *             .mount(kerberos.path())
 *             .keytabWo(StdFunctions.filebase64(Filebase64Args.builder()
 *                 .input("/path/to/vault.keytab")
 *                 .build()).result())
 *             .keytabWoVersion(1)
 *             .serviceAccount("vault/localhost}{@literal @}{@code EXAMPLE.COM")
 *             .removeInstanceName(true)
 *             .addGroupAliases(true)
 *             .build());
 * 
 *     }}{@code
 * }}{@code
 * }
 * </pre>
 * 
 * ### Using Namespace (Vault Enterprise)
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Namespace;
 * import com.pulumi.vault.NamespaceArgs;
 * import com.pulumi.vault.AuthBackend;
 * import com.pulumi.vault.AuthBackendArgs;
 * import com.pulumi.vault.KerberosAuthBackendConfig;
 * import com.pulumi.vault.KerberosAuthBackendConfigArgs;
 * import com.pulumi.std.StdFunctions;
 * import com.pulumi.std.inputs.Filebase64Args;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App }{{@code
 *     public static void main(String[] args) }{{@code
 *         Pulumi.run(App::stack);
 *     }}{@code
 * 
 *     public static void stack(Context ctx) }{{@code
 *         var example = new Namespace("example", NamespaceArgs.builder()
 *             .path("example-namespace")
 *             .build());
 * 
 *         var kerberos = new AuthBackend("kerberos", AuthBackendArgs.builder()
 *             .namespace(example.path())
 *             .type("kerberos")
 *             .path("kerberos")
 *             .build());
 * 
 *         var config = new KerberosAuthBackendConfig("config", KerberosAuthBackendConfigArgs.builder()
 *             .namespace(example.path())
 *             .mount(kerberos.path())
 *             .keytabWo(StdFunctions.filebase64(Filebase64Args.builder()
 *                 .input("/path/to/vault.keytab")
 *                 .build()).result())
 *             .keytabWoVersion(1)
 *             .serviceAccount("vault/localhost}{@literal @}{@code EXAMPLE.COM")
 *             .build());
 * 
 *     }}{@code
 * }}{@code
 * }
 * </pre>
 * 
 * ## Import
 * 
 * Kerberos auth backend configurations can be imported using the `path`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig config auth/kerberos/config
 * ```
 * 
 * &gt; **Note** The `keytabWo` field cannot be imported as it is write-only and not stored
 * in state. You will need to provide it in your configuration after import.
 * 
 * ### Importing with Namespace (Vault Enterprise)
 * 
 * For Vault Enterprise with namespaces, set the `TERRAFORM_VAULT_NAMESPACE_IMPORT` environment variable
 * before importing:
 * 
 */
@ResourceType(type="vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig")
public class KerberosAuthBackendConfig extends com.pulumi.resources.CustomResource {
    /**
     * Adds group aliases during authentication. When
     * enabled, Vault will create entity aliases for each group the user belongs to.
     * Defaults to `false`.
     * 
     */
    @Export(name="addGroupAliases", refs={Boolean.class}, tree="[0]")
    private Output</* @Nullable */ Boolean> addGroupAliases;

    /**
     * @return Adds group aliases during authentication. When
     * enabled, Vault will create entity aliases for each group the user belongs to.
     * Defaults to `false`.
     * 
     */
    public Output<Optional<Boolean>> addGroupAliases() {
        return Codegen.optional(this.addGroupAliases);
    }
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded keytab file content. This is a write-only
     * field and is not stored in Terraform state. The keytab must contain an entry
     * matching the `serviceAccount`.
     * 
     */
    @Export(name="keytabWo", refs={String.class}, tree="[0]")
    private Output<String> keytabWo;

    /**
     * @return **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * Base64-encoded keytab file content. This is a write-only
     * field and is not stored in Terraform state. The keytab must contain an entry
     * matching the `serviceAccount`.
     * 
     */
    public Output<String> keytabWo() {
        return this.keytabWo;
    }
    /**
     * Version identifier for keytab updates. Increment this
     * value to trigger a keytab update in Vault. This allows you to rotate the keytab without
     * forcing resource replacement.
     * 
     */
    @Export(name="keytabWoVersion", refs={Integer.class}, tree="[0]")
    private Output<Integer> keytabWoVersion;

    /**
     * @return Version identifier for keytab updates. Increment this
     * value to trigger a keytab update in Vault. This allows you to rotate the keytab without
     * forcing resource replacement.
     * 
     */
    public Output<Integer> keytabWoVersion() {
        return this.keytabWoVersion;
    }
    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }
    /**
     * Removes instance names from Kerberos service
     * principal names during authentication. This can be useful when the instance name
     * is not relevant for authentication. Defaults to `false`.
     * 
     */
    @Export(name="removeInstanceName", refs={Boolean.class}, tree="[0]")
    private Output</* @Nullable */ Boolean> removeInstanceName;

    /**
     * @return Removes instance names from Kerberos service
     * principal names during authentication. This can be useful when the instance name
     * is not relevant for authentication. Defaults to `false`.
     * 
     */
    public Output<Optional<Boolean>> removeInstanceName() {
        return Codegen.optional(this.removeInstanceName);
    }
    /**
     * The Kerberos service account associated with the
     * keytab entry (e.g., `vault/localhost{@literal @}EXAMPLE.COM` or `vaultSvc`).
     * 
     */
    @Export(name="serviceAccount", refs={String.class}, tree="[0]")
    private Output<String> serviceAccount;

    /**
     * @return The Kerberos service account associated with the
     * keytab entry (e.g., `vault/localhost{@literal @}EXAMPLE.COM` or `vaultSvc`).
     * 
     */
    public Output<String> serviceAccount() {
        return this.serviceAccount;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public KerberosAuthBackendConfig(java.lang.String name) {
        this(name, KerberosAuthBackendConfigArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public KerberosAuthBackendConfig(java.lang.String name, KerberosAuthBackendConfigArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public KerberosAuthBackendConfig(java.lang.String name, KerberosAuthBackendConfigArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private KerberosAuthBackendConfig(java.lang.String name, Output<java.lang.String> id, @Nullable KerberosAuthBackendConfigState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/kerberosAuthBackendConfig:KerberosAuthBackendConfig", name, state, makeResourceOptions(options, id), false);
    }

    private static KerberosAuthBackendConfigArgs makeArgs(KerberosAuthBackendConfigArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? KerberosAuthBackendConfigArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .additionalSecretOutputs(List.of(
                "keytabWo"
            ))
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static KerberosAuthBackendConfig get(java.lang.String name, Output<java.lang.String> id, @Nullable KerberosAuthBackendConfigState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new KerberosAuthBackendConfig(name, id, state, options);
    }
}

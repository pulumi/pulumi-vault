// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault.config;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.config.UiDefaultAuthArgs;
import com.pulumi.vault.config.inputs.UiDefaultAuthState;
import java.lang.Boolean;
import java.lang.String;
import java.util.List;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages the UI default authentication configuration for the Vault GUI login form. This resource configures which authentication method is displayed by default on the Vault UI login page, along with optional backup authentication methods that appear in the &#34;Sign in with other methods&#34; tab.
 * 
 * &gt; **Important** This feature is available only with Vault Enterprise 1.20.0 or later.
 * 
 * ## Example Usage
 * 
 * ### Basic Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiDefaultAuth;
 * import com.pulumi.vault.config.UiDefaultAuthArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var example = new UiDefaultAuth("example", UiDefaultAuthArgs.builder()
 *             .name("my-auth-config")
 *             .defaultAuthType("ldap")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Configuration with Backup Methods
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiDefaultAuth;
 * import com.pulumi.vault.config.UiDefaultAuthArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var example = new UiDefaultAuth("example", UiDefaultAuthArgs.builder()
 *             .name("my-auth-config")
 *             .defaultAuthType("oidc")
 *             .backupAuthTypes(            
 *                 "ldap",
 *                 "userpass",
 *                 "token")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Configuration for Specific Namespace
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiDefaultAuth;
 * import com.pulumi.vault.config.UiDefaultAuthArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var adminConfig = new UiDefaultAuth("adminConfig", UiDefaultAuthArgs.builder()
 *             .name("admin-auth-config")
 *             .namespacePath("admin")
 *             .defaultAuthType("ldap")
 *             .backupAuthTypes("token")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Configuration with Inheritance Disabled
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiDefaultAuth;
 * import com.pulumi.vault.config.UiDefaultAuthArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var parentConfig = new UiDefaultAuth("parentConfig", UiDefaultAuthArgs.builder()
 *             .name("parent-auth-config")
 *             .namespacePath("parent")
 *             .defaultAuthType("oidc")
 *             .backupAuthTypes(            
 *                 "github",
 *                 "token")
 *             .disableInheritance(true)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Complete Configuration Example
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiDefaultAuth;
 * import com.pulumi.vault.config.UiDefaultAuthArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var complete = new UiDefaultAuth("complete", UiDefaultAuthArgs.builder()
 *             .name("complete-auth-config")
 *             .namespacePath("engineering")
 *             .defaultAuthType("oidc")
 *             .backupAuthTypes(            
 *                 "ldap",
 *                 "userpass",
 *                 "token")
 *             .disableInheritance(true)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Notes
 * 
 * * **Enterprise Only**: This resource requires Vault Enterprise 1.20.0 or later.
 * 
 * * **Authentication Methods**: The authentication methods specified in `defaultAuthType` and `backupAuthTypes` must be enabled in Vault before they can be used in the UI configuration. The resource validates that only supported auth types are used.
 * 
 * * **Root Namespace**: When `namespacePath` is empty, omitted, or set to `&#34;root&#34;` or `&#34;root/&#34;`, the configuration applies to the root namespace. All these values are treated equivalently.
 * 
 * * **Order Preservation**: The order of methods in `backupAuthTypes` is preserved and determines the display order in the Vault UI&#39;s &#34;Sign in with other methods&#34; tab.
 * 
 * * **Supported Auth Types**: The following authentication types are supported: `github`, `jwt`, `ldap`, `oidc`, `okta`, `radius`, `saml`, `token`, and `userpass`. These correspond to Vault&#39;s authentication methods.
 * 
 * ## API Documentation
 * 
 * For more details on the underlying Vault API, see the [Vault UI Default Auth API documentation](https://developer.hashicorp.com/vault/api-docs/system/config-ui-login-default-auth).
 * 
 * ## Import
 * 
 * UI default authentication configurations can be imported using the `name`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:config/uiDefaultAuth:UiDefaultAuth example my-auth-config
 * ```
 * 
 * ### Importing with Namespaces
 * 
 * When importing a configuration that exists in a specific namespace, you must set the `TERRAFORM_VAULT_NAMESPACE_IMPORT` environment variable to ensure proper resource management:
 * 
 * ```sh
 * $ export TERRAFORM_VAULT_NAMESPACE_IMPORT=admin
 * $ pulumi import vault:config/uiDefaultAuth:UiDefaultAuth example my-auth-config
 * ```
 * 
 * Without setting the namespace during import, subsequent operations (update/delete) may target the wrong namespace and fail.
 * 
 */
@ResourceType(type="vault:config/uiDefaultAuth:UiDefaultAuth")
public class UiDefaultAuth extends com.pulumi.resources.CustomResource {
    /**
     * List of backup authentication methods displayed in the &#34;Sign in with other methods&#34; tab in the Vault UI. Each value must be a valid authentication type from the same list as `defaultAuthType`. The order of methods is preserved as specified.
     * 
     */
    @Export(name="backupAuthTypes", refs={List.class,String.class}, tree="[0,1]")
    private Output</* @Nullable */ List<String>> backupAuthTypes;

    /**
     * @return List of backup authentication methods displayed in the &#34;Sign in with other methods&#34; tab in the Vault UI. Each value must be a valid authentication type from the same list as `defaultAuthType`. The order of methods is preserved as specified.
     * 
     */
    public Output<Optional<List<String>>> backupAuthTypes() {
        return Codegen.optional(this.backupAuthTypes);
    }
    /**
     * The default authentication method displayed on the Vault UI login page. Must be one of: `github`, `jwt`, `ldap`, `oidc`, `okta`, `radius`, `saml`, `token`, or `userpass`.
     * 
     */
    @Export(name="defaultAuthType", refs={String.class}, tree="[0]")
    private Output<String> defaultAuthType;

    /**
     * @return The default authentication method displayed on the Vault UI login page. Must be one of: `github`, `jwt`, `ldap`, `oidc`, `okta`, `radius`, `saml`, `token`, or `userpass`.
     * 
     */
    public Output<String> defaultAuthType() {
        return this.defaultAuthType;
    }
    /**
     * If `true`, child namespaces will not inherit the `defaultAuthType` and `backupAuthTypes` from this configuration. Defaults to `false`.
     * 
     */
    @Export(name="disableInheritance", refs={Boolean.class}, tree="[0]")
    private Output</* @Nullable */ Boolean> disableInheritance;

    /**
     * @return If `true`, child namespaces will not inherit the `defaultAuthType` and `backupAuthTypes` from this configuration. Defaults to `false`.
     * 
     */
    public Output<Optional<Boolean>> disableInheritance() {
        return Codegen.optional(this.disableInheritance);
    }
    /**
     * Unique identifier for the configuration. Can contain letters, numbers, underscores, and dashes. Changing this forces resource recreation.
     * 
     */
    @Export(name="name", refs={String.class}, tree="[0]")
    private Output<String> name;

    /**
     * @return Unique identifier for the configuration. Can contain letters, numbers, underscores, and dashes. Changing this forces resource recreation.
     * 
     */
    public Output<String> name() {
        return this.name;
    }
    /**
     * Target namespace. (requires Enterprise)
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return Target namespace. (requires Enterprise)
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }
    /**
     * Target namespace for the configuration. Omit or leave empty to apply the configuration to the root namespace. Values `&#34;&#34;`, `&#34;root&#34;`, and `&#34;root/&#34;` are all treated as the root namespace. Vault returns namespace paths with trailing slashes, which are normalized to match your configuration format.
     * 
     */
    @Export(name="namespacePath", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespacePath;

    /**
     * @return Target namespace for the configuration. Omit or leave empty to apply the configuration to the root namespace. Values `&#34;&#34;`, `&#34;root&#34;`, and `&#34;root/&#34;` are all treated as the root namespace. Vault returns namespace paths with trailing slashes, which are normalized to match your configuration format.
     * 
     */
    public Output<Optional<String>> namespacePath() {
        return Codegen.optional(this.namespacePath);
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public UiDefaultAuth(java.lang.String name) {
        this(name, UiDefaultAuthArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public UiDefaultAuth(java.lang.String name, UiDefaultAuthArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public UiDefaultAuth(java.lang.String name, UiDefaultAuthArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:config/uiDefaultAuth:UiDefaultAuth", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private UiDefaultAuth(java.lang.String name, Output<java.lang.String> id, @Nullable UiDefaultAuthState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:config/uiDefaultAuth:UiDefaultAuth", name, state, makeResourceOptions(options, id), false);
    }

    private static UiDefaultAuthArgs makeArgs(UiDefaultAuthArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? UiDefaultAuthArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static UiDefaultAuth get(java.lang.String name, Output<java.lang.String> id, @Nullable UiDefaultAuthState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new UiDefaultAuth(name, id, state, options);
    }
}

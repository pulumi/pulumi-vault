// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.KerberosAuthBackendGroupArgs;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.inputs.KerberosAuthBackendGroupState;
import java.lang.String;
import java.util.List;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages LDAP group to Vault policy mappings for the Kerberos authentication method in Vault.
 * 
 * This resource allows you to map LDAP groups to Vault policies when using the Kerberos
 * authentication method. After a user successfully authenticates via Kerberos, Vault queries
 * LDAP to determine the user&#39;s group memberships. These group mappings then
 * determine which Vault policies are assigned to the authenticated user&#39;s token.
 * 
 * For more information, see the
 * [Vault Kerberos Auth Method documentation](https://www.vaultproject.io/docs/auth/kerberos).
 * 
 * &gt; **Note** This resource requires that LDAP integration be configured for the Kerberos
 * auth method using `vault.KerberosAuthBackendLdapConfig`.
 * Without LDAP configuration, group mappings cannot be resolved.
 * 
 * ## Example Usage
 * 
 * ### Basic Group Mapping
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.AuthBackend;
 * import com.pulumi.vault.AuthBackendArgs;
 * import com.pulumi.vault.KerberosAuthBackendConfig;
 * import com.pulumi.vault.KerberosAuthBackendConfigArgs;
 * import com.pulumi.std.StdFunctions;
 * import com.pulumi.std.inputs.Filebase64Args;
 * import com.pulumi.vault.KerberosAuthBackendLdapConfig;
 * import com.pulumi.vault.KerberosAuthBackendLdapConfigArgs;
 * import com.pulumi.vault.KerberosAuthBackendGroup;
 * import com.pulumi.vault.KerberosAuthBackendGroupArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App }{{@code
 *     public static void main(String[] args) }{{@code
 *         Pulumi.run(App::stack);
 *     }}{@code
 * 
 *     public static void stack(Context ctx) }{{@code
 *         var kerberos = new AuthBackend("kerberos", AuthBackendArgs.builder()
 *             .type("kerberos")
 *             .path("kerberos")
 *             .build());
 * 
 *         var kerberosKerberosAuthBackendConfig = new KerberosAuthBackendConfig("kerberosKerberosAuthBackendConfig", KerberosAuthBackendConfigArgs.builder()
 *             .mount(kerberos.path())
 *             .keytabWo(StdFunctions.filebase64(Filebase64Args.builder()
 *                 .input("/path/to/vault.keytab")
 *                 .build()).result())
 *             .keytabWoVersion(1)
 *             .serviceAccount("vault/localhost}{@literal @}{@code EXAMPLE.COM")
 *             .build());
 * 
 *         var ldap = new KerberosAuthBackendLdapConfig("ldap", KerberosAuthBackendLdapConfigArgs.builder()
 *             .mount(kerberos.path())
 *             .url("ldap://ldap.example.com")
 *             .binddn("cn=vault,ou=Users,dc=example,dc=com")
 *             .userdn("ou=People,dc=example,dc=org")
 *             .groupdn("ou=Groups,dc=example,dc=org")
 *             .build());
 * 
 *         var developers = new KerberosAuthBackendGroup("developers", KerberosAuthBackendGroupArgs.builder()
 *             .mount(kerberos.path())
 *             .name("developers")
 *             .policies(            
 *                 "dev-policy",
 *                 "read-only")
 *             .build());
 * 
 *     }}{@code
 * }}{@code
 * }
 * </pre>
 * 
 * ### Multiple Group Mappings
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.AuthBackend;
 * import com.pulumi.vault.AuthBackendArgs;
 * import com.pulumi.vault.KerberosAuthBackendGroup;
 * import com.pulumi.vault.KerberosAuthBackendGroupArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var kerberos = new AuthBackend("kerberos", AuthBackendArgs.builder()
 *             .type("kerberos")
 *             .path("kerberos")
 *             .build());
 * 
 *         var admins = new KerberosAuthBackendGroup("admins", KerberosAuthBackendGroupArgs.builder()
 *             .mount(kerberos.path())
 *             .name("admins")
 *             .policies(            
 *                 "admin-policy",
 *                 "default")
 *             .build());
 * 
 *         var developers = new KerberosAuthBackendGroup("developers", KerberosAuthBackendGroupArgs.builder()
 *             .mount(kerberos.path())
 *             .name("developers")
 *             .policies(            
 *                 "dev-policy",
 *                 "default")
 *             .build());
 * 
 *         var readonly = new KerberosAuthBackendGroup("readonly", KerberosAuthBackendGroupArgs.builder()
 *             .mount(kerberos.path())
 *             .name("readonly-users")
 *             .policies("read-only")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Using Namespace (Vault Enterprise)
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Namespace;
 * import com.pulumi.vault.NamespaceArgs;
 * import com.pulumi.vault.AuthBackend;
 * import com.pulumi.vault.AuthBackendArgs;
 * import com.pulumi.vault.KerberosAuthBackendGroup;
 * import com.pulumi.vault.KerberosAuthBackendGroupArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var example = new Namespace("example", NamespaceArgs.builder()
 *             .path("example-namespace")
 *             .build());
 * 
 *         var kerberos = new AuthBackend("kerberos", AuthBackendArgs.builder()
 *             .namespace(example.path())
 *             .type("kerberos")
 *             .path("kerberos")
 *             .build());
 * 
 *         var team = new KerberosAuthBackendGroup("team", KerberosAuthBackendGroupArgs.builder()
 *             .namespace(example.path())
 *             .mount(kerberos.path())
 *             .name("team-alpha")
 *             .policies("team-alpha-policy")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Group Without Policies
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.AuthBackend;
 * import com.pulumi.vault.AuthBackendArgs;
 * import com.pulumi.vault.KerberosAuthBackendGroup;
 * import com.pulumi.vault.KerberosAuthBackendGroupArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var kerberos = new AuthBackend("kerberos", AuthBackendArgs.builder()
 *             .type("kerberos")
 *             .path("kerberos")
 *             .build());
 * 
 *         // Create a group mapping without policies
 *         // Useful for tracking groups or for later policy assignment
 *         var contractors = new KerberosAuthBackendGroup("contractors", KerberosAuthBackendGroupArgs.builder()
 *             .mount(kerberos.path())
 *             .name("contractors")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Import
 * 
 * Kerberos auth backend group mappings can be imported using the path, e.g.
 * 
 * ```sh
 * $ pulumi import vault:index/kerberosAuthBackendGroup:KerberosAuthBackendGroup developers auth/kerberos/groups/developers
 * ```
 * 
 * ### Importing with Namespace (Vault Enterprise)
 * 
 * For Vault Enterprise with namespaces, set the `TERRAFORM_VAULT_NAMESPACE_IMPORT` environment variable
 * before importing:
 * 
 */
@ResourceType(type="vault:index/kerberosAuthBackendGroup:KerberosAuthBackendGroup")
public class KerberosAuthBackendGroup extends com.pulumi.resources.CustomResource {
    /**
     * Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return Path where the Kerberos auth method is mounted.
     * Changing this will force a new resource to be created.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * The name of the LDAP group to map to Vault policies.
     * This should match the group name as it appears in your LDAP directory.
     * Changing this will force a new resource to be created.
     * 
     */
    @Export(name="name", refs={String.class}, tree="[0]")
    private Output<String> name;

    /**
     * @return The name of the LDAP group to map to Vault policies.
     * This should match the group name as it appears in your LDAP directory.
     * Changing this will force a new resource to be created.
     * 
     */
    public Output<String> name() {
        return this.name;
    }
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }
    /**
     * Set of Vault policy names to associate with this group.
     * Users who are members of this LDAP group will receive these policies when they
     * authenticate via Kerberos.
     * 
     */
    @Export(name="policies", refs={List.class,String.class}, tree="[0,1]")
    private Output</* @Nullable */ List<String>> policies;

    /**
     * @return Set of Vault policy names to associate with this group.
     * Users who are members of this LDAP group will receive these policies when they
     * authenticate via Kerberos.
     * 
     */
    public Output<Optional<List<String>>> policies() {
        return Codegen.optional(this.policies);
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public KerberosAuthBackendGroup(java.lang.String name) {
        this(name, KerberosAuthBackendGroupArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public KerberosAuthBackendGroup(java.lang.String name, KerberosAuthBackendGroupArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public KerberosAuthBackendGroup(java.lang.String name, KerberosAuthBackendGroupArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/kerberosAuthBackendGroup:KerberosAuthBackendGroup", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private KerberosAuthBackendGroup(java.lang.String name, Output<java.lang.String> id, @Nullable KerberosAuthBackendGroupState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/kerberosAuthBackendGroup:KerberosAuthBackendGroup", name, state, makeResourceOptions(options, id), false);
    }

    private static KerberosAuthBackendGroupArgs makeArgs(KerberosAuthBackendGroupArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? KerberosAuthBackendGroupArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static KerberosAuthBackendGroup get(java.lang.String name, Output<java.lang.String> id, @Nullable KerberosAuthBackendGroupState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new KerberosAuthBackendGroup(name, id, state, options);
    }
}

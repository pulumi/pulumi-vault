// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.OsSecretBackendHostArgs;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.inputs.OsSecretBackendHostState;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.String;
import java.util.Map;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages host configurations in the OS Secrets Engine. Hosts represent remote systems where
 * Vault will manage operating system account credentials via SSH. This resource requires Vault 2.0.0 or later.
 * 
 * The OS Secrets Engine mount itself is managed separately, typically with `vault.Mount`. This resource manages
 * hosts beneath an existing OS mount.
 * 
 * Before mounting the OS Secrets Engine, the external OS plugin must already be registered in Vault&#39;s plugin catalog.
 * You can register it with the `vault.Plugin` resource.
 * 
 * The examples below use the canonical plugin name `vault-plugin-secrets-os`. If your Vault cluster registers the
 * OS plugin under a different catalog name, use that name in `vault_mount.type` instead.
 * 
 * See the [Vault documentation](https://www.vaultproject.io/docs/secrets/os) for more information.
 * 
 * ## Example Usage
 * 
 * ### Register Plugin And Configure Host
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Plugin;
 * import com.pulumi.vault.PluginArgs;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Plugin("os", PluginArgs.builder()
 *             .type("secret")
 *             .name("vault-plugin-secrets-os")
 *             .version("v0.1.0+ent")
 *             .build());
 * 
 *         var osMount = new Mount("osMount", MountArgs.builder()
 *             .path("os")
 *             .type(os.name())
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(osMount.path())
 *             .build());
 * 
 *         var example = new OsSecretBackendHost("example", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("web-server-01")
 *             .address("192.168.1.100")
 *             .port(22)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Basic Host Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .build());
 * 
 *         var example = new OsSecretBackendHost("example", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("web-server-01")
 *             .address("192.168.1.100")
 *             .port(22)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Advanced Host Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .build());
 * 
 *         var production = new OsSecretBackendHost("production", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("prod-db-01")
 *             .address("10.0.1.50")
 *             .port(2222)
 *             .rotationSchedule("0 2 * * *")
 *             .rotationWindow(3600)
 *             .customMetadata(Map.ofEntries(
 *                 Map.entry("environment", "production"),
 *                 Map.entry("team", "database"),
 *                 Map.entry("criticality", "high")
 *             ))
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Host with SSH Host Key
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .build());
 * 
 *         var secure = new OsSecretBackendHost("secure", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("secure-host")
 *             .address("192.168.1.200")
 *             .port(22)
 *             .sshHostKey("ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQC...")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Notes
 * 
 * * This resource requires Vault 2.0.0 or later.
 * * The OS Secrets Engine plugin must be registered before the mount is enabled. Use `vault.Plugin` to manage catalog registration when appropriate.
 * * Use `vault.Mount` to create, tune, or remove the OS Secrets Engine mount before managing hosts with this resource.
 * * The host must be configured before accounts can be created on it.
 * * When `sshHostKey` is not provided, the backend&#39;s `sshHostKeyTrustOnFirstUse` setting determines whether the host key will be automatically trusted on first connection.
 * * Use either `rotationPeriod` or `rotationSchedule`.
 * * Custom metadata is stored alongside the host configuration and can be used for organizational purposes, but does not affect host behavior.
 * * Changing `mount` or `name` will cause the resource to be recreated.
 * 
 * ## Import
 * 
 * OS Secret backend host can be imported using the format `&lt;mount&gt;/hosts/&lt;name&gt;`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:index/osSecretBackendHost:OsSecretBackendHost example os/hosts/web-server-01
 * ```
 * 
 */
@ResourceType(type="vault:index/osSecretBackendHost:OsSecretBackendHost")
public class OsSecretBackendHost extends com.pulumi.resources.CustomResource {
    /**
     * The address of the host (IP address or hostname).
     * 
     */
    @Export(name="address", refs={String.class}, tree="[0]")
    private Output<String> address;

    /**
     * @return The address of the host (IP address or hostname).
     * 
     */
    public Output<String> address() {
        return this.address;
    }
    /**
     * A map of string key-value pairs for storing custom metadata about the host.
     * 
     */
    @Export(name="customMetadata", refs={Map.class,String.class}, tree="[0,1,1]")
    private Output</* @Nullable */ Map<String,String>> customMetadata;

    /**
     * @return A map of string key-value pairs for storing custom metadata about the host.
     * 
     */
    public Output<Optional<Map<String,String>>> customMetadata() {
        return Codegen.optional(this.customMetadata);
    }
    /**
     * Disables automated rotation for the host.
     * 
     */
    @Export(name="disableAutomatedRotation", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> disableAutomatedRotation;

    /**
     * @return Disables automated rotation for the host.
     * 
     */
    public Output<Boolean> disableAutomatedRotation() {
        return this.disableAutomatedRotation;
    }
    /**
     * The path where the OS secrets engine is mounted.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return The path where the OS secrets engine is mounted.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * Unique name for the host within the mount.
     * 
     */
    @Export(name="name", refs={String.class}, tree="[0]")
    private Output<String> name;

    /**
     * @return Unique name for the host within the mount.
     * 
     */
    public Output<String> name() {
        return this.name;
    }
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }
    /**
     * The password policy inherited by accounts on this host unless overridden at the account level.
     * 
     */
    @Export(name="passwordPolicy", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> passwordPolicy;

    /**
     * @return The password policy inherited by accounts on this host unless overridden at the account level.
     * 
     */
    public Output<Optional<String>> passwordPolicy() {
        return Codegen.optional(this.passwordPolicy);
    }
    /**
     * The port to connect to on the host. Defaults to `22`.
     * 
     */
    @Export(name="port", refs={Integer.class}, tree="[0]")
    private Output<Integer> port;

    /**
     * @return The port to connect to on the host. Defaults to `22`.
     * 
     */
    public Output<Integer> port() {
        return this.port;
    }
    /**
     * How often to rotate credentials, in seconds. Mutually exclusive with `rotationSchedule`.
     * 
     */
    @Export(name="rotationPeriod", refs={Integer.class}, tree="[0]")
    private Output<Integer> rotationPeriod;

    /**
     * @return How often to rotate credentials, in seconds. Mutually exclusive with `rotationSchedule`.
     * 
     */
    public Output<Integer> rotationPeriod() {
        return this.rotationPeriod;
    }
    /**
     * A cron-style schedule for credential rotation (for example, `&#34;0 2 * * *&#34;`). Mutually exclusive with `rotationPeriod`.
     * 
     */
    @Export(name="rotationSchedule", refs={String.class}, tree="[0]")
    private Output<String> rotationSchedule;

    /**
     * @return A cron-style schedule for credential rotation (for example, `&#34;0 2 * * *&#34;`). Mutually exclusive with `rotationPeriod`.
     * 
     */
    public Output<String> rotationSchedule() {
        return this.rotationSchedule;
    }
    /**
     * The rotation window, in seconds. This is typically used with `rotationSchedule`.
     * 
     */
    @Export(name="rotationWindow", refs={Integer.class}, tree="[0]")
    private Output<Integer> rotationWindow;

    /**
     * @return The rotation window, in seconds. This is typically used with `rotationSchedule`.
     * 
     */
    public Output<Integer> rotationWindow() {
        return this.rotationWindow;
    }
    /**
     * The SSH host key for the remote host. If not provided and `sshHostKeyTrustOnFirstUse` is enabled on the backend, Vault can learn and persist the key on first connection.
     * 
     */
    @Export(name="sshHostKey", refs={String.class}, tree="[0]")
    private Output<String> sshHostKey;

    /**
     * @return The SSH host key for the remote host. If not provided and `sshHostKeyTrustOnFirstUse` is enabled on the backend, Vault can learn and persist the key on first connection.
     * 
     */
    public Output<String> sshHostKey() {
        return this.sshHostKey;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public OsSecretBackendHost(java.lang.String name) {
        this(name, OsSecretBackendHostArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public OsSecretBackendHost(java.lang.String name, OsSecretBackendHostArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public OsSecretBackendHost(java.lang.String name, OsSecretBackendHostArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/osSecretBackendHost:OsSecretBackendHost", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private OsSecretBackendHost(java.lang.String name, Output<java.lang.String> id, @Nullable OsSecretBackendHostState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/osSecretBackendHost:OsSecretBackendHost", name, state, makeResourceOptions(options, id), false);
    }

    private static OsSecretBackendHostArgs makeArgs(OsSecretBackendHostArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? OsSecretBackendHostArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static OsSecretBackendHost get(java.lang.String name, Output<java.lang.String> id, @Nullable OsSecretBackendHostState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new OsSecretBackendHost(name, id, state, options);
    }
}

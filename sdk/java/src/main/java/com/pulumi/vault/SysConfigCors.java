// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.SysConfigCorsArgs;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.inputs.SysConfigCorsState;
import java.lang.Boolean;
import java.lang.String;
import java.util.List;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages the CORS (Cross-Origin Resource Sharing) configuration for Vault, controlling which origins can make cross-origin requests and which headers are allowed.
 * 
 * &gt; **Important** This resource requires `sudo` capability and must be called from the root namespace. CORS configuration does not replicate across Performance Replication clusters in Vault Enterprise.
 * 
 * **Note** This feature is available in Vault 1.14+
 * 
 * ## Example Usage
 * 
 * ### Enable CORS for specific origins
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.SysConfigCors;
 * import com.pulumi.vault.SysConfigCorsArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var example = new SysConfigCors("example", SysConfigCorsArgs.builder()
 *             .allowedOrigins(            
 *                 "http://www.example.com",
 *                 "https://app.example.com")
 *             .allowedHeaders(            
 *                 "X-Custom-Header",
 *                 "X-Application-ID")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Allow all origins (wildcard)
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.SysConfigCors;
 * import com.pulumi.vault.SysConfigCorsArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var wildcard = new SysConfigCors("wildcard", SysConfigCorsArgs.builder()
 *             .allowedOrigins("*")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Production environment configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.SysConfigCors;
 * import com.pulumi.vault.SysConfigCorsArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var production = new SysConfigCors("production", SysConfigCorsArgs.builder()
 *             .allowedOrigins(            
 *                 "https://app.example.com",
 *                 "https://admin.example.com",
 *                 "https://api.example.com")
 *             .allowedHeaders(            
 *                 "X-Custom-Header",
 *                 "X-Request-ID",
 *                 "X-Application-Version")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Security Considerations
 * 
 * * The wildcard `&#34;*&#34;` origin should be used cautiously and typically only in development environments
 * * Allowing all origins in production can expose Vault to cross-site request forgery attacks
 * * Custom headers should be carefully reviewed to ensure they don&#39;t expose sensitive information
 * * CORS settings must be configured from the root namespace
 * * In Vault Enterprise with Performance Replication, CORS configuration does not replicate across clusters - each secondary cluster must have its CORS configuration set independently
 * * To disable CORS, delete the resource using `terraform destroy` or remove it from your configuration. There is no `enabled = false` option
 * 
 * ## API Documentation
 * 
 * For more information on the Vault CORS configuration API, see the [Vault API documentation](https://developer.hashicorp.com/vault/api-docs/system/config-cors).
 * 
 * ## Import
 * 
 * CORS configuration can be imported using the fixed ID `sys/config/cors`:
 * 
 * ```sh
 * $ pulumi import vault:index/sysConfigCors:SysConfigCors example sys/config/cors
 * ```
 * 
 */
@ResourceType(type="vault:index/sysConfigCors:SysConfigCors")
public class SysConfigCors extends com.pulumi.resources.CustomResource {
    /**
     * Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
     * - `Content-Type`
     * - `X-Requested-With`
     * - `X-Vault-AWS-IAM-Server-ID`
     * - `X-Vault-MFA`
     * - `X-Vault-No-Request-Forwarding`
     * - `X-Vault-Wrap-Format`
     * - `X-Vault-Wrap-TTL`
     * - `X-Vault-Policy-Override`
     * - `Authorization`
     * - `X-Vault-Token`
     * 
     */
    @Export(name="allowedHeaders", refs={List.class,String.class}, tree="[0,1]")
    private Output</* @Nullable */ List<String>> allowedHeaders;

    /**
     * @return Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
     * - `Content-Type`
     * - `X-Requested-With`
     * - `X-Vault-AWS-IAM-Server-ID`
     * - `X-Vault-MFA`
     * - `X-Vault-No-Request-Forwarding`
     * - `X-Vault-Wrap-Format`
     * - `X-Vault-Wrap-TTL`
     * - `X-Vault-Policy-Override`
     * - `Authorization`
     * - `X-Vault-Token`
     * 
     */
    public Output<Optional<List<String>>> allowedHeaders() {
        return Codegen.optional(this.allowedHeaders);
    }
    /**
     * Set of origins permitted to make cross-origin requests. Use `&#34;*&#34;` as the only value to allow all origins. Must contain at least one origin.
     * 
     */
    @Export(name="allowedOrigins", refs={List.class,String.class}, tree="[0,1]")
    private Output<List<String>> allowedOrigins;

    /**
     * @return Set of origins permitted to make cross-origin requests. Use `&#34;*&#34;` as the only value to allow all origins. Must contain at least one origin.
     * 
     */
    public Output<List<String>> allowedOrigins() {
        return this.allowedOrigins;
    }
    /**
     * (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
     * 
     */
    @Export(name="enabled", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> enabled;

    /**
     * @return (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
     * 
     */
    public Output<Boolean> enabled() {
        return this.enabled;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public SysConfigCors(java.lang.String name) {
        this(name, SysConfigCorsArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public SysConfigCors(java.lang.String name, SysConfigCorsArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public SysConfigCors(java.lang.String name, SysConfigCorsArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/sysConfigCors:SysConfigCors", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private SysConfigCors(java.lang.String name, Output<java.lang.String> id, @Nullable SysConfigCorsState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/sysConfigCors:SysConfigCors", name, state, makeResourceOptions(options, id), false);
    }

    private static SysConfigCorsArgs makeArgs(SysConfigCorsArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? SysConfigCorsArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static SysConfigCors get(java.lang.String name, Output<java.lang.String> id, @Nullable SysConfigCorsState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new SysConfigCors(name, id, state, options);
    }
}

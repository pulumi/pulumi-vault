// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault.keymgmt;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.keymgmt.DistributeKeyArgs;
import com.pulumi.vault.keymgmt.inputs.DistributeKeyState;
import java.lang.String;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Distributes a key from Vault&#39;s Key Management secrets engine to an external Key Management Service provider (AWS KMS, Azure Key Vault, or GCP Cloud KMS). This resource manages the distribution of cryptographic keys to external KMS systems, allowing them to be used for encryption, decryption, signing, and verification operations in cloud environments.
 * 
 * &gt; **Important**
 * All data provided in the resource configuration will be written in cleartext to
 * state and plan files generated by Terraform, and will appear in the console
 * output when Terraform runs. Protect these artifacts accordingly. See the main provider documentation for more details.
 * 
 * For more information on distributing keys with Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 * 
 * **Note** this feature is available only with Vault Enterprise.
 * 
 * ## Example Usage
 * 
 * ### Distribute to AWS KMS
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.keymgmt.Key;
 * import com.pulumi.vault.keymgmt.KeyArgs;
 * import com.pulumi.vault.keymgmt.AwsKms;
 * import com.pulumi.vault.keymgmt.AwsKmsArgs;
 * import com.pulumi.vault.keymgmt.DistributeKey;
 * import com.pulumi.vault.keymgmt.DistributeKeyArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var keymgmt = new Mount("keymgmt", MountArgs.builder()
 *             .path("keymgmt")
 *             .type("keymgmt")
 *             .build());
 * 
 *         var encryptionKey = new Key("encryptionKey", KeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("aws-encryption-key")
 *             .type("aes256-gcm96")
 *             .build());
 * 
 *         var aws = new AwsKms("aws", AwsKmsArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("aws-kms")
 *             .keyCollection("us-west-2")
 *             .credentialsWo(Map.ofEntries(
 *                 Map.entry("access_key", awsAccessKeyId),
 *                 Map.entry("secret_key", awsSecretAccessKey)
 *             ))
 *             .credentialsWoVersion(1)
 *             .build());
 * 
 *         var awsDist = new DistributeKey("awsDist", DistributeKeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .kmsName(aws.name())
 *             .keyName(encryptionKey.name())
 *             .purposes(            
 *                 "encrypt",
 *                 "decrypt")
 *             .protection("hsm")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Distribute to AWS KMS (Using Environment Variables)
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.keymgmt.AwsKms;
 * import com.pulumi.vault.keymgmt.AwsKmsArgs;
 * import com.pulumi.vault.keymgmt.DistributeKey;
 * import com.pulumi.vault.keymgmt.DistributeKeyArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         // When AWS credentials are configured via environment variables or IAM roles
 *         // on the Vault server, you can omit access_key and secret_key
 *         var aws = new AwsKms("aws", AwsKmsArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("aws-kms")
 *             .keyCollection("us-west-2")
 *             .build());
 * 
 *         var awsDist = new DistributeKey("awsDist", DistributeKeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .kmsName(aws.name())
 *             .keyName(encryptionKey.name())
 *             .purposes(            
 *                 "encrypt",
 *                 "decrypt")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Distribute to Azure Key Vault
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.keymgmt.Key;
 * import com.pulumi.vault.keymgmt.KeyArgs;
 * import com.pulumi.vault.keymgmt.AzureKms;
 * import com.pulumi.vault.keymgmt.AzureKmsArgs;
 * import com.pulumi.vault.keymgmt.DistributeKey;
 * import com.pulumi.vault.keymgmt.DistributeKeyArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var signingKey = new Key("signingKey", KeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("azure-signing-key")
 *             .type("rsa-2048")
 *             .build());
 * 
 *         var azure = new AzureKms("azure", AzureKmsArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("azure-kv")
 *             .keyCollection("my-keyvault")
 *             .credentialsWo(Map.ofEntries(
 *                 Map.entry("tenant_id", azureTenantId),
 *                 Map.entry("client_id", azureClientId),
 *                 Map.entry("client_secret", azureClientSecret)
 *             ))
 *             .credentialsWoVersion(1)
 *             .build());
 * 
 *         var azureDist = new DistributeKey("azureDist", DistributeKeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .kmsName(azure.name())
 *             .keyName(signingKey.name())
 *             .purposes(            
 *                 "sign",
 *                 "verify")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Distribute to GCP Cloud KMS
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.keymgmt.Key;
 * import com.pulumi.vault.keymgmt.KeyArgs;
 * import com.pulumi.vault.keymgmt.GcpKms;
 * import com.pulumi.vault.keymgmt.GcpKmsArgs;
 * import com.pulumi.std.StdFunctions;
 * import com.pulumi.std.inputs.FileArgs;
 * import com.pulumi.vault.keymgmt.DistributeKey;
 * import com.pulumi.vault.keymgmt.DistributeKeyArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var gcpKey = new Key("gcpKey", KeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("gcp-encryption-key")
 *             .type("aes256-gcm96")
 *             .build());
 * 
 *         var gcp = new GcpKms("gcp", GcpKmsArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("gcp-kms")
 *             .keyCollection("projects/my-project/locations/us-central1/keyRings/my-keyring")
 *             .credentialsWo(Map.ofEntries(
 *                 Map.entry("service_account_file", StdFunctions.file(FileArgs.builder()
 *                     .input("gcp-credentials.json")
 *                     .build()).result()),
 *                 Map.entry("project", "my-project"),
 *                 Map.entry("location", "us-central1")
 *             ))
 *             .credentialsWoVersion(1)
 *             .build());
 * 
 *         var gcpDist = new DistributeKey("gcpDist", DistributeKeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .kmsName(gcp.name())
 *             .keyName(gcpKey.name())
 *             .purposes(            
 *                 "encrypt",
 *                 "decrypt")
 *             .protection("software")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Import
 * 
 * Key distributions can be imported using the format `{path}/kms/{kms_name}/key/{key_name}`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:keymgmt/distributeKey:DistributeKey aws_dist keymgmt/kms/aws-kms/key/aws-encryption-key
 * ```
 * 
 * &gt; **Note:** When importing, the `purpose` and `protection` fields are not returned by the Vault API and will not be populated in state. Both fields carry `Forces new resource`, so a missing or mismatched value will trigger a destroy+recreate on the next `pulumi up`. Always set `purpose` (and `protection` if not using the default `hsm`) explicitly in your configuration after import.
 * 
 */
@ResourceType(type="vault:keymgmt/distributeKey:DistributeKey")
public class DistributeKey extends com.pulumi.resources.CustomResource {
    /**
     * Specifies the name of the key to distribute to the given KMS provider.
     * 
     */
    @Export(name="keyName", refs={String.class}, tree="[0]")
    private Output<String> keyName;

    /**
     * @return Specifies the name of the key to distribute to the given KMS provider.
     * 
     */
    public Output<String> keyName() {
        return this.keyName;
    }
    /**
     * Specifies the name of the KMS provider to distribute the given key to.
     * 
     */
    @Export(name="kmsName", refs={String.class}, tree="[0]")
    private Output<String> kmsName;

    /**
     * @return Specifies the name of the KMS provider to distribute the given key to.
     * 
     */
    public Output<String> kmsName() {
        return this.kmsName;
    }
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = &#34;keymgmt&#34;`. Use
     * `vault_mount.keymgmt.path` here.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = &#34;keymgmt&#34;`. Use
     * `vault_mount.keymgmt.path` here.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }
    /**
     * Specifies the protection of the key. The protection defines where cryptographic operations are performed with the key in the KMS provider. The following values are supported: `hsm`, `software`. Defaults to `hsm`.
     * 
     */
    @Export(name="protection", refs={String.class}, tree="[0]")
    private Output<String> protection;

    /**
     * @return Specifies the protection of the key. The protection defines where cryptographic operations are performed with the key in the KMS provider. The following values are supported: `hsm`, `software`. Defaults to `hsm`.
     * 
     */
    public Output<String> protection() {
        return this.protection;
    }
    /**
     * Specifies the purpose of the key. The purpose defines a set of cryptographic capabilities that the key will have in the KMS provider. A key must have at least one of the supported purposes. The following values are supported : encrypt, decrypt, sign, verify, wrap, unwrap.
     * 
     */
    @Export(name="purposes", refs={List.class,String.class}, tree="[0,1]")
    private Output<List<String>> purposes;

    /**
     * @return Specifies the purpose of the key. The purpose defines a set of cryptographic capabilities that the key will have in the KMS provider. A key must have at least one of the supported purposes. The following values are supported : encrypt, decrypt, sign, verify, wrap, unwrap.
     * 
     */
    public Output<List<String>> purposes() {
        return this.purposes;
    }
    /**
     * Map of distributed key versions to their identifiers in the KMS provider.
     * 
     */
    @Export(name="versions", refs={Map.class,String.class}, tree="[0,1,1]")
    private Output<Map<String,String>> versions;

    /**
     * @return Map of distributed key versions to their identifiers in the KMS provider.
     * 
     */
    public Output<Map<String,String>> versions() {
        return this.versions;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public DistributeKey(java.lang.String name) {
        this(name, DistributeKeyArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public DistributeKey(java.lang.String name, DistributeKeyArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public DistributeKey(java.lang.String name, DistributeKeyArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:keymgmt/distributeKey:DistributeKey", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private DistributeKey(java.lang.String name, Output<java.lang.String> id, @Nullable DistributeKeyState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:keymgmt/distributeKey:DistributeKey", name, state, makeResourceOptions(options, id), false);
    }

    private static DistributeKeyArgs makeArgs(DistributeKeyArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? DistributeKeyArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static DistributeKey get(java.lang.String name, Output<java.lang.String> id, @Nullable DistributeKeyState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new DistributeKey(name, id, state, options);
    }
}

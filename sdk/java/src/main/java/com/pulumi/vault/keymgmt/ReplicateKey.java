// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault.keymgmt;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.keymgmt.ReplicateKeyArgs;
import com.pulumi.vault.keymgmt.inputs.ReplicateKeyState;
import java.lang.String;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages key replication across multiple AWS regions in AWS KMS. This resource enables AWS KMS multi-region keys, allowing the same key material to be used across different AWS regions for high availability and disaster recovery.
 * 
 * **Note:** This resource is only applicable to AWS KMS and does not work with Azure Key Vault or GCP Cloud KMS.
 * 
 * When you replicate a key to AWS KMS, it becomes a multi-region key that can be used for encryption and decryption operations in multiple AWS regions while maintaining the same key material.
 * 
 * &gt; **Important**
 * All data provided in the resource configuration will be written in cleartext to
 * state and plan files generated by Terraform, and will appear in the console
 * output when Terraform runs. Protect these artifacts accordingly. See the main provider documentation for more details.
 * 
 * For more information on AWS KMS multi-region keys with Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 * 
 * **Note** this feature is available only with Vault Enterprise and requires a configured AWS KMS provider.
 * 
 * ## Example Usage
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.keymgmt.Key;
 * import com.pulumi.vault.keymgmt.KeyArgs;
 * import com.pulumi.vault.keymgmt.AwsKms;
 * import com.pulumi.vault.keymgmt.AwsKmsArgs;
 * import com.pulumi.vault.keymgmt.DistributeKey;
 * import com.pulumi.vault.keymgmt.DistributeKeyArgs;
 * import com.pulumi.vault.keymgmt.ReplicateKey;
 * import com.pulumi.vault.keymgmt.ReplicateKeyArgs;
 * import com.pulumi.resources.CustomResourceOptions;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var keymgmt = new Mount("keymgmt", MountArgs.builder()
 *             .path("keymgmt")
 *             .type("keymgmt")
 *             .build());
 * 
 *         var key = new Key("key", KeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("multi-region-key")
 *             .type("aes256-gcm96")
 *             .replicaRegions(            
 *                 "us-east-1",
 *                 "eu-west-1")
 *             .build());
 * 
 *         var aws = new AwsKms("aws", AwsKmsArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("aws-kms")
 *             .keyCollection("us-west-2")
 *             .credentialsWo(Map.ofEntries(
 *                 Map.entry("access_key", awsAccessKeyId),
 *                 Map.entry("secret_key", awsSecretAccessKey)
 *             ))
 *             .credentialsWoVersion(1)
 *             .build());
 * 
 *         var dist = new DistributeKey("dist", DistributeKeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .kmsName(aws.name())
 *             .keyName(key.name())
 *             .purposes(            
 *                 "encrypt",
 *                 "decrypt")
 *             .build());
 * 
 *         var replicate = new ReplicateKey("replicate", ReplicateKeyArgs.builder()
 *             .mount(keymgmt.path())
 *             .kmsName(aws.name())
 *             .keyName(key.name())
 *             .build(), CustomResourceOptions.builder()
 *                 .dependsOn(dist)
 *                 .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Import
 * 
 * Key replication resources can be imported using the format `{path}/kms/{kms_name}/key/{key_name}/replicate`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:keymgmt/replicateKey:ReplicateKey us_east keymgmt/kms/aws-us-west-2/key/multi-region-key/replicate
 * ```
 * 
 * **Note**: The key must have `replicaRegions` configured in `vault.keymgmt.Key` and must be distributed to the AWS KMS provider using `vault.keymgmt.DistributeKey` before replication.
 * 
 */
@ResourceType(type="vault:keymgmt/replicateKey:ReplicateKey")
public class ReplicateKey extends com.pulumi.resources.CustomResource {
    /**
     * Specifies the name of the key to replicate. The key must have `replicaRegions` configured and must already be distributed to the AWS KMS provider.
     * 
     */
    @Export(name="keyName", refs={String.class}, tree="[0]")
    private Output<String> keyName;

    /**
     * @return Specifies the name of the key to replicate. The key must have `replicaRegions` configured and must already be distributed to the AWS KMS provider.
     * 
     */
    public Output<String> keyName() {
        return this.keyName;
    }
    /**
     * Specifies the name of the AWS KMS provider.
     * 
     */
    @Export(name="kmsName", refs={String.class}, tree="[0]")
    private Output<String> kmsName;

    /**
     * @return Specifies the name of the AWS KMS provider.
     * 
     */
    public Output<String> kmsName() {
        return this.kmsName;
    }
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = &#34;keymgmt&#34;`. Use
     * `vault_mount.keymgmt.path` here.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = &#34;keymgmt&#34;`. Use
     * `vault_mount.keymgmt.path` here.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public ReplicateKey(java.lang.String name) {
        this(name, ReplicateKeyArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public ReplicateKey(java.lang.String name, ReplicateKeyArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public ReplicateKey(java.lang.String name, ReplicateKeyArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:keymgmt/replicateKey:ReplicateKey", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private ReplicateKey(java.lang.String name, Output<java.lang.String> id, @Nullable ReplicateKeyState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:keymgmt/replicateKey:ReplicateKey", name, state, makeResourceOptions(options, id), false);
    }

    private static ReplicateKeyArgs makeArgs(ReplicateKeyArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? ReplicateKeyArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static ReplicateKey get(java.lang.String name, Output<java.lang.String> id, @Nullable ReplicateKeyState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new ReplicateKey(name, id, state, options);
    }
}

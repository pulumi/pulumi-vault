// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import java.lang.Boolean;
import java.lang.String;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class SysConfigCorsState extends com.pulumi.resources.ResourceArgs {

    public static final SysConfigCorsState Empty = new SysConfigCorsState();

    /**
     * Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
     * - `Content-Type`
     * - `X-Requested-With`
     * - `X-Vault-AWS-IAM-Server-ID`
     * - `X-Vault-MFA`
     * - `X-Vault-No-Request-Forwarding`
     * - `X-Vault-Wrap-Format`
     * - `X-Vault-Wrap-TTL`
     * - `X-Vault-Policy-Override`
     * - `Authorization`
     * - `X-Vault-Token`
     * 
     */
    @Import(name="allowedHeaders")
    private @Nullable Output<List<String>> allowedHeaders;

    /**
     * @return Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
     * - `Content-Type`
     * - `X-Requested-With`
     * - `X-Vault-AWS-IAM-Server-ID`
     * - `X-Vault-MFA`
     * - `X-Vault-No-Request-Forwarding`
     * - `X-Vault-Wrap-Format`
     * - `X-Vault-Wrap-TTL`
     * - `X-Vault-Policy-Override`
     * - `Authorization`
     * - `X-Vault-Token`
     * 
     */
    public Optional<Output<List<String>>> allowedHeaders() {
        return Optional.ofNullable(this.allowedHeaders);
    }

    /**
     * Set of origins permitted to make cross-origin requests. Use `&#34;*&#34;` as the only value to allow all origins. Must contain at least one origin.
     * 
     */
    @Import(name="allowedOrigins")
    private @Nullable Output<List<String>> allowedOrigins;

    /**
     * @return Set of origins permitted to make cross-origin requests. Use `&#34;*&#34;` as the only value to allow all origins. Must contain at least one origin.
     * 
     */
    public Optional<Output<List<String>>> allowedOrigins() {
        return Optional.ofNullable(this.allowedOrigins);
    }

    /**
     * (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
     * 
     */
    @Import(name="enabled")
    private @Nullable Output<Boolean> enabled;

    /**
     * @return (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
     * 
     */
    public Optional<Output<Boolean>> enabled() {
        return Optional.ofNullable(this.enabled);
    }

    private SysConfigCorsState() {}

    private SysConfigCorsState(SysConfigCorsState $) {
        this.allowedHeaders = $.allowedHeaders;
        this.allowedOrigins = $.allowedOrigins;
        this.enabled = $.enabled;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(SysConfigCorsState defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private SysConfigCorsState $;

        public Builder() {
            $ = new SysConfigCorsState();
        }

        public Builder(SysConfigCorsState defaults) {
            $ = new SysConfigCorsState(Objects.requireNonNull(defaults));
        }

        /**
         * @param allowedHeaders Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
         * - `Content-Type`
         * - `X-Requested-With`
         * - `X-Vault-AWS-IAM-Server-ID`
         * - `X-Vault-MFA`
         * - `X-Vault-No-Request-Forwarding`
         * - `X-Vault-Wrap-Format`
         * - `X-Vault-Wrap-TTL`
         * - `X-Vault-Policy-Override`
         * - `Authorization`
         * - `X-Vault-Token`
         * 
         * @return builder
         * 
         */
        public Builder allowedHeaders(@Nullable Output<List<String>> allowedHeaders) {
            $.allowedHeaders = allowedHeaders;
            return this;
        }

        /**
         * @param allowedHeaders Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
         * - `Content-Type`
         * - `X-Requested-With`
         * - `X-Vault-AWS-IAM-Server-ID`
         * - `X-Vault-MFA`
         * - `X-Vault-No-Request-Forwarding`
         * - `X-Vault-Wrap-Format`
         * - `X-Vault-Wrap-TTL`
         * - `X-Vault-Policy-Override`
         * - `Authorization`
         * - `X-Vault-Token`
         * 
         * @return builder
         * 
         */
        public Builder allowedHeaders(List<String> allowedHeaders) {
            return allowedHeaders(Output.of(allowedHeaders));
        }

        /**
         * @param allowedHeaders Set of additional custom headers allowed on cross-origin requests. Vault automatically includes standard headers, so only specify custom headers here. The standard headers that are always included are:
         * - `Content-Type`
         * - `X-Requested-With`
         * - `X-Vault-AWS-IAM-Server-ID`
         * - `X-Vault-MFA`
         * - `X-Vault-No-Request-Forwarding`
         * - `X-Vault-Wrap-Format`
         * - `X-Vault-Wrap-TTL`
         * - `X-Vault-Policy-Override`
         * - `Authorization`
         * - `X-Vault-Token`
         * 
         * @return builder
         * 
         */
        public Builder allowedHeaders(String... allowedHeaders) {
            return allowedHeaders(List.of(allowedHeaders));
        }

        /**
         * @param allowedOrigins Set of origins permitted to make cross-origin requests. Use `&#34;*&#34;` as the only value to allow all origins. Must contain at least one origin.
         * 
         * @return builder
         * 
         */
        public Builder allowedOrigins(@Nullable Output<List<String>> allowedOrigins) {
            $.allowedOrigins = allowedOrigins;
            return this;
        }

        /**
         * @param allowedOrigins Set of origins permitted to make cross-origin requests. Use `&#34;*&#34;` as the only value to allow all origins. Must contain at least one origin.
         * 
         * @return builder
         * 
         */
        public Builder allowedOrigins(List<String> allowedOrigins) {
            return allowedOrigins(Output.of(allowedOrigins));
        }

        /**
         * @param allowedOrigins Set of origins permitted to make cross-origin requests. Use `&#34;*&#34;` as the only value to allow all origins. Must contain at least one origin.
         * 
         * @return builder
         * 
         */
        public Builder allowedOrigins(String... allowedOrigins) {
            return allowedOrigins(List.of(allowedOrigins));
        }

        /**
         * @param enabled (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
         * 
         * @return builder
         * 
         */
        public Builder enabled(@Nullable Output<Boolean> enabled) {
            $.enabled = enabled;
            return this;
        }

        /**
         * @param enabled (Computed) Whether CORS is currently enabled. Vault automatically sets this to `true` when `allowedOrigins` is configured, and `false` when CORS is deleted.
         * 
         * @return builder
         * 
         */
        public Builder enabled(Boolean enabled) {
            return enabled(Output.of(enabled));
        }

        public SysConfigCorsState build() {
            return $;
        }
    }

}

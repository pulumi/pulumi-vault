// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault.config;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.config.UiHeaderArgs;
import com.pulumi.vault.config.inputs.UiHeaderState;
import java.lang.String;
import java.util.List;
import javax.annotation.Nullable;

/**
 * Manages custom HTTP headers for the Vault UI. This resource allows you to configure
 * custom HTTP response headers that will be sent by the Vault UI, enabling security
 * policies, CORS configuration, and custom organizational headers.
 * 
 * &gt; **Important** This resource requires **Vault 1.16.0 or later**. The `sys/config/ui/headers` API endpoint was introduced in Vault 1.16.0.
 * 
 * &gt; **Important** All operations on this resource require the `sudo` capability on the
 * `sys/config/ui/headers/*` path.
 * 
 * ## Example Usage
 * 
 * ### Basic Header Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiHeader;
 * import com.pulumi.vault.config.UiHeaderArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var custom = new UiHeader("custom", UiHeaderArgs.builder()
 *             .name("X-Custom-Header")
 *             .values("custom-value")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Security Headers
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiHeader;
 * import com.pulumi.vault.config.UiHeaderArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         // Content Security Policy
 *         var csp = new UiHeader("csp", UiHeaderArgs.builder()
 *             .name("Content-Security-Policy")
 *             .values("default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'")
 *             .build());
 * 
 *         // X-Frame-Options
 *         var frameOptions = new UiHeader("frameOptions", UiHeaderArgs.builder()
 *             .name("X-Frame-Options")
 *             .values("DENY")
 *             .build());
 * 
 *         // Strict Transport Security
 *         var hsts = new UiHeader("hsts", UiHeaderArgs.builder()
 *             .name("Strict-Transport-Security")
 *             .values("max-age=31536000; includeSubDomains; preload")
 *             .build());
 * 
 *         // X-Content-Type-Options
 *         var contentTypeOptions = new UiHeader("contentTypeOptions", UiHeaderArgs.builder()
 *             .name("X-Content-Type-Options")
 *             .values("nosniff")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### CORS Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiHeader;
 * import com.pulumi.vault.config.UiHeaderArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var corsOrigin = new UiHeader("corsOrigin", UiHeaderArgs.builder()
 *             .name("Access-Control-Allow-Origin")
 *             .values(            
 *                 "https://example.com",
 *                 "https://app.example.com")
 *             .build());
 * 
 *         var corsMethods = new UiHeader("corsMethods", UiHeaderArgs.builder()
 *             .name("Access-Control-Allow-Methods")
 *             .values(            
 *                 "GET",
 *                 "POST",
 *                 "OPTIONS")
 *             .build());
 * 
 *         var corsHeaders = new UiHeader("corsHeaders", UiHeaderArgs.builder()
 *             .name("Access-Control-Allow-Headers")
 *             .values(            
 *                 "Content-Type",
 *                 "Authorization")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Multiple Values
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiHeader;
 * import com.pulumi.vault.config.UiHeaderArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var multiValue = new UiHeader("multiValue", UiHeaderArgs.builder()
 *             .name("X-Multi-Value-Header")
 *             .values(            
 *                 "value1",
 *                 "value2",
 *                 "value3")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Migration from vault.generic.Endpoint
 * 
 * If you&#39;re currently managing UI headers using `vault.generic.Endpoint`, you can migrate
 * to this dedicated resource:
 * 
 * ### Before (using generic_endpoint)
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.generic.Endpoint;
 * import com.pulumi.vault.generic.EndpointArgs;
 * import static com.pulumi.codegen.internal.Serialization.*;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var cspHeader = new Endpoint("cspHeader", EndpointArgs.builder()
 *             .path("sys/config/ui/headers/Content-Security-Policy")
 *             .dataJson(serializeJson(
 *                 jsonObject(
 *                     jsonProperty("values", jsonArray("default-src 'self'"))
 *                 )))
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### After (using dedicated resource)
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.config.UiHeader;
 * import com.pulumi.vault.config.UiHeaderArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var csp = new UiHeader("csp", UiHeaderArgs.builder()
 *             .name("Content-Security-Policy")
 *             .values("default-src 'self'")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Migration Steps
 * 
 * 1. Add the new `vault.config.UiHeader` resource to your configuration
 * 2. Import the existing header: `terraform import vault_config_ui_header.csp Content-Security-Policy`
 * 3. Remove the old `vault.generic.Endpoint` resource from your configuration
 * 4. Run `pulumi preview` to verify no changes are required
 * 
 * ## Important Notes
 * 
 * ### State Management
 * 
 * This resource follows Terraform best practices by reading the actual configuration from Vault
 * after create and update operations. This ensures that the Terraform state always reflects the
 * actual state in Vault, including any server-side processing or normalization of values.
 * 
 * ## Import
 * 
 * UI header configurations can be imported using the header `name`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:config/uiHeader:UiHeader csp Content-Security-Policy
 * ```
 * 
 */
@ResourceType(type="vault:config/uiHeader:UiHeader")
public class UiHeader extends com.pulumi.resources.CustomResource {
    /**
     * The name of the custom header (e.g., &#34;Content-Security-Policy&#34;, &#34;X-Frame-Options&#34;).
     * Changing this will recreate the resource.
     * 
     */
    @Export(name="name", refs={String.class}, tree="[0]")
    private Output<String> name;

    /**
     * @return The name of the custom header (e.g., &#34;Content-Security-Policy&#34;, &#34;X-Frame-Options&#34;).
     * Changing this will recreate the resource.
     * 
     */
    public Output<String> name() {
        return this.name;
    }
    /**
     * A set of values for the header. At least one value is required.
     * Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
     * 
     * &gt; **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
     * 
     */
    @Export(name="values", refs={List.class,String.class}, tree="[0,1]")
    private Output<List<String>> values;

    /**
     * @return A set of values for the header. At least one value is required.
     * Multiple values can be provided for headers that support them. Because this argument uses set semantics, value order is not preserved and duplicate values are removed.
     * 
     * &gt; **Important** This resource must be called from the **root namespace**. UI header configuration is a global setting that applies to the entire Vault cluster.
     * 
     */
    public Output<List<String>> values() {
        return this.values;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public UiHeader(java.lang.String name) {
        this(name, UiHeaderArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public UiHeader(java.lang.String name, UiHeaderArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public UiHeader(java.lang.String name, UiHeaderArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:config/uiHeader:UiHeader", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private UiHeader(java.lang.String name, Output<java.lang.String> id, @Nullable UiHeaderState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:config/uiHeader:UiHeader", name, state, makeResourceOptions(options, id), false);
    }

    private static UiHeaderArgs makeArgs(UiHeaderArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? UiHeaderArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static UiHeader get(java.lang.String name, Output<java.lang.String> id, @Nullable UiHeaderState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new UiHeader(name, id, state, options);
    }
}

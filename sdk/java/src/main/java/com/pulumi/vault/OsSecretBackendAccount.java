// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.OsSecretBackendAccountArgs;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.inputs.OsSecretBackendAccountState;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.String;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages account configurations in the OS Secrets Engine. Accounts represent operating system user
 * accounts on remote hosts that Vault will manage, including automatic password rotation.
 * This resource requires Vault 2.0.0 or later.
 * 
 * &gt; **Important** The `passwordWo` field is write-only, create-only, and will not be read back from Vault.
 * It can only be set during resource creation. To update the password after creation, use the Vault CLI or API
 * to call the reset endpoint directly (see &#34;Password Management&#34; section below).
 * All data provided in the resource configuration will be written in cleartext to state and plan
 * files generated by Terraform. Protect these artifacts accordingly. See
 * the main provider documentation for more details.
 * 
 * See the [Vault documentation](https://www.vaultproject.io/docs/secrets/os) for more information.
 * 
 * The OS Secrets Engine mount itself is managed separately, typically with `vault.Mount`. This resource manages
 * accounts beneath an existing OS mount and host.
 * 
 * Before mounting the OS Secrets Engine, the external OS plugin must already be registered in Vault&#39;s plugin catalog.
 * You can register it with the `vault.Plugin` resource.
 * 
 * The examples below use the canonical plugin name `vault-plugin-secrets-os`. If your Vault cluster registers the
 * OS plugin under a different catalog name, use that name in `vault_mount.type` instead.
 * 
 * ## Example Usage
 * 
 * ### Register Plugin And Configure Account
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Plugin;
 * import com.pulumi.vault.PluginArgs;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import com.pulumi.vault.OsSecretBackendAccount;
 * import com.pulumi.vault.OsSecretBackendAccountArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Plugin("os", PluginArgs.builder()
 *             .type("secret")
 *             .name("vault-plugin-secrets-os")
 *             .version("v0.1.0+ent")
 *             .build());
 * 
 *         var osMount = new Mount("osMount", MountArgs.builder()
 *             .path("os")
 *             .type(os.name())
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(osMount.path())
 *             .build());
 * 
 *         var server = new OsSecretBackendHost("server", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("web-server")
 *             .address("192.168.1.100")
 *             .port(22)
 *             .build());
 * 
 *         var admin = new OsSecretBackendAccount("admin", OsSecretBackendAccountArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .host(server.name())
 *             .name("admin-account")
 *             .username("admin")
 *             .passwordWo("initial-secure-password-123")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Basic Account Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import com.pulumi.vault.OsSecretBackendAccount;
 * import com.pulumi.vault.OsSecretBackendAccountArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .build());
 * 
 *         var server = new OsSecretBackendHost("server", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("web-server")
 *             .address("192.168.1.100")
 *             .port(22)
 *             .build());
 * 
 *         var admin = new OsSecretBackendAccount("admin", OsSecretBackendAccountArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .host(server.name())
 *             .name("admin-account")
 *             .username("admin")
 *             .passwordWo("initial-secure-password-123")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Account with Rotation Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import com.pulumi.vault.OsSecretBackendAccount;
 * import com.pulumi.vault.OsSecretBackendAccountArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .build());
 * 
 *         var database = new OsSecretBackendHost("database", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("db-server")
 *             .address("10.0.1.50")
 *             .port(22)
 *             .build());
 * 
 *         var dbadmin = new OsSecretBackendAccount("dbadmin", OsSecretBackendAccountArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .host(database.name())
 *             .name("dbadmin")
 *             .username("postgres")
 *             .passwordWo("initial-password-456")
 *             .rotationPeriod(86400)
 *             .verifyConnection(false)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Account with Scheduled Rotation
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import com.pulumi.vault.OsSecretBackendHost;
 * import com.pulumi.vault.OsSecretBackendHostArgs;
 * import com.pulumi.vault.OsSecretBackendAccount;
 * import com.pulumi.vault.OsSecretBackendAccountArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .build());
 * 
 *         var appServer = new OsSecretBackendHost("appServer", OsSecretBackendHostArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .name("app-01")
 *             .address("192.168.1.200")
 *             .port(22)
 *             .build());
 * 
 *         var service = new OsSecretBackendAccount("service", OsSecretBackendAccountArgs.builder()
 *             .mount(osOsSecretBackend.mount())
 *             .host(appServer.name())
 *             .name("service-account")
 *             .username("appuser")
 *             .passwordWo(initialPassword)
 *             .rotationSchedule("0 3 * * 0")
 *             .rotationWindow(3600)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Password Management
 * 
 * The `passwordWo` field is **create-only** by design. This aligns with Vault&#39;s security model where password updates
 * should be performed through dedicated reset operations rather than general configuration updates.
 * 
 * ### Updating Passwords After Creation
 * 
 * To update an account password after the resource has been created, use the Vault CLI or API directly:
 * 
 * **Using Vault CLI:**
 * 
 * **Using Vault API:**
 * 
 * **Example:**
 * 
 * This approach ensures that password updates are explicit operations separate from other configuration changes,
 * providing better audit trails and security controls.
 * 
 * ## Notes
 * 
 * * This resource requires Vault 2.0.0 or later.
 * * The OS Secrets Engine plugin must be registered before the mount is enabled. Use `vault.Plugin` to manage catalog registration when appropriate.
 * * Use `vault.Mount` to create, tune, or remove the OS Secrets Engine mount before managing accounts with this resource.
 * * The account must reference an existing host configuration.
 * * The `passwordWo` field is write-only and create-only for security reasons. Once set, Vault will manage the password through rotation, but the current password cannot be retrieved through the API.
 * * To manually update a password after creation, use the Vault CLI or API reset endpoint (see &#34;Password Management&#34; section above).
 * * `verifyConnection` defaults to `true`. In environments where SSH connectivity is intentionally unavailable during creation, set it explicitly to `false`.
 * * Use either `rotationPeriod` or `rotationSchedule`.
 * * After the initial password is set, Vault will rotate it according to the configured schedule. Applications should retrieve the current password from Vault rather than storing it.
 * * Changing `mount`, `host`, `name`, or `passwordWo` will cause the resource to be recreated.
 * * The computed fields `lastVaultRotation` and `nextVaultRotation` are populated by Vault when rotation metadata is available.
 * 
 * ## Security Considerations
 * 
 * * Store the initial `passwordWo` value in a secure location such as Terraform variables or a secrets management system.
 * * Use `sensitive = true` in variable definitions for passwords.
 * * The `passwordWo` field is create-only and cannot be updated through Terraform after resource creation. This is intentional to enforce proper password management practices.
 * * For password updates after creation, use the Vault CLI or API reset endpoint, which provides proper audit logging.
 * * Ensure Terraform state files are properly secured as they will contain the initial password in cleartext.
 * * Applications should retrieve credentials from Vault dynamically rather than storing them.
 * * When importing existing accounts, use `lifecycle { ignoreChanges = [passwordWo] }` to prevent resource recreation.
 * 
 * ## Import
 * 
 * OS Secret backend account can be imported using the format `&lt;mount&gt;/hosts/&lt;host&gt;/accounts/&lt;name&gt;`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:index/osSecretBackendAccount:OsSecretBackendAccount admin os/hosts/web-server/accounts/admin-account
 * ```
 * 
 * &gt; **Note:** When importing, the `passwordWo` field will not be populated since it is write-only.
 * You must provide a placeholder value in your configuration (e.g., `passwordWo = &#34;PLACEHOLDER&#34;`).
 * After import, use `lifecycle { ignoreChanges = [passwordWo] }` to prevent Terraform from trying to recreate the resource.
 * 
 */
@ResourceType(type="vault:index/osSecretBackendAccount:OsSecretBackendAccount")
public class OsSecretBackendAccount extends com.pulumi.resources.CustomResource {
    /**
     * Custom metadata associated with the account.
     * 
     */
    @Export(name="customMetadata", refs={Map.class,String.class}, tree="[0,1,1]")
    private Output</* @Nullable */ Map<String,String>> customMetadata;

    /**
     * @return Custom metadata associated with the account.
     * 
     */
    public Output<Optional<Map<String,String>>> customMetadata() {
        return Codegen.optional(this.customMetadata);
    }
    /**
     * Disables automated rotation for the account.
     * 
     */
    @Export(name="disableAutomatedRotation", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> disableAutomatedRotation;

    /**
     * @return Disables automated rotation for the account.
     * 
     */
    public Output<Boolean> disableAutomatedRotation() {
        return this.disableAutomatedRotation;
    }
    /**
     * The name of the host where this account exists.
     * 
     */
    @Export(name="host", refs={String.class}, tree="[0]")
    private Output<String> host;

    /**
     * @return The name of the host where this account exists.
     * 
     */
    public Output<String> host() {
        return this.host;
    }
    /**
     * (Computed) The timestamp of the last password rotation performed by Vault.
     * 
     */
    @Export(name="lastVaultRotation", refs={String.class}, tree="[0]")
    private Output<String> lastVaultRotation;

    /**
     * @return (Computed) The timestamp of the last password rotation performed by Vault.
     * 
     */
    public Output<String> lastVaultRotation() {
        return this.lastVaultRotation;
    }
    /**
     * The path where the OS secrets engine is mounted.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return The path where the OS secrets engine is mounted.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * Unique name for the account within the host.
     * 
     */
    @Export(name="name", refs={String.class}, tree="[0]")
    private Output<String> name;

    /**
     * @return Unique name for the account within the host.
     * 
     */
    public Output<String> name() {
        return this.name;
    }
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }
    /**
     * (Computed) The timestamp when the next password rotation is scheduled to occur.
     * 
     */
    @Export(name="nextVaultRotation", refs={String.class}, tree="[0]")
    private Output<String> nextVaultRotation;

    /**
     * @return (Computed) The timestamp when the next password rotation is scheduled to occur.
     * 
     */
    public Output<String> nextVaultRotation() {
        return this.nextVaultRotation;
    }
    /**
     * Reference to a parent account that manages rotation for this account.
     * 
     */
    @Export(name="parentAccountRef", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> parentAccountRef;

    /**
     * @return Reference to a parent account that manages rotation for this account.
     * 
     */
    public Output<Optional<String>> parentAccountRef() {
        return Codegen.optional(this.parentAccountRef);
    }
    /**
     * Password policy to use for generated passwords.
     * 
     */
    @Export(name="passwordPolicy", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> passwordPolicy;

    /**
     * @return Password policy to use for generated passwords.
     * 
     */
    public Output<Optional<String>> passwordPolicy() {
        return Codegen.optional(this.passwordPolicy);
    }
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The initial password for the account. This field is write-only and create-only - it can only be set during resource creation and will not be read back from Vault. To update the password after creation, use the Vault CLI or API reset endpoint (see &#34;Password Management&#34; section below). Changing this value will force resource recreation.
     * 
     */
    @Export(name="passwordWo", refs={String.class}, tree="[0]")
    private Output<String> passwordWo;

    /**
     * @return **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The initial password for the account. This field is write-only and create-only - it can only be set during resource creation and will not be read back from Vault. To update the password after creation, use the Vault CLI or API reset endpoint (see &#34;Password Management&#34; section below). Changing this value will force resource recreation.
     * 
     */
    public Output<String> passwordWo() {
        return this.passwordWo;
    }
    /**
     * The period between automatic password rotations, in seconds. Mutually exclusive with `rotationSchedule`.
     * 
     */
    @Export(name="rotationPeriod", refs={Integer.class}, tree="[0]")
    private Output<Integer> rotationPeriod;

    /**
     * @return The period between automatic password rotations, in seconds. Mutually exclusive with `rotationSchedule`.
     * 
     */
    public Output<Integer> rotationPeriod() {
        return this.rotationPeriod;
    }
    /**
     * A cron-style schedule for password rotation (for example, `&#34;0 3 * * 0&#34;`). Mutually exclusive with `rotationPeriod`.
     * 
     */
    @Export(name="rotationSchedule", refs={String.class}, tree="[0]")
    private Output<String> rotationSchedule;

    /**
     * @return A cron-style schedule for password rotation (for example, `&#34;0 3 * * 0&#34;`). Mutually exclusive with `rotationPeriod`.
     * 
     */
    public Output<String> rotationSchedule() {
        return this.rotationSchedule;
    }
    /**
     * The rotation window, in seconds.
     * 
     */
    @Export(name="rotationWindow", refs={Integer.class}, tree="[0]")
    private Output<Integer> rotationWindow;

    /**
     * @return The rotation window, in seconds.
     * 
     */
    public Output<Integer> rotationWindow() {
        return this.rotationWindow;
    }
    /**
     * The username of the operating system account on the remote host.
     * 
     */
    @Export(name="username", refs={String.class}, tree="[0]")
    private Output<String> username;

    /**
     * @return The username of the operating system account on the remote host.
     * 
     */
    public Output<String> username() {
        return this.username;
    }
    /**
     * Whether Vault should verify the host connection and supplied credentials during account onboarding. Defaults to `true`.
     * 
     */
    @Export(name="verifyConnection", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> verifyConnection;

    /**
     * @return Whether Vault should verify the host connection and supplied credentials during account onboarding. Defaults to `true`.
     * 
     */
    public Output<Boolean> verifyConnection() {
        return this.verifyConnection;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public OsSecretBackendAccount(java.lang.String name) {
        this(name, OsSecretBackendAccountArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public OsSecretBackendAccount(java.lang.String name, OsSecretBackendAccountArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public OsSecretBackendAccount(java.lang.String name, OsSecretBackendAccountArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/osSecretBackendAccount:OsSecretBackendAccount", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private OsSecretBackendAccount(java.lang.String name, Output<java.lang.String> id, @Nullable OsSecretBackendAccountState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/osSecretBackendAccount:OsSecretBackendAccount", name, state, makeResourceOptions(options, id), false);
    }

    private static OsSecretBackendAccountArgs makeArgs(OsSecretBackendAccountArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? OsSecretBackendAccountArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .additionalSecretOutputs(List.of(
                "passwordWo"
            ))
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static OsSecretBackendAccount get(java.lang.String name, Output<java.lang.String> id, @Nullable OsSecretBackendAccountState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new OsSecretBackendAccount(name, id, state, options);
    }
}

// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.OsSecretBackendArgs;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.inputs.OsSecretBackendState;
import java.lang.Boolean;
import java.lang.Integer;
import java.lang.String;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages OS Secrets Engine backend configuration in a Vault server. The OS Secrets Engine manages credentials
 * for operating system accounts on remote hosts via SSH. This resource requires Vault 2.0.0 or later.
 * 
 * The OS Secrets Engine mount itself is managed separately, typically with `vault.Mount`. This resource only manages
 * backend configuration for an existing OS mount.
 * 
 * Before mounting the OS Secrets Engine, the external OS plugin must already be registered in Vault&#39;s plugin catalog.
 * You can register it with the `vault.Plugin` resource.
 * 
 * The examples below use the canonical plugin name `vault-plugin-secrets-os`. If your Vault cluster registers the
 * OS plugin under a different catalog name, use that name in `vault_mount.type` instead.
 * 
 * See the [Vault documentation](https://www.vaultproject.io/docs/secrets/os) for more information.
 * 
 * ## Example Usage
 * 
 * ### Register Plugin And Configure Backend
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Plugin;
 * import com.pulumi.vault.PluginArgs;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Plugin("os", PluginArgs.builder()
 *             .type("secret")
 *             .name("vault-plugin-secrets-os")
 *             .version("v0.1.0+ent")
 *             .build());
 * 
 *         var osMount = new Mount("osMount", MountArgs.builder()
 *             .path("os")
 *             .type(os.name())
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(osMount.path())
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Basic Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Advanced Configuration
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.OsSecretBackend;
 * import com.pulumi.vault.OsSecretBackendArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var os = new Mount("os", MountArgs.builder()
 *             .path("os-prod")
 *             .type("vault-plugin-secrets-os")
 *             .build());
 * 
 *         var osOsSecretBackend = new OsSecretBackend("osOsSecretBackend", OsSecretBackendArgs.builder()
 *             .mount(os.path())
 *             .maxVersions(10)
 *             .sshHostKeyTrustOnFirstUse(true)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Notes
 * 
 * * This resource requires Vault 2.0.0 or later.
 * * The OS Secrets Engine plugin must be registered before the mount is enabled. Use `vault.Plugin` to manage catalog registration when appropriate.
 * * Use `vault.Mount` to create, tune, or remove the OS Secrets Engine mount.
 * * When `sshHostKeyTrustOnFirstUse` is enabled, the first connection to a host will automatically trust and store its SSH host key.
 * 
 * ## Import
 * 
 * OS Secret backend can be imported using the `mount`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:index/osSecretBackend:OsSecretBackend os os
 * ```
 * 
 */
@ResourceType(type="vault:index/osSecretBackend:OsSecretBackend")
public class OsSecretBackend extends com.pulumi.resources.CustomResource {
    /**
     * The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
     * 
     */
    @Export(name="maxVersions", refs={Integer.class}, tree="[0]")
    private Output<Integer> maxVersions;

    /**
     * @return The maximum number of versions to keep. When omitted, Vault applies its server-side default of `10`. If you later remove the field from configuration, Vault retains the current value. Set to `0` to explicitly store zero in Vault.
     * 
     */
    public Output<Integer> maxVersions() {
        return this.maxVersions;
    }
    /**
     * The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return The path where the OS secrets engine is already mounted. Must not begin or end with a `/`.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace to provision the resource in.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }
    /**
     * If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
     * 
     */
    @Export(name="sshHostKeyTrustOnFirstUse", refs={Boolean.class}, tree="[0]")
    private Output<Boolean> sshHostKeyTrustOnFirstUse;

    /**
     * @return If `true`, SSH host keys will be trusted on first use (TOFU). If `false`, host keys must be explicitly configured. Defaults to `false`.
     * 
     */
    public Output<Boolean> sshHostKeyTrustOnFirstUse() {
        return this.sshHostKeyTrustOnFirstUse;
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public OsSecretBackend(java.lang.String name) {
        this(name, OsSecretBackendArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public OsSecretBackend(java.lang.String name, OsSecretBackendArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public OsSecretBackend(java.lang.String name, OsSecretBackendArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/osSecretBackend:OsSecretBackend", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private OsSecretBackend(java.lang.String name, Output<java.lang.String> id, @Nullable OsSecretBackendState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/osSecretBackend:OsSecretBackend", name, state, makeResourceOptions(options, id), false);
    }

    private static OsSecretBackendArgs makeArgs(OsSecretBackendArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? OsSecretBackendArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static OsSecretBackend get(java.lang.String name, Output<java.lang.String> id, @Nullable OsSecretBackendState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new OsSecretBackend(name, id, state, options);
    }
}

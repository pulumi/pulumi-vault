// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.vault;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Export;
import com.pulumi.core.annotations.ResourceType;
import com.pulumi.core.internal.Codegen;
import com.pulumi.vault.KeymgmtGcpKmsArgs;
import com.pulumi.vault.Utilities;
import com.pulumi.vault.inputs.KeymgmtGcpKmsState;
import java.lang.Integer;
import java.lang.String;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import javax.annotation.Nullable;

/**
 * Manages a GCP Cloud KMS provider in the Vault Key Management secrets engine. This resource configures Vault to integrate with Google Cloud Platform&#39;s Key Management Service, allowing keys created in Vault to be distributed to GCP Cloud KMS for use in GCP services.
 * 
 * Once configured, keys can be distributed to GCP Cloud KMS using the `vault.KeymgmtDistributeKey` resource.
 * 
 * &gt; **Important** This resource requires **Terraform 1.11+** for write-only attribute support.
 * The `credentialsWo` field is write-only and will never be stored in Terraform state.
 * See the main provider documentation for more details.
 * 
 * For more information on managing GCP Cloud KMS with Vault, please refer to the Vault [documentation](https://developer.hashicorp.com/vault/docs/secrets/key-management).
 * 
 * **Note** this feature is available only with Vault Enterprise.
 * 
 * ## Example Usage
 * 
 * ### Using Explicit Credentials
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.KeymgmtGcpKms;
 * import com.pulumi.vault.KeymgmtGcpKmsArgs;
 * import com.pulumi.std.StdFunctions;
 * import com.pulumi.std.inputs.FileArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         var keymgmt = new Mount("keymgmt", MountArgs.builder()
 *             .path("keymgmt")
 *             .type("keymgmt")
 *             .build());
 * 
 *         var production = new KeymgmtGcpKms("production", KeymgmtGcpKmsArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("gcp-production")
 *             .keyCollection("projects/my-project/locations/us-central1/keyRings/my-keyring")
 *             .credentialsWo(Map.ofEntries(
 *                 Map.entry("service_account_file", StdFunctions.file(FileArgs.builder()
 *                     .input("gcp-credentials.json")
 *                     .build()).result()),
 *                 Map.entry("project", "my-project"),
 *                 Map.entry("location", "us-central1")
 *             ))
 *             .credentialsWoVersion(1)
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ### Using GCP Application Default Credentials
 * 
 * <pre>
 * {@code
 * package generated_program;
 * 
 * import com.pulumi.Context;
 * import com.pulumi.Pulumi;
 * import com.pulumi.core.Output;
 * import com.pulumi.vault.Mount;
 * import com.pulumi.vault.MountArgs;
 * import com.pulumi.vault.KeymgmtGcpKms;
 * import com.pulumi.vault.KeymgmtGcpKmsArgs;
 * import java.util.ArrayList;
 * import java.util.Arrays;
 * import java.util.Map;
 * import java.io.File;
 * import java.nio.file.Files;
 * import java.nio.file.Paths;
 * 
 * public class App {
 *     public static void main(String[] args) {
 *         Pulumi.run(App::stack);
 *     }
 * 
 *     public static void stack(Context ctx) {
 *         // When credentials_wo is not provided, Vault uses its own environment to
 *         // authenticate with GCP. Supported options include:
 *         // 1. The GOOGLE_APPLICATION_CREDENTIALS environment variable set on the Vault
 *         //    server, pointing to a service account key file.
 *         // 2. Application Default Credentials (ADC) when Vault runs on GCP infrastructure
 *         //    with an attached service account.
 *         var keymgmt = new Mount("keymgmt", MountArgs.builder()
 *             .path("keymgmt")
 *             .type("keymgmt")
 *             .build());
 * 
 *         var production = new KeymgmtGcpKms("production", KeymgmtGcpKmsArgs.builder()
 *             .mount(keymgmt.path())
 *             .name("gcp-production")
 *             .keyCollection("projects/my-project/locations/us-central1/keyRings/my-keyring")
 *             .build());
 * 
 *     }
 * }
 * }
 * </pre>
 * 
 * ## Required GCP Permissions
 * 
 * The service account must be authorized with the following minimum IAM permissions on the target key ring resource:
 * 
 * - `cloudkms.cryptoKeys.create`
 * - `cloudkms.cryptoKeys.update`
 * - `cloudkms.importJobs.create`
 * - `cloudkms.importJobs.get`
 * - `cloudkms.importJobs.useToImport`
 * - `cloudkms.cryptoKeyVersions.list`
 * - `cloudkms.cryptoKeyVersions.destroy`
 * - `cloudkms.cryptoKeyVersions.update`
 * - `cloudkms.cryptoKeyVersions.create`
 * 
 * ## Import
 * 
 * GCP Cloud KMS providers can be imported using the format `{path}/kms/{name}`, e.g.
 * 
 * ```sh
 * $ pulumi import vault:index/keymgmtGcpKms:KeymgmtGcpKms production keymgmt/kms/gcp-production
 * ```
 * 
 * &gt; **Note:** Import sets the `mount` attribute from the import ID. The `credentialsWo` and `credentialsWoVersion` fields will not be populated as they are not returned by the Vault API. You must supply these values in your configuration after import. The corresponding `vault.Mount` resource must also be present in your configuration (or separately imported).
 * 
 */
@ResourceType(type="vault:index/keymgmtGcpKms:KeymgmtGcpKms")
public class KeymgmtGcpKms extends com.pulumi.resources.CustomResource {
    /**
     * **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The credentials to use for authentication with Google Cloud KMS. Supplying values for this parameter is optional, as credentials may also be specified through environment variables or Application Default Credentials. The order of precedence is environment variables, then the credentials provided to this parameter and Application Default Credentials. This value is write-only and will not be stored in Terraform state.
     * The following values are supported:
     * 
     */
    @Export(name="credentialsWo", refs={Map.class,String.class}, tree="[0,1,1]")
    private Output</* @Nullable */ Map<String,String>> credentialsWo;

    /**
     * @return **NOTE:** This field is write-only and its value will not be updated in state as part of read operations.
     * The credentials to use for authentication with Google Cloud KMS. Supplying values for this parameter is optional, as credentials may also be specified through environment variables or Application Default Credentials. The order of precedence is environment variables, then the credentials provided to this parameter and Application Default Credentials. This value is write-only and will not be stored in Terraform state.
     * The following values are supported:
     * 
     */
    public Output<Optional<Map<String,String>>> credentialsWo() {
        return Codegen.optional(this.credentialsWo);
    }
    /**
     * Version number for the write-only credentials. Increment this value to trigger a credential rotation. Changing this value will cause the credentials to be re-sent to Vault during the next apply. For more info see updating write-only attributes.
     * 
     */
    @Export(name="credentialsWoVersion", refs={Integer.class}, tree="[0]")
    private Output</* @Nullable */ Integer> credentialsWoVersion;

    /**
     * @return Version number for the write-only credentials. Increment this value to trigger a credential rotation. Changing this value will cause the credentials to be re-sent to Vault during the next apply. For more info see updating write-only attributes.
     * 
     */
    public Output<Optional<Integer>> credentialsWoVersion() {
        return Codegen.optional(this.credentialsWoVersion);
    }
    /**
     * Refers to the resource ID of an existing GCP Cloud KMS key ring. Cannot be changed after creation.
     * 
     */
    @Export(name="keyCollection", refs={String.class}, tree="[0]")
    private Output<String> keyCollection;

    /**
     * @return Refers to the resource ID of an existing GCP Cloud KMS key ring. Cannot be changed after creation.
     * 
     */
    public Output<String> keyCollection() {
        return this.keyCollection;
    }
    /**
     * Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = &#34;keymgmt&#34;`. Use
     * `vault_mount.keymgmt.path` here.
     * 
     */
    @Export(name="mount", refs={String.class}, tree="[0]")
    private Output<String> mount;

    /**
     * @return Path of the Key Management secrets engine mount. Must match the
     * `path` of a `vault.Mount` resource with `type = &#34;keymgmt&#34;`. Use
     * `vault_mount.keymgmt.path` here.
     * 
     */
    public Output<String> mount() {
        return this.mount;
    }
    /**
     * Specifies the name of the GCP Cloud KMS provider. Cannot be changed after creation.
     * 
     */
    @Export(name="name", refs={String.class}, tree="[0]")
    private Output<String> name;

    /**
     * @return Specifies the name of the GCP Cloud KMS provider. Cannot be changed after creation.
     * 
     */
    public Output<String> name() {
        return this.name;
    }
    /**
     * The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    @Export(name="namespace", refs={String.class}, tree="[0]")
    private Output</* @Nullable */ String> namespace;

    /**
     * @return The namespace of the target resource.
     * The value should not contain leading or trailing forward slashes.
     * The `namespace` is always relative to the provider&#39;s configured
     * [namespace](https://www.terraform.io/docs/providers/vault/index.html#namespace).
     * *Available only for Vault Enterprise*.
     * 
     */
    public Output<Optional<String>> namespace() {
        return Codegen.optional(this.namespace);
    }

    /**
     *
     * @param name The _unique_ name of the resulting resource.
     */
    public KeymgmtGcpKms(java.lang.String name) {
        this(name, KeymgmtGcpKmsArgs.Empty);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     */
    public KeymgmtGcpKms(java.lang.String name, KeymgmtGcpKmsArgs args) {
        this(name, args, null);
    }
    /**
     *
     * @param name The _unique_ name of the resulting resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param options A bag of options that control this resource's behavior.
     */
    public KeymgmtGcpKms(java.lang.String name, KeymgmtGcpKmsArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/keymgmtGcpKms:KeymgmtGcpKms", name, makeArgs(args, options), makeResourceOptions(options, Codegen.empty()), false);
    }

    private KeymgmtGcpKms(java.lang.String name, Output<java.lang.String> id, @Nullable KeymgmtGcpKmsState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        super("vault:index/keymgmtGcpKms:KeymgmtGcpKms", name, state, makeResourceOptions(options, id), false);
    }

    private static KeymgmtGcpKmsArgs makeArgs(KeymgmtGcpKmsArgs args, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        if (options != null && options.getUrn().isPresent()) {
            return null;
        }
        return args == null ? KeymgmtGcpKmsArgs.Empty : args;
    }

    private static com.pulumi.resources.CustomResourceOptions makeResourceOptions(@Nullable com.pulumi.resources.CustomResourceOptions options, @Nullable Output<java.lang.String> id) {
        var defaultOptions = com.pulumi.resources.CustomResourceOptions.builder()
            .version(Utilities.getVersion())
            .additionalSecretOutputs(List.of(
                "credentialsWo"
            ))
            .build();
        return com.pulumi.resources.CustomResourceOptions.merge(defaultOptions, options, id);
    }

    /**
     * Get an existing Host resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state
     * @param options Optional settings to control the behavior of the CustomResource.
     */
    public static KeymgmtGcpKms get(java.lang.String name, Output<java.lang.String> id, @Nullable KeymgmtGcpKmsState state, @Nullable com.pulumi.resources.CustomResourceOptions options) {
        return new KeymgmtGcpKms(name, id, state, options);
    }
}
